
    
---
title: Moth Records
date: "`r Sys.Date()`"
author: "Tim Newbold"
output:
  rmdformats::downcute:
    code_folding: show
    self_contained: true
    thumbnails: false
    lightbox: true
---   

```{r,echo=FALSE,results=FALSE}
moth.data <- read.csv("TimNewboldMothDataPublicRelease.csv")

total.individs <- sum(moth.data$NumberCaught,na.rm=TRUE)

total.species <- length(unique(na.omit(moth.data)$Species))

total.traps <- length(unique(moth.data$TrapNumber))

all.yrs <- seq(from=min(moth.data$Year),to=max(moth.data$Year))
```
    
# Overview

I have been trapping moths in Cambridgeshire since 8th August 2020.

Since, then I have run `r total.traps` traps, and caught `r total.individs` individual moths, of `r total.species` different species.

You can download my data <a href="https://timnewbold.github.io/TimNewboldMothDataPublicRelease.csv">here</a>.

```{r,echo=FALSE,results=TRUE}

traps <- unique(moth.data$TrapNumber)

sp.accum <- sapply(X = 1:length(traps),
                   FUN = function(i){
                     sub.data <- moth.data[(moth.data$TrapNumber<=traps[i]),]
                     
                     cum.sp <- length(unique(tolower(na.omit(sub.data$Species))))
                     
                   })

par(mar=c(3.2,3.2,0.5,0.2))
par(mgp=c(2.0,0.2,0))
par(tck=-0.01)
par(las=1)

plot(1:length(sp.accum),sp.accum,type="l",bty="l",col="#A40122",
     lwd=3,xlab="Trap number",ylab="Cumulative species count",
     xaxt="n")
axis(side = 1,at = 1:length(sp.accum))
```


```{r,echo=FALSE,results=TRUE}

TrapCountsByMonth <- tapply(X = moth.data$TrapNumber,
                            INDEX = moth.data$Year_Month,
                            FUN = function(x) return(
                              length(unique(x))))

barplot(height = TrapCountsByMonth,col="#A40122",
        xlab="Month",ylab="No. traps")
box(bty="l")
```


```{r,echo=FALSE,results=TRUE}
trap.sums <- data.frame(TotalCaught=tapply(X = moth.data$NumberCaught,
                                           INDEX = moth.data$TrapNumber,
                                           FUN = sum,na.rm=TRUE))

trap.sums$TrapNumber <- row.names(trap.sums)

trap.sums$Month <- moth.data$Month[match(trap.sums$TrapNumber,moth.data$TrapNumber)]

trap.sums$Year <- moth.data$Year[match(trap.sums$TrapNumber,moth.data$TrapNumber)]

colour.pal <- viridis::viridis(n = length(unique(trap.sums$Year)))

ymax <- max(tapply(
  X = trap.sums$TotalCaught,
  INDEX = paste(trap.sums$Month,trap.sums$Year),
  FUN = median))

par(mar=c(3.2,3.2,0.5,0.2))
par(mgp=c(2.0,0.2,0))
par(tck=-0.01)
par(las=1)

plot(9e99,9e99,xlim=c(0.5,12.5),ylim=c(0,sqrt(ymax)),
xlab="Month",ylab="Median Individuals Caught",yaxt="n")

x.pos <- seq(from=0,to=14,by=2)
axis(side = 2,at = x.pos,labels = x.pos^2)

invisible(mapply(FUN = function(yr.data,colour){
  
  month.avgs <- tapply(X = yr.data$TotalCaught,INDEX = yr.data$Month,FUN = median)
  
  points(names(month.avgs),sqrt(month.avgs),
         type="b",pch=16,col=colour)
  
},split(x = trap.sums,f = trap.sums$Year),as.list(colour.pal)))

month.avgs <- tapply(X = trap.sums$TotalCaught,INDEX = trap.sums$Month,FUN = median)

points(names(month.avgs),sqrt(month.avgs),type="b",pch=16,col="#00000099")

legend(x = 1,y = sqrt(ymax),legend = c(unique(trap.sums$Year),"All Years"),
bty="n",lty=1,col=c(colour.pal,"#00000099"))

```


```{r,echo=FALSE,results=TRUE}

species.counts.allyrs <- data.frame(SpeciesNumber=tapply(
  X = na.omit(moth.data)$Species,
  INDEX = na.omit(moth.data)$Month,
  FUN = function(spp){
    length(unique(spp))}))

species.counts.allyrs$Month <- row.names(species.counts.allyrs)  

species.counts <- data.frame(SpeciesNumber=tapply(
  X = na.omit(moth.data)$Species,
  INDEX = na.omit(moth.data)$Year_Month,
  FUN = function(spp){
    length(unique(spp))}))
    
species.counts$Year <- as.integer(sapply(
  X = row.names(species.counts),
  FUN = function(x) return(strsplit(x,"_")[[1]][1])))

species.counts$Month <- as.integer(sapply(
  X = row.names(species.counts),
  FUN = function(x) return(strsplit(x,"_")[[1]][2])))

ymax <- max(species.counts.allyrs$SpeciesNumber)

par(mar=c(3.2,3.2,0.5,0.2))
par(mgp=c(2.0,0.2,0))
par(tck=-0.01)
par(las=1)

plot(9e99,9e99,xlim=c(0.5,12.5),ylim=c(0,sqrt(ymax)),
     xlab="Month",ylab="Total species caught",yaxt="n")

x.pos <- seq(from=0,to=10,by=1)
axis(side = 2,at = x.pos,labels = x.pos^2)

invisible(mapply(FUN = function(yr.data,colour){
  
  points(yr.data$Month,sqrt(yr.data$SpeciesNumber),
         type="b",pch=16,col=colour)
  
},split(x = species.counts,f = species.counts$Year),as.list(colour.pal)))

points(species.counts.allyrs$Month,sqrt(species.counts.allyrs$SpeciesNumber),
       type="b",pch=16,col="#00000099")

legend(x = 1,y = sqrt(ymax),legend = c(unique(trap.sums$Year),"All years"),
bty="n",lty=1,col=c(colour.pal,"#00000099"))
```

```{r,echo=FALSE,results=TRUE}
suppressWarnings(suppressMessages(library(treemap)))

species.sums <- tapply(X = moth.data$NumberCaught,
                       INDEX = moth.data$Species,
                       FUN = sum)

species.sums <- data.frame(Sp=names(species.sums),
                           N=species.sums)

treemap(dtf = species.sums,index = "Sp",vSize = "N")
```



# By Month

## January

```{r,echo=FALSE,results=TRUE}
month.data <- moth.data[(moth.data$Month==1),]
trap.sums <- tapply(X = month.data$NumberCaught,
                    INDEX = month.data$TrapNumber,
                    FUN = sum)

trap.sums <- data.frame(AvgCatch=trap.sums,
                        Year=month.data$Year[
                          match(names(trap.sums),
                                month.data$TrapNumber)])

yr.avg.month.catch <- tapply(X = trap.sums$AvgCatch,
                             INDEX = trap.sums$Year,
                             FUN = mean,na.rm=TRUE)

yr.avg.month.catch[is.na(yr.avg.month.catch)] <- 0

par(mar=c(3.2,3.2,0.5,0.2))
par(mgp=c(2.0,0.2,0))
par(tck=-0.01)
par(las=1)

plot(names(yr.avg.month.catch),yr.avg.month.catch,
     xaxp=c(min(all.yrs),max(all.yrs),length(all.yrs)-1),
     xlab="Year",ylab="Average Catch",
     pch=16,type="b",col="#A40122",bty="l")

month.sp.counts.by.year <- tapply(X = month.data$Species,
                                  INDEX = month.data$Year,
                                  FUN = function(x) 
                                    return(length(unique(x[!is.na(x)]))))


par(mar=c(3.2,3.2,0.5,0.2))
par(mgp=c(2.0,0.2,0))
par(tck=-0.01)
par(las=1)

plot(names(month.sp.counts.by.year),month.sp.counts.by.year,
     xaxp=c(min(all.yrs),max(all.yrs),length(all.yrs)-1),
     xlab="Year",ylab="Total Number of Species",
     pch=16,type="b",col="#A40122",bty="l")

month.species.counts <- tapply(X = month.data$NumberCaught,
                               INDEX = month.data$Species,
                               FUN = sum)

month.species.counts <- data.frame(Sp=names(month.species.counts),
                                   N=month.species.counts)

treemap(dtf = month.species.counts,index = "Sp",vSize = "N")
```

## February

```{r,echo=FALSE,results=TRUE}
month.data <- moth.data[(moth.data$Month==2),]
trap.sums <- tapply(X = month.data$NumberCaught,
                    INDEX = month.data$TrapNumber,
                    FUN = sum)

trap.sums <- data.frame(AvgCatch=trap.sums,
                        Year=month.data$Year[
                          match(names(trap.sums),
                                month.data$TrapNumber)])

yr.avg.month.catch <- tapply(X = trap.sums$AvgCatch,
                             INDEX = trap.sums$Year,
                             FUN = mean,na.rm=TRUE)

yr.avg.month.catch[is.na(yr.avg.month.catch)] <- 0

par(mar=c(3.2,3.2,0.5,0.2))
par(mgp=c(2.0,0.2,0))
par(tck=-0.01)
par(las=1)

plot(names(yr.avg.month.catch),yr.avg.month.catch,
     xaxp=c(min(all.yrs),max(all.yrs),length(all.yrs)-1),
     xlab="Year",ylab="Average Catch",
     pch=16,type="b",col="#A40122",bty="l")

month.sp.counts.by.year <- tapply(X = month.data$Species,
                                  INDEX = month.data$Year,
                                  FUN = function(x) 
                                    return(length(unique(x[!is.na(x)]))))


par(mar=c(3.2,3.2,0.5,0.2))
par(mgp=c(2.0,0.2,0))
par(tck=-0.01)
par(las=1)

plot(names(month.sp.counts.by.year),month.sp.counts.by.year,
     xaxp=c(min(all.yrs),max(all.yrs),length(all.yrs)-1),
     xlab="Year",ylab="Total Number of Species",
     pch=16,type="b",col="#A40122",bty="l")

month.species.counts <- tapply(X = month.data$NumberCaught,
                               INDEX = month.data$Species,
                               FUN = sum)

month.species.counts <- data.frame(Sp=names(month.species.counts),
                                   N=month.species.counts)

treemap(dtf = month.species.counts,index = "Sp",vSize = "N")
```

## March

```{r,echo=FALSE,results=TRUE}
month.data <- moth.data[(moth.data$Month==3),]
trap.sums <- tapply(X = month.data$NumberCaught,
                    INDEX = month.data$TrapNumber,
                    FUN = sum)

trap.sums <- data.frame(AvgCatch=trap.sums,
                        Year=month.data$Year[
                          match(names(trap.sums),
                                month.data$TrapNumber)])

yr.avg.month.catch <- tapply(X = trap.sums$AvgCatch,
                             INDEX = trap.sums$Year,
                             FUN = mean,na.rm=TRUE)

yr.avg.month.catch[is.na(yr.avg.month.catch)] <- 0

par(mar=c(3.2,3.2,0.5,0.2))
par(mgp=c(2.0,0.2,0))
par(tck=-0.01)
par(las=1)

plot(names(yr.avg.month.catch),yr.avg.month.catch,
     xaxp=c(min(all.yrs),max(all.yrs),length(all.yrs)-1),
     xlab="Year",ylab="Average Catch",
     pch=16,type="b",col="#A40122",bty="l")

month.sp.counts.by.year <- tapply(X = month.data$Species,
                                  INDEX = month.data$Year,
                                  FUN = function(x) 
                                    return(length(unique(x[!is.na(x)]))))


par(mar=c(3.2,3.2,0.5,0.2))
par(mgp=c(2.0,0.2,0))
par(tck=-0.01)
par(las=1)

plot(names(month.sp.counts.by.year),month.sp.counts.by.year,
     xaxp=c(min(all.yrs),max(all.yrs),length(all.yrs)-1),
     xlab="Year",ylab="Total Number of Species",
     pch=16,type="b",col="#A40122",bty="l")

month.species.counts <- tapply(X = month.data$NumberCaught,
                               INDEX = month.data$Species,
                               FUN = sum)

month.species.counts <- data.frame(Sp=names(month.species.counts),
                                   N=month.species.counts)

treemap(dtf = month.species.counts,index = "Sp",vSize = "N")
```

## April

```{r,echo=FALSE,results=TRUE}
month.data <- moth.data[(moth.data$Month==4),]
trap.sums <- tapply(X = month.data$NumberCaught,
                    INDEX = month.data$TrapNumber,
                    FUN = sum)

trap.sums <- data.frame(AvgCatch=trap.sums,
                        Year=month.data$Year[
                          match(names(trap.sums),
                                month.data$TrapNumber)])

yr.avg.month.catch <- tapply(X = trap.sums$AvgCatch,
                             INDEX = trap.sums$Year,
                             FUN = mean,na.rm=TRUE)

yr.avg.month.catch[is.na(yr.avg.month.catch)] <- 0

par(mar=c(3.2,3.2,0.5,0.2))
par(mgp=c(2.0,0.2,0))
par(tck=-0.01)
par(las=1)

plot(names(yr.avg.month.catch),yr.avg.month.catch,
     xaxp=c(min(all.yrs),max(all.yrs),length(all.yrs)-1),
     xlab="Year",ylab="Average Catch",
     pch=16,type="b",col="#A40122",bty="l")

month.sp.counts.by.year <- tapply(X = month.data$Species,
                                  INDEX = month.data$Year,
                                  FUN = function(x) 
                                    return(length(unique(x[!is.na(x)]))))


par(mar=c(3.2,3.2,0.5,0.2))
par(mgp=c(2.0,0.2,0))
par(tck=-0.01)
par(las=1)

plot(names(month.sp.counts.by.year),month.sp.counts.by.year,
     xaxp=c(min(all.yrs),max(all.yrs),length(all.yrs)-1),
     xlab="Year",ylab="Total Number of Species",
     pch=16,type="b",col="#A40122",bty="l")

month.species.counts <- tapply(X = month.data$NumberCaught,
                               INDEX = month.data$Species,
                               FUN = sum)

month.species.counts <- data.frame(Sp=names(month.species.counts),
                                   N=month.species.counts)

treemap(dtf = month.species.counts,index = "Sp",vSize = "N")
```

## May

```{r,echo=FALSE,results=TRUE}
month.data <- moth.data[(moth.data$Month==5),]
trap.sums <- tapply(X = month.data$NumberCaught,
                    INDEX = month.data$TrapNumber,
                    FUN = sum)

trap.sums <- data.frame(AvgCatch=trap.sums,
                        Year=month.data$Year[
                          match(names(trap.sums),
                                month.data$TrapNumber)])

yr.avg.month.catch <- tapply(X = trap.sums$AvgCatch,
                             INDEX = trap.sums$Year,
                             FUN = mean,na.rm=TRUE)

yr.avg.month.catch[is.na(yr.avg.month.catch)] <- 0

par(mar=c(3.2,3.2,0.5,0.2))
par(mgp=c(2.0,0.2,0))
par(tck=-0.01)
par(las=1)

plot(names(yr.avg.month.catch),yr.avg.month.catch,
     xaxp=c(min(all.yrs),max(all.yrs),length(all.yrs)-1),
     xlab="Year",ylab="Average Catch",
     pch=16,type="b",col="#A40122",bty="l")

month.sp.counts.by.year <- tapply(X = month.data$Species,
                                  INDEX = month.data$Year,
                                  FUN = function(x) 
                                    return(length(unique(x[!is.na(x)]))))


par(mar=c(3.2,3.2,0.5,0.2))
par(mgp=c(2.0,0.2,0))
par(tck=-0.01)
par(las=1)

plot(names(month.sp.counts.by.year),month.sp.counts.by.year,
     xaxp=c(min(all.yrs),max(all.yrs),length(all.yrs)-1),
     xlab="Year",ylab="Total Number of Species",
     pch=16,type="b",col="#A40122",bty="l")

month.species.counts <- tapply(X = month.data$NumberCaught,
                               INDEX = month.data$Species,
                               FUN = sum)

month.species.counts <- data.frame(Sp=names(month.species.counts),
                                   N=month.species.counts)

treemap(dtf = month.species.counts,index = "Sp",vSize = "N")
```

## June

```{r,echo=FALSE,results=TRUE}
month.data <- moth.data[(moth.data$Month==6),]
trap.sums <- tapply(X = month.data$NumberCaught,
                    INDEX = month.data$TrapNumber,
                    FUN = sum)

trap.sums <- data.frame(AvgCatch=trap.sums,
                        Year=month.data$Year[
                          match(names(trap.sums),
                                month.data$TrapNumber)])

yr.avg.month.catch <- tapply(X = trap.sums$AvgCatch,
                             INDEX = trap.sums$Year,
                             FUN = mean,na.rm=TRUE)

yr.avg.month.catch[is.na(yr.avg.month.catch)] <- 0

par(mar=c(3.2,3.2,0.5,0.2))
par(mgp=c(2.0,0.2,0))
par(tck=-0.01)
par(las=1)

plot(names(yr.avg.month.catch),yr.avg.month.catch,
     xaxp=c(min(all.yrs),max(all.yrs),length(all.yrs)-1),
     xlab="Year",ylab="Average Catch",
     pch=16,type="b",col="#A40122",bty="l")

month.sp.counts.by.year <- tapply(X = month.data$Species,
                                  INDEX = month.data$Year,
                                  FUN = function(x) 
                                    return(length(unique(x[!is.na(x)]))))


par(mar=c(3.2,3.2,0.5,0.2))
par(mgp=c(2.0,0.2,0))
par(tck=-0.01)
par(las=1)

plot(names(month.sp.counts.by.year),month.sp.counts.by.year,
     xaxp=c(min(all.yrs),max(all.yrs),length(all.yrs)-1),
     xlab="Year",ylab="Total Number of Species",
     pch=16,type="b",col="#A40122",bty="l")

month.species.counts <- tapply(X = month.data$NumberCaught,
                               INDEX = month.data$Species,
                               FUN = sum)

month.species.counts <- data.frame(Sp=names(month.species.counts),
                                   N=month.species.counts)

treemap(dtf = month.species.counts,index = "Sp",vSize = "N")
```

## July

```{r,echo=FALSE,results=TRUE}
month.data <- moth.data[(moth.data$Month==7),]
trap.sums <- tapply(X = month.data$NumberCaught,
                    INDEX = month.data$TrapNumber,
                    FUN = sum)

trap.sums <- data.frame(AvgCatch=trap.sums,
                        Year=month.data$Year[
                          match(names(trap.sums),
                                month.data$TrapNumber)])

yr.avg.month.catch <- tapply(X = trap.sums$AvgCatch,
                             INDEX = trap.sums$Year,
                             FUN = mean,na.rm=TRUE)

yr.avg.month.catch[is.na(yr.avg.month.catch)] <- 0

par(mar=c(3.2,3.2,0.5,0.2))
par(mgp=c(2.0,0.2,0))
par(tck=-0.01)
par(las=1)

plot(names(yr.avg.month.catch),yr.avg.month.catch,
     xaxp=c(min(all.yrs),max(all.yrs),length(all.yrs)-1),
     xlab="Year",ylab="Average Catch",
     pch=16,type="b",col="#A40122",bty="l")

month.sp.counts.by.year <- tapply(X = month.data$Species,
                                  INDEX = month.data$Year,
                                  FUN = function(x) 
                                    return(length(unique(x[!is.na(x)]))))


par(mar=c(3.2,3.2,0.5,0.2))
par(mgp=c(2.0,0.2,0))
par(tck=-0.01)
par(las=1)

plot(names(month.sp.counts.by.year),month.sp.counts.by.year,
     xaxp=c(min(all.yrs),max(all.yrs),length(all.yrs)-1),
     xlab="Year",ylab="Total Number of Species",
     pch=16,type="b",col="#A40122",bty="l")

month.species.counts <- tapply(X = month.data$NumberCaught,
                               INDEX = month.data$Species,
                               FUN = sum)

month.species.counts <- data.frame(Sp=names(month.species.counts),
                                   N=month.species.counts)

treemap(dtf = month.species.counts,index = "Sp",vSize = "N")
```

## August

```{r,echo=FALSE,results=TRUE}
month.data <- moth.data[(moth.data$Month==8),]
trap.sums <- tapply(X = month.data$NumberCaught,
                    INDEX = month.data$TrapNumber,
                    FUN = sum)

trap.sums <- data.frame(AvgCatch=trap.sums,
                        Year=month.data$Year[
                          match(names(trap.sums),
                                month.data$TrapNumber)])

yr.avg.month.catch <- tapply(X = trap.sums$AvgCatch,
                             INDEX = trap.sums$Year,
                             FUN = mean,na.rm=TRUE)

yr.avg.month.catch[is.na(yr.avg.month.catch)] <- 0

par(mar=c(3.2,3.2,0.5,0.2))
par(mgp=c(2.0,0.2,0))
par(tck=-0.01)
par(las=1)

plot(names(yr.avg.month.catch),yr.avg.month.catch,
     xaxp=c(min(all.yrs),max(all.yrs),length(all.yrs)-1),
     xlab="Year",ylab="Average Catch",
     pch=16,type="b",col="#A40122",bty="l")

month.sp.counts.by.year <- tapply(X = month.data$Species,
                                  INDEX = month.data$Year,
                                  FUN = function(x) 
                                    return(length(unique(x[!is.na(x)]))))


par(mar=c(3.2,3.2,0.5,0.2))
par(mgp=c(2.0,0.2,0))
par(tck=-0.01)
par(las=1)

plot(names(month.sp.counts.by.year),month.sp.counts.by.year,
     xaxp=c(min(all.yrs),max(all.yrs),length(all.yrs)-1),
     xlab="Year",ylab="Total Number of Species",
     pch=16,type="b",col="#A40122",bty="l")

month.species.counts <- tapply(X = month.data$NumberCaught,
                               INDEX = month.data$Species,
                               FUN = sum)

month.species.counts <- data.frame(Sp=names(month.species.counts),
                                   N=month.species.counts)

treemap(dtf = month.species.counts,index = "Sp",vSize = "N")
```

## September

```{r,echo=FALSE,results=TRUE}
month.data <- moth.data[(moth.data$Month==9),]
trap.sums <- tapply(X = month.data$NumberCaught,
                    INDEX = month.data$TrapNumber,
                    FUN = sum)

trap.sums <- data.frame(AvgCatch=trap.sums,
                        Year=month.data$Year[
                          match(names(trap.sums),
                                month.data$TrapNumber)])

yr.avg.month.catch <- tapply(X = trap.sums$AvgCatch,
                             INDEX = trap.sums$Year,
                             FUN = mean,na.rm=TRUE)

yr.avg.month.catch[is.na(yr.avg.month.catch)] <- 0

par(mar=c(3.2,3.2,0.5,0.2))
par(mgp=c(2.0,0.2,0))
par(tck=-0.01)
par(las=1)

plot(names(yr.avg.month.catch),yr.avg.month.catch,
     xaxp=c(min(all.yrs),max(all.yrs),length(all.yrs)-1),
     xlab="Year",ylab="Average Catch",
     pch=16,type="b",col="#A40122",bty="l")

month.sp.counts.by.year <- tapply(X = month.data$Species,
                                  INDEX = month.data$Year,
                                  FUN = function(x) 
                                    return(length(unique(x[!is.na(x)]))))


par(mar=c(3.2,3.2,0.5,0.2))
par(mgp=c(2.0,0.2,0))
par(tck=-0.01)
par(las=1)

plot(names(month.sp.counts.by.year),month.sp.counts.by.year,
     xaxp=c(min(all.yrs),max(all.yrs),length(all.yrs)-1),
     xlab="Year",ylab="Total Number of Species",
     pch=16,type="b",col="#A40122",bty="l")

month.species.counts <- tapply(X = month.data$NumberCaught,
                               INDEX = month.data$Species,
                               FUN = sum)

month.species.counts <- data.frame(Sp=names(month.species.counts),
                                   N=month.species.counts)

treemap(dtf = month.species.counts,index = "Sp",vSize = "N")
```

## October

```{r,echo=FALSE,results=TRUE}
month.data <- moth.data[(moth.data$Month==10),]
trap.sums <- tapply(X = month.data$NumberCaught,
                    INDEX = month.data$TrapNumber,
                    FUN = sum)

trap.sums <- data.frame(AvgCatch=trap.sums,
                        Year=month.data$Year[
                          match(names(trap.sums),
                                month.data$TrapNumber)])

yr.avg.month.catch <- tapply(X = trap.sums$AvgCatch,
                             INDEX = trap.sums$Year,
                             FUN = mean,na.rm=TRUE)

yr.avg.month.catch[is.na(yr.avg.month.catch)] <- 0

par(mar=c(3.2,3.2,0.5,0.2))
par(mgp=c(2.0,0.2,0))
par(tck=-0.01)
par(las=1)

plot(names(yr.avg.month.catch),yr.avg.month.catch,
     xaxp=c(min(all.yrs),max(all.yrs),length(all.yrs)-1),
     xlab="Year",ylab="Average Catch",
     pch=16,type="b",col="#A40122",bty="l")

month.sp.counts.by.year <- tapply(X = month.data$Species,
                                  INDEX = month.data$Year,
                                  FUN = function(x) 
                                    return(length(unique(x[!is.na(x)]))))


par(mar=c(3.2,3.2,0.5,0.2))
par(mgp=c(2.0,0.2,0))
par(tck=-0.01)
par(las=1)

plot(names(month.sp.counts.by.year),month.sp.counts.by.year,
     xaxp=c(min(all.yrs),max(all.yrs),length(all.yrs)-1),
     xlab="Year",ylab="Total Number of Species",
     pch=16,type="b",col="#A40122",bty="l")

month.species.counts <- tapply(X = month.data$NumberCaught,
                               INDEX = month.data$Species,
                               FUN = sum)

month.species.counts <- data.frame(Sp=names(month.species.counts),
                                   N=month.species.counts)

treemap(dtf = month.species.counts,index = "Sp",vSize = "N")
```

## November

```{r,echo=FALSE,results=TRUE}
month.data <- moth.data[(moth.data$Month==11),]
trap.sums <- tapply(X = month.data$NumberCaught,
                    INDEX = month.data$TrapNumber,
                    FUN = sum)

trap.sums <- data.frame(AvgCatch=trap.sums,
                        Year=month.data$Year[
                          match(names(trap.sums),
                                month.data$TrapNumber)])

yr.avg.month.catch <- tapply(X = trap.sums$AvgCatch,
                             INDEX = trap.sums$Year,
                             FUN = mean,na.rm=TRUE)

yr.avg.month.catch[is.na(yr.avg.month.catch)] <- 0

par(mar=c(3.2,3.2,0.5,0.2))
par(mgp=c(2.0,0.2,0))
par(tck=-0.01)
par(las=1)

plot(names(yr.avg.month.catch),yr.avg.month.catch,
     xaxp=c(min(all.yrs),max(all.yrs),length(all.yrs)-1),
     xlab="Year",ylab="Average Catch",
     pch=16,type="b",col="#A40122",bty="l")

month.sp.counts.by.year <- tapply(X = month.data$Species,
                                  INDEX = month.data$Year,
                                  FUN = function(x) 
                                    return(length(unique(x[!is.na(x)]))))


par(mar=c(3.2,3.2,0.5,0.2))
par(mgp=c(2.0,0.2,0))
par(tck=-0.01)
par(las=1)

plot(names(month.sp.counts.by.year),month.sp.counts.by.year,
     xaxp=c(min(all.yrs),max(all.yrs),length(all.yrs)-1),
     xlab="Year",ylab="Total Number of Species",
     pch=16,type="b",col="#A40122",bty="l")

month.species.counts <- tapply(X = month.data$NumberCaught,
                               INDEX = month.data$Species,
                               FUN = sum)

month.species.counts <- data.frame(Sp=names(month.species.counts),
                                   N=month.species.counts)

treemap(dtf = month.species.counts,index = "Sp",vSize = "N")
```

## December

```{r,echo=FALSE,results=TRUE}
month.data <- moth.data[(moth.data$Month==12),]
trap.sums <- tapply(X = month.data$NumberCaught,
                    INDEX = month.data$TrapNumber,
                    FUN = sum)

trap.sums <- data.frame(AvgCatch=trap.sums,
                        Year=month.data$Year[
                          match(names(trap.sums),
                                month.data$TrapNumber)])

yr.avg.month.catch <- tapply(X = trap.sums$AvgCatch,
                             INDEX = trap.sums$Year,
                             FUN = mean,na.rm=TRUE)

yr.avg.month.catch[is.na(yr.avg.month.catch)] <- 0

par(mar=c(3.2,3.2,0.5,0.2))
par(mgp=c(2.0,0.2,0))
par(tck=-0.01)
par(las=1)

plot(names(yr.avg.month.catch),yr.avg.month.catch,
     xaxp=c(min(all.yrs),max(all.yrs),length(all.yrs)-1),
     xlab="Year",ylab="Average Catch",
     pch=16,type="b",col="#A40122",bty="l")

month.sp.counts.by.year <- tapply(X = month.data$Species,
                                  INDEX = month.data$Year,
                                  FUN = function(x) 
                                    return(length(unique(x[!is.na(x)]))))


par(mar=c(3.2,3.2,0.5,0.2))
par(mgp=c(2.0,0.2,0))
par(tck=-0.01)
par(las=1)

plot(names(month.sp.counts.by.year),month.sp.counts.by.year,
     xaxp=c(min(all.yrs),max(all.yrs),length(all.yrs)-1),
     xlab="Year",ylab="Total Number of Species",
     pch=16,type="b",col="#A40122",bty="l")

month.species.counts <- tapply(X = month.data$NumberCaught,
                               INDEX = month.data$Species,
                               FUN = sum)

month.species.counts <- data.frame(Sp=names(month.species.counts),
                                   N=month.species.counts)

treemap(dtf = month.species.counts,index = "Sp",vSize = "N")
```

# By Year

## 2020

```{r,echo=FALSE,results=TRUE}
yr.data <- moth.data[(moth.data$Year==2020),]
total.individs <- sum(yr.data$NumberCaught,na.rm=TRUE)

total.species <- length(unique(na.omit(yr.data)$Species))

total.traps <- length(unique(yr.data$TrapNumber))

```

I ran a total of `r total.traps` traps this year, catching `r total.individs` of `r total.species` species

```{r,echo=FALSE,results=TRUE}
traps <- unique(yr.data$TrapNumber)

sp.accum <- sapply(X = 1:length(traps),
                   FUN = function(i){
                     sub.data <- yr.data[(yr.data$TrapNumber<=traps[i]),]
                     
                     cum.sp <- length(unique(tolower(na.omit(sub.data$Species))))
                     
                   })

par(mar=c(3.2,3.2,0.5,0.2))
par(mgp=c(2.0,0.2,0))
par(tck=-0.01)
par(las=1)

plot(1:length(sp.accum),sp.accum,type="l",bty="l",col="#A40122",
     lwd=3,xlab="Trap number",ylab="Cumulative species count",
     xaxt="n")
axis(side = 1,at = 1:length(sp.accum))
```

```{r,echo=FALSE,results=TRUE}

species.sums <- tapply(X = yr.data$NumberCaught,
                       INDEX = yr.data$Species,
                       FUN = sum)

species.sums <- data.frame(Sp=names(species.sums),
                           N=species.sums)

treemap(dtf = species.sums,index = "Sp",vSize = "N")
```
    

## 2021

```{r,echo=FALSE,results=TRUE}
yr.data <- moth.data[(moth.data$Year==2021),]
total.individs <- sum(yr.data$NumberCaught,na.rm=TRUE)

total.species <- length(unique(na.omit(yr.data)$Species))

total.traps <- length(unique(yr.data$TrapNumber))

```

I ran a total of `r total.traps` traps this year, catching `r total.individs` of `r total.species` species

```{r,echo=FALSE,results=TRUE}
traps <- unique(yr.data$TrapNumber)

sp.accum <- sapply(X = 1:length(traps),
                   FUN = function(i){
                     sub.data <- yr.data[(yr.data$TrapNumber<=traps[i]),]
                     
                     cum.sp <- length(unique(tolower(na.omit(sub.data$Species))))
                     
                   })

par(mar=c(3.2,3.2,0.5,0.2))
par(mgp=c(2.0,0.2,0))
par(tck=-0.01)
par(las=1)

plot(1:length(sp.accum),sp.accum,type="l",bty="l",col="#A40122",
     lwd=3,xlab="Trap number",ylab="Cumulative species count",
     xaxt="n")
axis(side = 1,at = 1:length(sp.accum))
```

```{r,echo=FALSE,results=TRUE}

species.sums <- tapply(X = yr.data$NumberCaught,
                       INDEX = yr.data$Species,
                       FUN = sum)

species.sums <- data.frame(Sp=names(species.sums),
                           N=species.sums)

treemap(dtf = species.sums,index = "Sp",vSize = "N")
```
    

## 2022

```{r,echo=FALSE,results=TRUE}
yr.data <- moth.data[(moth.data$Year==2022),]
total.individs <- sum(yr.data$NumberCaught,na.rm=TRUE)

total.species <- length(unique(na.omit(yr.data)$Species))

total.traps <- length(unique(yr.data$TrapNumber))

```

I ran a total of `r total.traps` traps this year, catching `r total.individs` of `r total.species` species

```{r,echo=FALSE,results=TRUE}
traps <- unique(yr.data$TrapNumber)

sp.accum <- sapply(X = 1:length(traps),
                   FUN = function(i){
                     sub.data <- yr.data[(yr.data$TrapNumber<=traps[i]),]
                     
                     cum.sp <- length(unique(tolower(na.omit(sub.data$Species))))
                     
                   })

par(mar=c(3.2,3.2,0.5,0.2))
par(mgp=c(2.0,0.2,0))
par(tck=-0.01)
par(las=1)

plot(1:length(sp.accum),sp.accum,type="l",bty="l",col="#A40122",
     lwd=3,xlab="Trap number",ylab="Cumulative species count",
     xaxt="n")
axis(side = 1,at = 1:length(sp.accum))
```

```{r,echo=FALSE,results=TRUE}

species.sums <- tapply(X = yr.data$NumberCaught,
                       INDEX = yr.data$Species,
                       FUN = sum)

species.sums <- data.frame(Sp=names(species.sums),
                           N=species.sums)

treemap(dtf = species.sums,index = "Sp",vSize = "N")
```
    

## 2023

```{r,echo=FALSE,results=TRUE}
yr.data <- moth.data[(moth.data$Year==2023),]
total.individs <- sum(yr.data$NumberCaught,na.rm=TRUE)

total.species <- length(unique(na.omit(yr.data)$Species))

total.traps <- length(unique(yr.data$TrapNumber))

```

I ran a total of `r total.traps` traps this year, catching `r total.individs` of `r total.species` species

```{r,echo=FALSE,results=TRUE}
traps <- unique(yr.data$TrapNumber)

sp.accum <- sapply(X = 1:length(traps),
                   FUN = function(i){
                     sub.data <- yr.data[(yr.data$TrapNumber<=traps[i]),]
                     
                     cum.sp <- length(unique(tolower(na.omit(sub.data$Species))))
                     
                   })

par(mar=c(3.2,3.2,0.5,0.2))
par(mgp=c(2.0,0.2,0))
par(tck=-0.01)
par(las=1)

plot(1:length(sp.accum),sp.accum,type="l",bty="l",col="#A40122",
     lwd=3,xlab="Trap number",ylab="Cumulative species count",
     xaxt="n")
axis(side = 1,at = 1:length(sp.accum))
```

```{r,echo=FALSE,results=TRUE}

species.sums <- tapply(X = yr.data$NumberCaught,
                       INDEX = yr.data$Species,
                       FUN = sum)

species.sums <- data.frame(Sp=names(species.sums),
                           N=species.sums)

treemap(dtf = species.sums,index = "Sp",vSize = "N")
```
    

# By Species

## Angle shades

```{r,echo=FALSE,results=TRUE}
species.data <- moth.data[which(moth.data$Species=="Angle shades"),]
total.caught <- sum(species.data$NumberCaught,na.rm=TRUE)
n.traps <- length(unique(species.data$TrapNumber))
first.year <- min(species.data$Year,na.rm=TRUE)
last.year <- max(species.data$Year,na.rm=TRUE)

species.data$Year <- factor(x = species.data$Year,levels = all.yrs)
```
In total, I have caught `r total.caught` individuals of Angle shades, in `r n.traps` traps, between `r first.year` and `r last.year`.

```{r,echo=FALSE,results=TRUE}
yr.avg.catches <- tapply(X = species.data$NumberCaught,INDEX = species.data$Year,FUN = mean)
yr.avg.catches[is.na(yr.avg.catches)] <- 0

par(mar=c(3.2,3.2,0.5,0.2))
par(mgp=c(2.0,0.2,0))
par(tck=-0.01)
par(las=1)

plot(all.yrs,yr.avg.catches,xaxp=c(min(all.yrs),max(all.yrs),length(all.yrs)-1),xlab="Year",ylab="Average Catch",pch=16,type="b",col="#A40122",bty="l")

dens <- hist(species.data$Month,breaks=seq(from=0.5,to=12.5,by=1),plot=FALSE)
      
barplot(dens$density,space = 0,border=NA,col="#A40122",ylab="Density")
axis(side = 1,at = seq(from=0.5,to=11.5,length.out=12),labels = month.abb)
title(xlab="Month")
box(bty="l")
```

## Barred sallow

```{r,echo=FALSE,results=TRUE}
species.data <- moth.data[which(moth.data$Species=="Barred sallow"),]
total.caught <- sum(species.data$NumberCaught,na.rm=TRUE)
n.traps <- length(unique(species.data$TrapNumber))
first.year <- min(species.data$Year,na.rm=TRUE)
last.year <- max(species.data$Year,na.rm=TRUE)

species.data$Year <- factor(x = species.data$Year,levels = all.yrs)
```
In total, I have caught `r total.caught` individuals of Barred sallow, in `r n.traps` traps, between `r first.year` and `r last.year`.

```{r,echo=FALSE,results=TRUE}
yr.avg.catches <- tapply(X = species.data$NumberCaught,INDEX = species.data$Year,FUN = mean)
yr.avg.catches[is.na(yr.avg.catches)] <- 0

par(mar=c(3.2,3.2,0.5,0.2))
par(mgp=c(2.0,0.2,0))
par(tck=-0.01)
par(las=1)

plot(all.yrs,yr.avg.catches,xaxp=c(min(all.yrs),max(all.yrs),length(all.yrs)-1),xlab="Year",ylab="Average Catch",pch=16,type="b",col="#A40122",bty="l")

dens <- hist(species.data$Month,breaks=seq(from=0.5,to=12.5,by=1),plot=FALSE)
      
barplot(dens$density,space = 0,border=NA,col="#A40122",ylab="Density")
axis(side = 1,at = seq(from=0.5,to=11.5,length.out=12),labels = month.abb)
title(xlab="Month")
box(bty="l")
```

## Barred straw

```{r,echo=FALSE,results=TRUE}
species.data <- moth.data[which(moth.data$Species=="Barred straw"),]
total.caught <- sum(species.data$NumberCaught,na.rm=TRUE)
n.traps <- length(unique(species.data$TrapNumber))
first.year <- min(species.data$Year,na.rm=TRUE)
last.year <- max(species.data$Year,na.rm=TRUE)

species.data$Year <- factor(x = species.data$Year,levels = all.yrs)
```
In total, I have caught `r total.caught` individuals of Barred straw, in `r n.traps` traps, between `r first.year` and `r last.year`.

```{r,echo=FALSE,results=TRUE}
yr.avg.catches <- tapply(X = species.data$NumberCaught,INDEX = species.data$Year,FUN = mean)
yr.avg.catches[is.na(yr.avg.catches)] <- 0

par(mar=c(3.2,3.2,0.5,0.2))
par(mgp=c(2.0,0.2,0))
par(tck=-0.01)
par(las=1)

plot(all.yrs,yr.avg.catches,xaxp=c(min(all.yrs),max(all.yrs),length(all.yrs)-1),xlab="Year",ylab="Average Catch",pch=16,type="b",col="#A40122",bty="l")

dens <- hist(species.data$Month,breaks=seq(from=0.5,to=12.5,by=1),plot=FALSE)
      
barplot(dens$density,space = 0,border=NA,col="#A40122",ylab="Density")
axis(side = 1,at = seq(from=0.5,to=11.5,length.out=12),labels = month.abb)
title(xlab="Month")
box(bty="l")
```

## Beaded chestnut

```{r,echo=FALSE,results=TRUE}
species.data <- moth.data[which(moth.data$Species=="Beaded chestnut"),]
total.caught <- sum(species.data$NumberCaught,na.rm=TRUE)
n.traps <- length(unique(species.data$TrapNumber))
first.year <- min(species.data$Year,na.rm=TRUE)
last.year <- max(species.data$Year,na.rm=TRUE)

species.data$Year <- factor(x = species.data$Year,levels = all.yrs)
```
In total, I have caught `r total.caught` individuals of Beaded chestnut, in `r n.traps` traps, between `r first.year` and `r last.year`.

```{r,echo=FALSE,results=TRUE}
yr.avg.catches <- tapply(X = species.data$NumberCaught,INDEX = species.data$Year,FUN = mean)
yr.avg.catches[is.na(yr.avg.catches)] <- 0

par(mar=c(3.2,3.2,0.5,0.2))
par(mgp=c(2.0,0.2,0))
par(tck=-0.01)
par(las=1)

plot(all.yrs,yr.avg.catches,xaxp=c(min(all.yrs),max(all.yrs),length(all.yrs)-1),xlab="Year",ylab="Average Catch",pch=16,type="b",col="#A40122",bty="l")

dens <- hist(species.data$Month,breaks=seq(from=0.5,to=12.5,by=1),plot=FALSE)
      
barplot(dens$density,space = 0,border=NA,col="#A40122",ylab="Density")
axis(side = 1,at = seq(from=0.5,to=11.5,length.out=12),labels = month.abb)
title(xlab="Month")
box(bty="l")
```

## Beautiful hook-tip

```{r,echo=FALSE,results=TRUE}
species.data <- moth.data[which(moth.data$Species=="Beautiful hook-tip"),]
total.caught <- sum(species.data$NumberCaught,na.rm=TRUE)
n.traps <- length(unique(species.data$TrapNumber))
first.year <- min(species.data$Year,na.rm=TRUE)
last.year <- max(species.data$Year,na.rm=TRUE)

species.data$Year <- factor(x = species.data$Year,levels = all.yrs)
```
In total, I have caught `r total.caught` individuals of Beautiful hook-tip, in `r n.traps` traps, between `r first.year` and `r last.year`.

```{r,echo=FALSE,results=TRUE}
yr.avg.catches <- tapply(X = species.data$NumberCaught,INDEX = species.data$Year,FUN = mean)
yr.avg.catches[is.na(yr.avg.catches)] <- 0

par(mar=c(3.2,3.2,0.5,0.2))
par(mgp=c(2.0,0.2,0))
par(tck=-0.01)
par(las=1)

plot(all.yrs,yr.avg.catches,xaxp=c(min(all.yrs),max(all.yrs),length(all.yrs)-1),xlab="Year",ylab="Average Catch",pch=16,type="b",col="#A40122",bty="l")

dens <- hist(species.data$Month,breaks=seq(from=0.5,to=12.5,by=1),plot=FALSE)
      
barplot(dens$density,space = 0,border=NA,col="#A40122",ylab="Density")
axis(side = 1,at = seq(from=0.5,to=11.5,length.out=12),labels = month.abb)
title(xlab="Month")
box(bty="l")
```

## Black rustic

```{r,echo=FALSE,results=TRUE}
species.data <- moth.data[which(moth.data$Species=="Black rustic"),]
total.caught <- sum(species.data$NumberCaught,na.rm=TRUE)
n.traps <- length(unique(species.data$TrapNumber))
first.year <- min(species.data$Year,na.rm=TRUE)
last.year <- max(species.data$Year,na.rm=TRUE)

species.data$Year <- factor(x = species.data$Year,levels = all.yrs)
```
In total, I have caught `r total.caught` individuals of Black rustic, in `r n.traps` traps, between `r first.year` and `r last.year`.

```{r,echo=FALSE,results=TRUE}
yr.avg.catches <- tapply(X = species.data$NumberCaught,INDEX = species.data$Year,FUN = mean)
yr.avg.catches[is.na(yr.avg.catches)] <- 0

par(mar=c(3.2,3.2,0.5,0.2))
par(mgp=c(2.0,0.2,0))
par(tck=-0.01)
par(las=1)

plot(all.yrs,yr.avg.catches,xaxp=c(min(all.yrs),max(all.yrs),length(all.yrs)-1),xlab="Year",ylab="Average Catch",pch=16,type="b",col="#A40122",bty="l")

dens <- hist(species.data$Month,breaks=seq(from=0.5,to=12.5,by=1),plot=FALSE)
      
barplot(dens$density,space = 0,border=NA,col="#A40122",ylab="Density")
axis(side = 1,at = seq(from=0.5,to=11.5,length.out=12),labels = month.abb)
title(xlab="Month")
box(bty="l")
```

## Blair's shoulder-knot

```{r,echo=FALSE,results=TRUE}
species.data <- moth.data[which(moth.data$Species=="Blair's shoulder-knot"),]
total.caught <- sum(species.data$NumberCaught,na.rm=TRUE)
n.traps <- length(unique(species.data$TrapNumber))
first.year <- min(species.data$Year,na.rm=TRUE)
last.year <- max(species.data$Year,na.rm=TRUE)

species.data$Year <- factor(x = species.data$Year,levels = all.yrs)
```
In total, I have caught `r total.caught` individuals of Blair's shoulder-knot, in `r n.traps` traps, between `r first.year` and `r last.year`.

```{r,echo=FALSE,results=TRUE}
yr.avg.catches <- tapply(X = species.data$NumberCaught,INDEX = species.data$Year,FUN = mean)
yr.avg.catches[is.na(yr.avg.catches)] <- 0

par(mar=c(3.2,3.2,0.5,0.2))
par(mgp=c(2.0,0.2,0))
par(tck=-0.01)
par(las=1)

plot(all.yrs,yr.avg.catches,xaxp=c(min(all.yrs),max(all.yrs),length(all.yrs)-1),xlab="Year",ylab="Average Catch",pch=16,type="b",col="#A40122",bty="l")

dens <- hist(species.data$Month,breaks=seq(from=0.5,to=12.5,by=1),plot=FALSE)
      
barplot(dens$density,space = 0,border=NA,col="#A40122",ylab="Density")
axis(side = 1,at = seq(from=0.5,to=11.5,length.out=12),labels = month.abb)
title(xlab="Month")
box(bty="l")
```

## Blood-vein

```{r,echo=FALSE,results=TRUE}
species.data <- moth.data[which(moth.data$Species=="Blood-vein"),]
total.caught <- sum(species.data$NumberCaught,na.rm=TRUE)
n.traps <- length(unique(species.data$TrapNumber))
first.year <- min(species.data$Year,na.rm=TRUE)
last.year <- max(species.data$Year,na.rm=TRUE)

species.data$Year <- factor(x = species.data$Year,levels = all.yrs)
```
In total, I have caught `r total.caught` individuals of Blood-vein, in `r n.traps` traps, between `r first.year` and `r last.year`.

```{r,echo=FALSE,results=TRUE}
yr.avg.catches <- tapply(X = species.data$NumberCaught,INDEX = species.data$Year,FUN = mean)
yr.avg.catches[is.na(yr.avg.catches)] <- 0

par(mar=c(3.2,3.2,0.5,0.2))
par(mgp=c(2.0,0.2,0))
par(tck=-0.01)
par(las=1)

plot(all.yrs,yr.avg.catches,xaxp=c(min(all.yrs),max(all.yrs),length(all.yrs)-1),xlab="Year",ylab="Average Catch",pch=16,type="b",col="#A40122",bty="l")

dens <- hist(species.data$Month,breaks=seq(from=0.5,to=12.5,by=1),plot=FALSE)
      
barplot(dens$density,space = 0,border=NA,col="#A40122",ylab="Density")
axis(side = 1,at = seq(from=0.5,to=11.5,length.out=12),labels = month.abb)
title(xlab="Month")
box(bty="l")
```

## Blossom underwing

```{r,echo=FALSE,results=TRUE}
species.data <- moth.data[which(moth.data$Species=="Blossom underwing"),]
total.caught <- sum(species.data$NumberCaught,na.rm=TRUE)
n.traps <- length(unique(species.data$TrapNumber))
first.year <- min(species.data$Year,na.rm=TRUE)
last.year <- max(species.data$Year,na.rm=TRUE)

species.data$Year <- factor(x = species.data$Year,levels = all.yrs)
```
In total, I have caught `r total.caught` individuals of Blossom underwing, in `r n.traps` traps, between `r first.year` and `r last.year`.

```{r,echo=FALSE,results=TRUE}
yr.avg.catches <- tapply(X = species.data$NumberCaught,INDEX = species.data$Year,FUN = mean)
yr.avg.catches[is.na(yr.avg.catches)] <- 0

par(mar=c(3.2,3.2,0.5,0.2))
par(mgp=c(2.0,0.2,0))
par(tck=-0.01)
par(las=1)

plot(all.yrs,yr.avg.catches,xaxp=c(min(all.yrs),max(all.yrs),length(all.yrs)-1),xlab="Year",ylab="Average Catch",pch=16,type="b",col="#A40122",bty="l")

dens <- hist(species.data$Month,breaks=seq(from=0.5,to=12.5,by=1),plot=FALSE)
      
barplot(dens$density,space = 0,border=NA,col="#A40122",ylab="Density")
axis(side = 1,at = seq(from=0.5,to=11.5,length.out=12),labels = month.abb)
title(xlab="Month")
box(bty="l")
```

## Blue-bordered carpet

```{r,echo=FALSE,results=TRUE}
species.data <- moth.data[which(moth.data$Species=="Blue-bordered carpet"),]
total.caught <- sum(species.data$NumberCaught,na.rm=TRUE)
n.traps <- length(unique(species.data$TrapNumber))
first.year <- min(species.data$Year,na.rm=TRUE)
last.year <- max(species.data$Year,na.rm=TRUE)

species.data$Year <- factor(x = species.data$Year,levels = all.yrs)
```
In total, I have caught `r total.caught` individuals of Blue-bordered carpet, in `r n.traps` traps, between `r first.year` and `r last.year`.

```{r,echo=FALSE,results=TRUE}
yr.avg.catches <- tapply(X = species.data$NumberCaught,INDEX = species.data$Year,FUN = mean)
yr.avg.catches[is.na(yr.avg.catches)] <- 0

par(mar=c(3.2,3.2,0.5,0.2))
par(mgp=c(2.0,0.2,0))
par(tck=-0.01)
par(las=1)

plot(all.yrs,yr.avg.catches,xaxp=c(min(all.yrs),max(all.yrs),length(all.yrs)-1),xlab="Year",ylab="Average Catch",pch=16,type="b",col="#A40122",bty="l")

dens <- hist(species.data$Month,breaks=seq(from=0.5,to=12.5,by=1),plot=FALSE)
      
barplot(dens$density,space = 0,border=NA,col="#A40122",ylab="Density")
axis(side = 1,at = seq(from=0.5,to=11.5,length.out=12),labels = month.abb)
title(xlab="Month")
box(bty="l")
```

## Brick

```{r,echo=FALSE,results=TRUE}
species.data <- moth.data[which(moth.data$Species=="Brick"),]
total.caught <- sum(species.data$NumberCaught,na.rm=TRUE)
n.traps <- length(unique(species.data$TrapNumber))
first.year <- min(species.data$Year,na.rm=TRUE)
last.year <- max(species.data$Year,na.rm=TRUE)

species.data$Year <- factor(x = species.data$Year,levels = all.yrs)
```
In total, I have caught `r total.caught` individuals of Brick, in `r n.traps` traps, between `r first.year` and `r last.year`.

```{r,echo=FALSE,results=TRUE}
yr.avg.catches <- tapply(X = species.data$NumberCaught,INDEX = species.data$Year,FUN = mean)
yr.avg.catches[is.na(yr.avg.catches)] <- 0

par(mar=c(3.2,3.2,0.5,0.2))
par(mgp=c(2.0,0.2,0))
par(tck=-0.01)
par(las=1)

plot(all.yrs,yr.avg.catches,xaxp=c(min(all.yrs),max(all.yrs),length(all.yrs)-1),xlab="Year",ylab="Average Catch",pch=16,type="b",col="#A40122",bty="l")

dens <- hist(species.data$Month,breaks=seq(from=0.5,to=12.5,by=1),plot=FALSE)
      
barplot(dens$density,space = 0,border=NA,col="#A40122",ylab="Density")
axis(side = 1,at = seq(from=0.5,to=11.5,length.out=12),labels = month.abb)
title(xlab="Month")
box(bty="l")
```

## Bright-line brown-eye

```{r,echo=FALSE,results=TRUE}
species.data <- moth.data[which(moth.data$Species=="Bright-line brown-eye"),]
total.caught <- sum(species.data$NumberCaught,na.rm=TRUE)
n.traps <- length(unique(species.data$TrapNumber))
first.year <- min(species.data$Year,na.rm=TRUE)
last.year <- max(species.data$Year,na.rm=TRUE)

species.data$Year <- factor(x = species.data$Year,levels = all.yrs)
```
In total, I have caught `r total.caught` individuals of Bright-line brown-eye, in `r n.traps` traps, between `r first.year` and `r last.year`.

```{r,echo=FALSE,results=TRUE}
yr.avg.catches <- tapply(X = species.data$NumberCaught,INDEX = species.data$Year,FUN = mean)
yr.avg.catches[is.na(yr.avg.catches)] <- 0

par(mar=c(3.2,3.2,0.5,0.2))
par(mgp=c(2.0,0.2,0))
par(tck=-0.01)
par(las=1)

plot(all.yrs,yr.avg.catches,xaxp=c(min(all.yrs),max(all.yrs),length(all.yrs)-1),xlab="Year",ylab="Average Catch",pch=16,type="b",col="#A40122",bty="l")

dens <- hist(species.data$Month,breaks=seq(from=0.5,to=12.5,by=1),plot=FALSE)
      
barplot(dens$density,space = 0,border=NA,col="#A40122",ylab="Density")
axis(side = 1,at = seq(from=0.5,to=11.5,length.out=12),labels = month.abb)
title(xlab="Month")
box(bty="l")
```

## Brimstone

```{r,echo=FALSE,results=TRUE}
species.data <- moth.data[which(moth.data$Species=="Brimstone"),]
total.caught <- sum(species.data$NumberCaught,na.rm=TRUE)
n.traps <- length(unique(species.data$TrapNumber))
first.year <- min(species.data$Year,na.rm=TRUE)
last.year <- max(species.data$Year,na.rm=TRUE)

species.data$Year <- factor(x = species.data$Year,levels = all.yrs)
```
In total, I have caught `r total.caught` individuals of Brimstone, in `r n.traps` traps, between `r first.year` and `r last.year`.

```{r,echo=FALSE,results=TRUE}
yr.avg.catches <- tapply(X = species.data$NumberCaught,INDEX = species.data$Year,FUN = mean)
yr.avg.catches[is.na(yr.avg.catches)] <- 0

par(mar=c(3.2,3.2,0.5,0.2))
par(mgp=c(2.0,0.2,0))
par(tck=-0.01)
par(las=1)

plot(all.yrs,yr.avg.catches,xaxp=c(min(all.yrs),max(all.yrs),length(all.yrs)-1),xlab="Year",ylab="Average Catch",pch=16,type="b",col="#A40122",bty="l")

dens <- hist(species.data$Month,breaks=seq(from=0.5,to=12.5,by=1),plot=FALSE)
      
barplot(dens$density,space = 0,border=NA,col="#A40122",ylab="Density")
axis(side = 1,at = seq(from=0.5,to=11.5,length.out=12),labels = month.abb)
title(xlab="Month")
box(bty="l")
```

## Brindled pug

```{r,echo=FALSE,results=TRUE}
species.data <- moth.data[which(moth.data$Species=="Brindled pug"),]
total.caught <- sum(species.data$NumberCaught,na.rm=TRUE)
n.traps <- length(unique(species.data$TrapNumber))
first.year <- min(species.data$Year,na.rm=TRUE)
last.year <- max(species.data$Year,na.rm=TRUE)

species.data$Year <- factor(x = species.data$Year,levels = all.yrs)
```
In total, I have caught `r total.caught` individuals of Brindled pug, in `r n.traps` traps, between `r first.year` and `r last.year`.

```{r,echo=FALSE,results=TRUE}
yr.avg.catches <- tapply(X = species.data$NumberCaught,INDEX = species.data$Year,FUN = mean)
yr.avg.catches[is.na(yr.avg.catches)] <- 0

par(mar=c(3.2,3.2,0.5,0.2))
par(mgp=c(2.0,0.2,0))
par(tck=-0.01)
par(las=1)

plot(all.yrs,yr.avg.catches,xaxp=c(min(all.yrs),max(all.yrs),length(all.yrs)-1),xlab="Year",ylab="Average Catch",pch=16,type="b",col="#A40122",bty="l")

dens <- hist(species.data$Month,breaks=seq(from=0.5,to=12.5,by=1),plot=FALSE)
      
barplot(dens$density,space = 0,border=NA,col="#A40122",ylab="Density")
axis(side = 1,at = seq(from=0.5,to=11.5,length.out=12),labels = month.abb)
title(xlab="Month")
box(bty="l")
```

## Broad-bordered yellow underwing

```{r,echo=FALSE,results=TRUE}
species.data <- moth.data[which(moth.data$Species=="Broad-bordered yellow underwing"),]
total.caught <- sum(species.data$NumberCaught,na.rm=TRUE)
n.traps <- length(unique(species.data$TrapNumber))
first.year <- min(species.data$Year,na.rm=TRUE)
last.year <- max(species.data$Year,na.rm=TRUE)

species.data$Year <- factor(x = species.data$Year,levels = all.yrs)
```
In total, I have caught `r total.caught` individuals of Broad-bordered yellow underwing, in `r n.traps` traps, between `r first.year` and `r last.year`.

```{r,echo=FALSE,results=TRUE}
yr.avg.catches <- tapply(X = species.data$NumberCaught,INDEX = species.data$Year,FUN = mean)
yr.avg.catches[is.na(yr.avg.catches)] <- 0

par(mar=c(3.2,3.2,0.5,0.2))
par(mgp=c(2.0,0.2,0))
par(tck=-0.01)
par(las=1)

plot(all.yrs,yr.avg.catches,xaxp=c(min(all.yrs),max(all.yrs),length(all.yrs)-1),xlab="Year",ylab="Average Catch",pch=16,type="b",col="#A40122",bty="l")

dens <- hist(species.data$Month,breaks=seq(from=0.5,to=12.5,by=1),plot=FALSE)
      
barplot(dens$density,space = 0,border=NA,col="#A40122",ylab="Density")
axis(side = 1,at = seq(from=0.5,to=11.5,length.out=12),labels = month.abb)
title(xlab="Month")
box(bty="l")
```

## Brown-line bright-eye

```{r,echo=FALSE,results=TRUE}
species.data <- moth.data[which(moth.data$Species=="Brown-line bright-eye"),]
total.caught <- sum(species.data$NumberCaught,na.rm=TRUE)
n.traps <- length(unique(species.data$TrapNumber))
first.year <- min(species.data$Year,na.rm=TRUE)
last.year <- max(species.data$Year,na.rm=TRUE)

species.data$Year <- factor(x = species.data$Year,levels = all.yrs)
```
In total, I have caught `r total.caught` individuals of Brown-line bright-eye, in `r n.traps` traps, between `r first.year` and `r last.year`.

```{r,echo=FALSE,results=TRUE}
yr.avg.catches <- tapply(X = species.data$NumberCaught,INDEX = species.data$Year,FUN = mean)
yr.avg.catches[is.na(yr.avg.catches)] <- 0

par(mar=c(3.2,3.2,0.5,0.2))
par(mgp=c(2.0,0.2,0))
par(tck=-0.01)
par(las=1)

plot(all.yrs,yr.avg.catches,xaxp=c(min(all.yrs),max(all.yrs),length(all.yrs)-1),xlab="Year",ylab="Average Catch",pch=16,type="b",col="#A40122",bty="l")

dens <- hist(species.data$Month,breaks=seq(from=0.5,to=12.5,by=1),plot=FALSE)
      
barplot(dens$density,space = 0,border=NA,col="#A40122",ylab="Density")
axis(side = 1,at = seq(from=0.5,to=11.5,length.out=12),labels = month.abb)
title(xlab="Month")
box(bty="l")
```

## Brown-spot pinion

```{r,echo=FALSE,results=TRUE}
species.data <- moth.data[which(moth.data$Species=="Brown-spot pinion"),]
total.caught <- sum(species.data$NumberCaught,na.rm=TRUE)
n.traps <- length(unique(species.data$TrapNumber))
first.year <- min(species.data$Year,na.rm=TRUE)
last.year <- max(species.data$Year,na.rm=TRUE)

species.data$Year <- factor(x = species.data$Year,levels = all.yrs)
```
In total, I have caught `r total.caught` individuals of Brown-spot pinion, in `r n.traps` traps, between `r first.year` and `r last.year`.

```{r,echo=FALSE,results=TRUE}
yr.avg.catches <- tapply(X = species.data$NumberCaught,INDEX = species.data$Year,FUN = mean)
yr.avg.catches[is.na(yr.avg.catches)] <- 0

par(mar=c(3.2,3.2,0.5,0.2))
par(mgp=c(2.0,0.2,0))
par(tck=-0.01)
par(las=1)

plot(all.yrs,yr.avg.catches,xaxp=c(min(all.yrs),max(all.yrs),length(all.yrs)-1),xlab="Year",ylab="Average Catch",pch=16,type="b",col="#A40122",bty="l")

dens <- hist(species.data$Month,breaks=seq(from=0.5,to=12.5,by=1),plot=FALSE)
      
barplot(dens$density,space = 0,border=NA,col="#A40122",ylab="Density")
axis(side = 1,at = seq(from=0.5,to=11.5,length.out=12),labels = month.abb)
title(xlab="Month")
box(bty="l")
```

## Brown-tail

```{r,echo=FALSE,results=TRUE}
species.data <- moth.data[which(moth.data$Species=="Brown-tail"),]
total.caught <- sum(species.data$NumberCaught,na.rm=TRUE)
n.traps <- length(unique(species.data$TrapNumber))
first.year <- min(species.data$Year,na.rm=TRUE)
last.year <- max(species.data$Year,na.rm=TRUE)

species.data$Year <- factor(x = species.data$Year,levels = all.yrs)
```
In total, I have caught `r total.caught` individuals of Brown-tail, in `r n.traps` traps, between `r first.year` and `r last.year`.

```{r,echo=FALSE,results=TRUE}
yr.avg.catches <- tapply(X = species.data$NumberCaught,INDEX = species.data$Year,FUN = mean)
yr.avg.catches[is.na(yr.avg.catches)] <- 0

par(mar=c(3.2,3.2,0.5,0.2))
par(mgp=c(2.0,0.2,0))
par(tck=-0.01)
par(las=1)

plot(all.yrs,yr.avg.catches,xaxp=c(min(all.yrs),max(all.yrs),length(all.yrs)-1),xlab="Year",ylab="Average Catch",pch=16,type="b",col="#A40122",bty="l")

dens <- hist(species.data$Month,breaks=seq(from=0.5,to=12.5,by=1),plot=FALSE)
      
barplot(dens$density,space = 0,border=NA,col="#A40122",ylab="Density")
axis(side = 1,at = seq(from=0.5,to=11.5,length.out=12),labels = month.abb)
title(xlab="Month")
box(bty="l")
```

## Brown rustic

```{r,echo=FALSE,results=TRUE}
species.data <- moth.data[which(moth.data$Species=="Brown rustic"),]
total.caught <- sum(species.data$NumberCaught,na.rm=TRUE)
n.traps <- length(unique(species.data$TrapNumber))
first.year <- min(species.data$Year,na.rm=TRUE)
last.year <- max(species.data$Year,na.rm=TRUE)

species.data$Year <- factor(x = species.data$Year,levels = all.yrs)
```
In total, I have caught `r total.caught` individuals of Brown rustic, in `r n.traps` traps, between `r first.year` and `r last.year`.

```{r,echo=FALSE,results=TRUE}
yr.avg.catches <- tapply(X = species.data$NumberCaught,INDEX = species.data$Year,FUN = mean)
yr.avg.catches[is.na(yr.avg.catches)] <- 0

par(mar=c(3.2,3.2,0.5,0.2))
par(mgp=c(2.0,0.2,0))
par(tck=-0.01)
par(las=1)

plot(all.yrs,yr.avg.catches,xaxp=c(min(all.yrs),max(all.yrs),length(all.yrs)-1),xlab="Year",ylab="Average Catch",pch=16,type="b",col="#A40122",bty="l")

dens <- hist(species.data$Month,breaks=seq(from=0.5,to=12.5,by=1),plot=FALSE)
      
barplot(dens$density,space = 0,border=NA,col="#A40122",ylab="Density")
axis(side = 1,at = seq(from=0.5,to=11.5,length.out=12),labels = month.abb)
title(xlab="Month")
box(bty="l")
```

## Buff-tip

```{r,echo=FALSE,results=TRUE}
species.data <- moth.data[which(moth.data$Species=="Buff-tip"),]
total.caught <- sum(species.data$NumberCaught,na.rm=TRUE)
n.traps <- length(unique(species.data$TrapNumber))
first.year <- min(species.data$Year,na.rm=TRUE)
last.year <- max(species.data$Year,na.rm=TRUE)

species.data$Year <- factor(x = species.data$Year,levels = all.yrs)
```
In total, I have caught `r total.caught` individuals of Buff-tip, in `r n.traps` traps, between `r first.year` and `r last.year`.

```{r,echo=FALSE,results=TRUE}
yr.avg.catches <- tapply(X = species.data$NumberCaught,INDEX = species.data$Year,FUN = mean)
yr.avg.catches[is.na(yr.avg.catches)] <- 0

par(mar=c(3.2,3.2,0.5,0.2))
par(mgp=c(2.0,0.2,0))
par(tck=-0.01)
par(las=1)

plot(all.yrs,yr.avg.catches,xaxp=c(min(all.yrs),max(all.yrs),length(all.yrs)-1),xlab="Year",ylab="Average Catch",pch=16,type="b",col="#A40122",bty="l")

dens <- hist(species.data$Month,breaks=seq(from=0.5,to=12.5,by=1),plot=FALSE)
      
barplot(dens$density,space = 0,border=NA,col="#A40122",ylab="Density")
axis(side = 1,at = seq(from=0.5,to=11.5,length.out=12),labels = month.abb)
title(xlab="Month")
box(bty="l")
```

## Buff ermine

```{r,echo=FALSE,results=TRUE}
species.data <- moth.data[which(moth.data$Species=="Buff ermine"),]
total.caught <- sum(species.data$NumberCaught,na.rm=TRUE)
n.traps <- length(unique(species.data$TrapNumber))
first.year <- min(species.data$Year,na.rm=TRUE)
last.year <- max(species.data$Year,na.rm=TRUE)

species.data$Year <- factor(x = species.data$Year,levels = all.yrs)
```
In total, I have caught `r total.caught` individuals of Buff ermine, in `r n.traps` traps, between `r first.year` and `r last.year`.

```{r,echo=FALSE,results=TRUE}
yr.avg.catches <- tapply(X = species.data$NumberCaught,INDEX = species.data$Year,FUN = mean)
yr.avg.catches[is.na(yr.avg.catches)] <- 0

par(mar=c(3.2,3.2,0.5,0.2))
par(mgp=c(2.0,0.2,0))
par(tck=-0.01)
par(las=1)

plot(all.yrs,yr.avg.catches,xaxp=c(min(all.yrs),max(all.yrs),length(all.yrs)-1),xlab="Year",ylab="Average Catch",pch=16,type="b",col="#A40122",bty="l")

dens <- hist(species.data$Month,breaks=seq(from=0.5,to=12.5,by=1),plot=FALSE)
      
barplot(dens$density,space = 0,border=NA,col="#A40122",ylab="Density")
axis(side = 1,at = seq(from=0.5,to=11.5,length.out=12),labels = month.abb)
title(xlab="Month")
box(bty="l")
```

## Buff footman

```{r,echo=FALSE,results=TRUE}
species.data <- moth.data[which(moth.data$Species=="Buff footman"),]
total.caught <- sum(species.data$NumberCaught,na.rm=TRUE)
n.traps <- length(unique(species.data$TrapNumber))
first.year <- min(species.data$Year,na.rm=TRUE)
last.year <- max(species.data$Year,na.rm=TRUE)

species.data$Year <- factor(x = species.data$Year,levels = all.yrs)
```
In total, I have caught `r total.caught` individuals of Buff footman, in `r n.traps` traps, between `r first.year` and `r last.year`.

```{r,echo=FALSE,results=TRUE}
yr.avg.catches <- tapply(X = species.data$NumberCaught,INDEX = species.data$Year,FUN = mean)
yr.avg.catches[is.na(yr.avg.catches)] <- 0

par(mar=c(3.2,3.2,0.5,0.2))
par(mgp=c(2.0,0.2,0))
par(tck=-0.01)
par(las=1)

plot(all.yrs,yr.avg.catches,xaxp=c(min(all.yrs),max(all.yrs),length(all.yrs)-1),xlab="Year",ylab="Average Catch",pch=16,type="b",col="#A40122",bty="l")

dens <- hist(species.data$Month,breaks=seq(from=0.5,to=12.5,by=1),plot=FALSE)
      
barplot(dens$density,space = 0,border=NA,col="#A40122",ylab="Density")
axis(side = 1,at = seq(from=0.5,to=11.5,length.out=12),labels = month.abb)
title(xlab="Month")
box(bty="l")
```

## Bulrush wainscot

```{r,echo=FALSE,results=TRUE}
species.data <- moth.data[which(moth.data$Species=="Bulrush wainscot"),]
total.caught <- sum(species.data$NumberCaught,na.rm=TRUE)
n.traps <- length(unique(species.data$TrapNumber))
first.year <- min(species.data$Year,na.rm=TRUE)
last.year <- max(species.data$Year,na.rm=TRUE)

species.data$Year <- factor(x = species.data$Year,levels = all.yrs)
```
In total, I have caught `r total.caught` individuals of Bulrush wainscot, in `r n.traps` traps, between `r first.year` and `r last.year`.

```{r,echo=FALSE,results=TRUE}
yr.avg.catches <- tapply(X = species.data$NumberCaught,INDEX = species.data$Year,FUN = mean)
yr.avg.catches[is.na(yr.avg.catches)] <- 0

par(mar=c(3.2,3.2,0.5,0.2))
par(mgp=c(2.0,0.2,0))
par(tck=-0.01)
par(las=1)

plot(all.yrs,yr.avg.catches,xaxp=c(min(all.yrs),max(all.yrs),length(all.yrs)-1),xlab="Year",ylab="Average Catch",pch=16,type="b",col="#A40122",bty="l")

dens <- hist(species.data$Month,breaks=seq(from=0.5,to=12.5,by=1),plot=FALSE)
      
barplot(dens$density,space = 0,border=NA,col="#A40122",ylab="Density")
axis(side = 1,at = seq(from=0.5,to=11.5,length.out=12),labels = month.abb)
title(xlab="Month")
box(bty="l")
```

## Burnished brass

```{r,echo=FALSE,results=TRUE}
species.data <- moth.data[which(moth.data$Species=="Burnished brass"),]
total.caught <- sum(species.data$NumberCaught,na.rm=TRUE)
n.traps <- length(unique(species.data$TrapNumber))
first.year <- min(species.data$Year,na.rm=TRUE)
last.year <- max(species.data$Year,na.rm=TRUE)

species.data$Year <- factor(x = species.data$Year,levels = all.yrs)
```
In total, I have caught `r total.caught` individuals of Burnished brass, in `r n.traps` traps, between `r first.year` and `r last.year`.

```{r,echo=FALSE,results=TRUE}
yr.avg.catches <- tapply(X = species.data$NumberCaught,INDEX = species.data$Year,FUN = mean)
yr.avg.catches[is.na(yr.avg.catches)] <- 0

par(mar=c(3.2,3.2,0.5,0.2))
par(mgp=c(2.0,0.2,0))
par(tck=-0.01)
par(las=1)

plot(all.yrs,yr.avg.catches,xaxp=c(min(all.yrs),max(all.yrs),length(all.yrs)-1),xlab="Year",ylab="Average Catch",pch=16,type="b",col="#A40122",bty="l")

dens <- hist(species.data$Month,breaks=seq(from=0.5,to=12.5,by=1),plot=FALSE)
      
barplot(dens$density,space = 0,border=NA,col="#A40122",ylab="Density")
axis(side = 1,at = seq(from=0.5,to=11.5,length.out=12),labels = month.abb)
title(xlab="Month")
box(bty="l")
```

## Buttoned snout

```{r,echo=FALSE,results=TRUE}
species.data <- moth.data[which(moth.data$Species=="Buttoned snout"),]
total.caught <- sum(species.data$NumberCaught,na.rm=TRUE)
n.traps <- length(unique(species.data$TrapNumber))
first.year <- min(species.data$Year,na.rm=TRUE)
last.year <- max(species.data$Year,na.rm=TRUE)

species.data$Year <- factor(x = species.data$Year,levels = all.yrs)
```
In total, I have caught `r total.caught` individuals of Buttoned snout, in `r n.traps` traps, between `r first.year` and `r last.year`.

```{r,echo=FALSE,results=TRUE}
yr.avg.catches <- tapply(X = species.data$NumberCaught,INDEX = species.data$Year,FUN = mean)
yr.avg.catches[is.na(yr.avg.catches)] <- 0

par(mar=c(3.2,3.2,0.5,0.2))
par(mgp=c(2.0,0.2,0))
par(tck=-0.01)
par(las=1)

plot(all.yrs,yr.avg.catches,xaxp=c(min(all.yrs),max(all.yrs),length(all.yrs)-1),xlab="Year",ylab="Average Catch",pch=16,type="b",col="#A40122",bty="l")

dens <- hist(species.data$Month,breaks=seq(from=0.5,to=12.5,by=1),plot=FALSE)
      
barplot(dens$density,space = 0,border=NA,col="#A40122",ylab="Density")
axis(side = 1,at = seq(from=0.5,to=11.5,length.out=12),labels = month.abb)
title(xlab="Month")
box(bty="l")
```

## Cabbage

```{r,echo=FALSE,results=TRUE}
species.data <- moth.data[which(moth.data$Species=="Cabbage"),]
total.caught <- sum(species.data$NumberCaught,na.rm=TRUE)
n.traps <- length(unique(species.data$TrapNumber))
first.year <- min(species.data$Year,na.rm=TRUE)
last.year <- max(species.data$Year,na.rm=TRUE)

species.data$Year <- factor(x = species.data$Year,levels = all.yrs)
```
In total, I have caught `r total.caught` individuals of Cabbage, in `r n.traps` traps, between `r first.year` and `r last.year`.

```{r,echo=FALSE,results=TRUE}
yr.avg.catches <- tapply(X = species.data$NumberCaught,INDEX = species.data$Year,FUN = mean)
yr.avg.catches[is.na(yr.avg.catches)] <- 0

par(mar=c(3.2,3.2,0.5,0.2))
par(mgp=c(2.0,0.2,0))
par(tck=-0.01)
par(las=1)

plot(all.yrs,yr.avg.catches,xaxp=c(min(all.yrs),max(all.yrs),length(all.yrs)-1),xlab="Year",ylab="Average Catch",pch=16,type="b",col="#A40122",bty="l")

dens <- hist(species.data$Month,breaks=seq(from=0.5,to=12.5,by=1),plot=FALSE)
      
barplot(dens$density,space = 0,border=NA,col="#A40122",ylab="Density")
axis(side = 1,at = seq(from=0.5,to=11.5,length.out=12),labels = month.abb)
title(xlab="Month")
box(bty="l")
```

## Campion

```{r,echo=FALSE,results=TRUE}
species.data <- moth.data[which(moth.data$Species=="Campion"),]
total.caught <- sum(species.data$NumberCaught,na.rm=TRUE)
n.traps <- length(unique(species.data$TrapNumber))
first.year <- min(species.data$Year,na.rm=TRUE)
last.year <- max(species.data$Year,na.rm=TRUE)

species.data$Year <- factor(x = species.data$Year,levels = all.yrs)
```
In total, I have caught `r total.caught` individuals of Campion, in `r n.traps` traps, between `r first.year` and `r last.year`.

```{r,echo=FALSE,results=TRUE}
yr.avg.catches <- tapply(X = species.data$NumberCaught,INDEX = species.data$Year,FUN = mean)
yr.avg.catches[is.na(yr.avg.catches)] <- 0

par(mar=c(3.2,3.2,0.5,0.2))
par(mgp=c(2.0,0.2,0))
par(tck=-0.01)
par(las=1)

plot(all.yrs,yr.avg.catches,xaxp=c(min(all.yrs),max(all.yrs),length(all.yrs)-1),xlab="Year",ylab="Average Catch",pch=16,type="b",col="#A40122",bty="l")

dens <- hist(species.data$Month,breaks=seq(from=0.5,to=12.5,by=1),plot=FALSE)
      
barplot(dens$density,space = 0,border=NA,col="#A40122",ylab="Density")
axis(side = 1,at = seq(from=0.5,to=11.5,length.out=12),labels = month.abb)
title(xlab="Month")
box(bty="l")
```

## Canary-shouldered thorn

```{r,echo=FALSE,results=TRUE}
species.data <- moth.data[which(moth.data$Species=="Canary-shouldered thorn"),]
total.caught <- sum(species.data$NumberCaught,na.rm=TRUE)
n.traps <- length(unique(species.data$TrapNumber))
first.year <- min(species.data$Year,na.rm=TRUE)
last.year <- max(species.data$Year,na.rm=TRUE)

species.data$Year <- factor(x = species.data$Year,levels = all.yrs)
```
In total, I have caught `r total.caught` individuals of Canary-shouldered thorn, in `r n.traps` traps, between `r first.year` and `r last.year`.

```{r,echo=FALSE,results=TRUE}
yr.avg.catches <- tapply(X = species.data$NumberCaught,INDEX = species.data$Year,FUN = mean)
yr.avg.catches[is.na(yr.avg.catches)] <- 0

par(mar=c(3.2,3.2,0.5,0.2))
par(mgp=c(2.0,0.2,0))
par(tck=-0.01)
par(las=1)

plot(all.yrs,yr.avg.catches,xaxp=c(min(all.yrs),max(all.yrs),length(all.yrs)-1),xlab="Year",ylab="Average Catch",pch=16,type="b",col="#A40122",bty="l")

dens <- hist(species.data$Month,breaks=seq(from=0.5,to=12.5,by=1),plot=FALSE)
      
barplot(dens$density,space = 0,border=NA,col="#A40122",ylab="Density")
axis(side = 1,at = seq(from=0.5,to=11.5,length.out=12),labels = month.abb)
title(xlab="Month")
box(bty="l")
```

## Centre-barred sallow

```{r,echo=FALSE,results=TRUE}
species.data <- moth.data[which(moth.data$Species=="Centre-barred sallow"),]
total.caught <- sum(species.data$NumberCaught,na.rm=TRUE)
n.traps <- length(unique(species.data$TrapNumber))
first.year <- min(species.data$Year,na.rm=TRUE)
last.year <- max(species.data$Year,na.rm=TRUE)

species.data$Year <- factor(x = species.data$Year,levels = all.yrs)
```
In total, I have caught `r total.caught` individuals of Centre-barred sallow, in `r n.traps` traps, between `r first.year` and `r last.year`.

```{r,echo=FALSE,results=TRUE}
yr.avg.catches <- tapply(X = species.data$NumberCaught,INDEX = species.data$Year,FUN = mean)
yr.avg.catches[is.na(yr.avg.catches)] <- 0

par(mar=c(3.2,3.2,0.5,0.2))
par(mgp=c(2.0,0.2,0))
par(tck=-0.01)
par(las=1)

plot(all.yrs,yr.avg.catches,xaxp=c(min(all.yrs),max(all.yrs),length(all.yrs)-1),xlab="Year",ylab="Average Catch",pch=16,type="b",col="#A40122",bty="l")

dens <- hist(species.data$Month,breaks=seq(from=0.5,to=12.5,by=1),plot=FALSE)
      
barplot(dens$density,space = 0,border=NA,col="#A40122",ylab="Density")
axis(side = 1,at = seq(from=0.5,to=11.5,length.out=12),labels = month.abb)
title(xlab="Month")
box(bty="l")
```

## Chestnut

```{r,echo=FALSE,results=TRUE}
species.data <- moth.data[which(moth.data$Species=="Chestnut"),]
total.caught <- sum(species.data$NumberCaught,na.rm=TRUE)
n.traps <- length(unique(species.data$TrapNumber))
first.year <- min(species.data$Year,na.rm=TRUE)
last.year <- max(species.data$Year,na.rm=TRUE)

species.data$Year <- factor(x = species.data$Year,levels = all.yrs)
```
In total, I have caught `r total.caught` individuals of Chestnut, in `r n.traps` traps, between `r first.year` and `r last.year`.

```{r,echo=FALSE,results=TRUE}
yr.avg.catches <- tapply(X = species.data$NumberCaught,INDEX = species.data$Year,FUN = mean)
yr.avg.catches[is.na(yr.avg.catches)] <- 0

par(mar=c(3.2,3.2,0.5,0.2))
par(mgp=c(2.0,0.2,0))
par(tck=-0.01)
par(las=1)

plot(all.yrs,yr.avg.catches,xaxp=c(min(all.yrs),max(all.yrs),length(all.yrs)-1),xlab="Year",ylab="Average Catch",pch=16,type="b",col="#A40122",bty="l")

dens <- hist(species.data$Month,breaks=seq(from=0.5,to=12.5,by=1),plot=FALSE)
      
barplot(dens$density,space = 0,border=NA,col="#A40122",ylab="Density")
axis(side = 1,at = seq(from=0.5,to=11.5,length.out=12),labels = month.abb)
title(xlab="Month")
box(bty="l")
```

## Chinese character

```{r,echo=FALSE,results=TRUE}
species.data <- moth.data[which(moth.data$Species=="Chinese character"),]
total.caught <- sum(species.data$NumberCaught,na.rm=TRUE)
n.traps <- length(unique(species.data$TrapNumber))
first.year <- min(species.data$Year,na.rm=TRUE)
last.year <- max(species.data$Year,na.rm=TRUE)

species.data$Year <- factor(x = species.data$Year,levels = all.yrs)
```
In total, I have caught `r total.caught` individuals of Chinese character, in `r n.traps` traps, between `r first.year` and `r last.year`.

```{r,echo=FALSE,results=TRUE}
yr.avg.catches <- tapply(X = species.data$NumberCaught,INDEX = species.data$Year,FUN = mean)
yr.avg.catches[is.na(yr.avg.catches)] <- 0

par(mar=c(3.2,3.2,0.5,0.2))
par(mgp=c(2.0,0.2,0))
par(tck=-0.01)
par(las=1)

plot(all.yrs,yr.avg.catches,xaxp=c(min(all.yrs),max(all.yrs),length(all.yrs)-1),xlab="Year",ylab="Average Catch",pch=16,type="b",col="#A40122",bty="l")

dens <- hist(species.data$Month,breaks=seq(from=0.5,to=12.5,by=1),plot=FALSE)
      
barplot(dens$density,space = 0,border=NA,col="#A40122",ylab="Density")
axis(side = 1,at = seq(from=0.5,to=11.5,length.out=12),labels = month.abb)
title(xlab="Month")
box(bty="l")
```

## Chocolate-tip

```{r,echo=FALSE,results=TRUE}
species.data <- moth.data[which(moth.data$Species=="Chocolate-tip"),]
total.caught <- sum(species.data$NumberCaught,na.rm=TRUE)
n.traps <- length(unique(species.data$TrapNumber))
first.year <- min(species.data$Year,na.rm=TRUE)
last.year <- max(species.data$Year,na.rm=TRUE)

species.data$Year <- factor(x = species.data$Year,levels = all.yrs)
```
In total, I have caught `r total.caught` individuals of Chocolate-tip, in `r n.traps` traps, between `r first.year` and `r last.year`.

```{r,echo=FALSE,results=TRUE}
yr.avg.catches <- tapply(X = species.data$NumberCaught,INDEX = species.data$Year,FUN = mean)
yr.avg.catches[is.na(yr.avg.catches)] <- 0

par(mar=c(3.2,3.2,0.5,0.2))
par(mgp=c(2.0,0.2,0))
par(tck=-0.01)
par(las=1)

plot(all.yrs,yr.avg.catches,xaxp=c(min(all.yrs),max(all.yrs),length(all.yrs)-1),xlab="Year",ylab="Average Catch",pch=16,type="b",col="#A40122",bty="l")

dens <- hist(species.data$Month,breaks=seq(from=0.5,to=12.5,by=1),plot=FALSE)
      
barplot(dens$density,space = 0,border=NA,col="#A40122",ylab="Density")
axis(side = 1,at = seq(from=0.5,to=11.5,length.out=12),labels = month.abb)
title(xlab="Month")
box(bty="l")
```

## Cinnabar

```{r,echo=FALSE,results=TRUE}
species.data <- moth.data[which(moth.data$Species=="Cinnabar"),]
total.caught <- sum(species.data$NumberCaught,na.rm=TRUE)
n.traps <- length(unique(species.data$TrapNumber))
first.year <- min(species.data$Year,na.rm=TRUE)
last.year <- max(species.data$Year,na.rm=TRUE)

species.data$Year <- factor(x = species.data$Year,levels = all.yrs)
```
In total, I have caught `r total.caught` individuals of Cinnabar, in `r n.traps` traps, between `r first.year` and `r last.year`.

```{r,echo=FALSE,results=TRUE}
yr.avg.catches <- tapply(X = species.data$NumberCaught,INDEX = species.data$Year,FUN = mean)
yr.avg.catches[is.na(yr.avg.catches)] <- 0

par(mar=c(3.2,3.2,0.5,0.2))
par(mgp=c(2.0,0.2,0))
par(tck=-0.01)
par(las=1)

plot(all.yrs,yr.avg.catches,xaxp=c(min(all.yrs),max(all.yrs),length(all.yrs)-1),xlab="Year",ylab="Average Catch",pch=16,type="b",col="#A40122",bty="l")

dens <- hist(species.data$Month,breaks=seq(from=0.5,to=12.5,by=1),plot=FALSE)
      
barplot(dens$density,space = 0,border=NA,col="#A40122",ylab="Density")
axis(side = 1,at = seq(from=0.5,to=11.5,length.out=12),labels = month.abb)
title(xlab="Month")
box(bty="l")
```

## Clay

```{r,echo=FALSE,results=TRUE}
species.data <- moth.data[which(moth.data$Species=="Clay"),]
total.caught <- sum(species.data$NumberCaught,na.rm=TRUE)
n.traps <- length(unique(species.data$TrapNumber))
first.year <- min(species.data$Year,na.rm=TRUE)
last.year <- max(species.data$Year,na.rm=TRUE)

species.data$Year <- factor(x = species.data$Year,levels = all.yrs)
```
In total, I have caught `r total.caught` individuals of Clay, in `r n.traps` traps, between `r first.year` and `r last.year`.

```{r,echo=FALSE,results=TRUE}
yr.avg.catches <- tapply(X = species.data$NumberCaught,INDEX = species.data$Year,FUN = mean)
yr.avg.catches[is.na(yr.avg.catches)] <- 0

par(mar=c(3.2,3.2,0.5,0.2))
par(mgp=c(2.0,0.2,0))
par(tck=-0.01)
par(las=1)

plot(all.yrs,yr.avg.catches,xaxp=c(min(all.yrs),max(all.yrs),length(all.yrs)-1),xlab="Year",ylab="Average Catch",pch=16,type="b",col="#A40122",bty="l")

dens <- hist(species.data$Month,breaks=seq(from=0.5,to=12.5,by=1),plot=FALSE)
      
barplot(dens$density,space = 0,border=NA,col="#A40122",ylab="Density")
axis(side = 1,at = seq(from=0.5,to=11.5,length.out=12),labels = month.abb)
title(xlab="Month")
box(bty="l")
```

## Clifden non-pareil

```{r,echo=FALSE,results=TRUE}
species.data <- moth.data[which(moth.data$Species=="Clifden non-pareil"),]
total.caught <- sum(species.data$NumberCaught,na.rm=TRUE)
n.traps <- length(unique(species.data$TrapNumber))
first.year <- min(species.data$Year,na.rm=TRUE)
last.year <- max(species.data$Year,na.rm=TRUE)

species.data$Year <- factor(x = species.data$Year,levels = all.yrs)
```
In total, I have caught `r total.caught` individuals of Clifden non-pareil, in `r n.traps` traps, between `r first.year` and `r last.year`.

```{r,echo=FALSE,results=TRUE}
yr.avg.catches <- tapply(X = species.data$NumberCaught,INDEX = species.data$Year,FUN = mean)
yr.avg.catches[is.na(yr.avg.catches)] <- 0

par(mar=c(3.2,3.2,0.5,0.2))
par(mgp=c(2.0,0.2,0))
par(tck=-0.01)
par(las=1)

plot(all.yrs,yr.avg.catches,xaxp=c(min(all.yrs),max(all.yrs),length(all.yrs)-1),xlab="Year",ylab="Average Catch",pch=16,type="b",col="#A40122",bty="l")

dens <- hist(species.data$Month,breaks=seq(from=0.5,to=12.5,by=1),plot=FALSE)
      
barplot(dens$density,space = 0,border=NA,col="#A40122",ylab="Density")
axis(side = 1,at = seq(from=0.5,to=11.5,length.out=12),labels = month.abb)
title(xlab="Month")
box(bty="l")
```

## Cloaked minor

```{r,echo=FALSE,results=TRUE}
species.data <- moth.data[which(moth.data$Species=="Cloaked minor"),]
total.caught <- sum(species.data$NumberCaught,na.rm=TRUE)
n.traps <- length(unique(species.data$TrapNumber))
first.year <- min(species.data$Year,na.rm=TRUE)
last.year <- max(species.data$Year,na.rm=TRUE)

species.data$Year <- factor(x = species.data$Year,levels = all.yrs)
```
In total, I have caught `r total.caught` individuals of Cloaked minor, in `r n.traps` traps, between `r first.year` and `r last.year`.

```{r,echo=FALSE,results=TRUE}
yr.avg.catches <- tapply(X = species.data$NumberCaught,INDEX = species.data$Year,FUN = mean)
yr.avg.catches[is.na(yr.avg.catches)] <- 0

par(mar=c(3.2,3.2,0.5,0.2))
par(mgp=c(2.0,0.2,0))
par(tck=-0.01)
par(las=1)

plot(all.yrs,yr.avg.catches,xaxp=c(min(all.yrs),max(all.yrs),length(all.yrs)-1),xlab="Year",ylab="Average Catch",pch=16,type="b",col="#A40122",bty="l")

dens <- hist(species.data$Month,breaks=seq(from=0.5,to=12.5,by=1),plot=FALSE)
      
barplot(dens$density,space = 0,border=NA,col="#A40122",ylab="Density")
axis(side = 1,at = seq(from=0.5,to=11.5,length.out=12),labels = month.abb)
title(xlab="Month")
box(bty="l")
```

## Clouded border

```{r,echo=FALSE,results=TRUE}
species.data <- moth.data[which(moth.data$Species=="Clouded border"),]
total.caught <- sum(species.data$NumberCaught,na.rm=TRUE)
n.traps <- length(unique(species.data$TrapNumber))
first.year <- min(species.data$Year,na.rm=TRUE)
last.year <- max(species.data$Year,na.rm=TRUE)

species.data$Year <- factor(x = species.data$Year,levels = all.yrs)
```
In total, I have caught `r total.caught` individuals of Clouded border, in `r n.traps` traps, between `r first.year` and `r last.year`.

```{r,echo=FALSE,results=TRUE}
yr.avg.catches <- tapply(X = species.data$NumberCaught,INDEX = species.data$Year,FUN = mean)
yr.avg.catches[is.na(yr.avg.catches)] <- 0

par(mar=c(3.2,3.2,0.5,0.2))
par(mgp=c(2.0,0.2,0))
par(tck=-0.01)
par(las=1)

plot(all.yrs,yr.avg.catches,xaxp=c(min(all.yrs),max(all.yrs),length(all.yrs)-1),xlab="Year",ylab="Average Catch",pch=16,type="b",col="#A40122",bty="l")

dens <- hist(species.data$Month,breaks=seq(from=0.5,to=12.5,by=1),plot=FALSE)
      
barplot(dens$density,space = 0,border=NA,col="#A40122",ylab="Density")
axis(side = 1,at = seq(from=0.5,to=11.5,length.out=12),labels = month.abb)
title(xlab="Month")
box(bty="l")
```

## Clouded drab

```{r,echo=FALSE,results=TRUE}
species.data <- moth.data[which(moth.data$Species=="Clouded drab"),]
total.caught <- sum(species.data$NumberCaught,na.rm=TRUE)
n.traps <- length(unique(species.data$TrapNumber))
first.year <- min(species.data$Year,na.rm=TRUE)
last.year <- max(species.data$Year,na.rm=TRUE)

species.data$Year <- factor(x = species.data$Year,levels = all.yrs)
```
In total, I have caught `r total.caught` individuals of Clouded drab, in `r n.traps` traps, between `r first.year` and `r last.year`.

```{r,echo=FALSE,results=TRUE}
yr.avg.catches <- tapply(X = species.data$NumberCaught,INDEX = species.data$Year,FUN = mean)
yr.avg.catches[is.na(yr.avg.catches)] <- 0

par(mar=c(3.2,3.2,0.5,0.2))
par(mgp=c(2.0,0.2,0))
par(tck=-0.01)
par(las=1)

plot(all.yrs,yr.avg.catches,xaxp=c(min(all.yrs),max(all.yrs),length(all.yrs)-1),xlab="Year",ylab="Average Catch",pch=16,type="b",col="#A40122",bty="l")

dens <- hist(species.data$Month,breaks=seq(from=0.5,to=12.5,by=1),plot=FALSE)
      
barplot(dens$density,space = 0,border=NA,col="#A40122",ylab="Density")
axis(side = 1,at = seq(from=0.5,to=11.5,length.out=12),labels = month.abb)
title(xlab="Month")
box(bty="l")
```

## Common carpet

```{r,echo=FALSE,results=TRUE}
species.data <- moth.data[which(moth.data$Species=="Common carpet"),]
total.caught <- sum(species.data$NumberCaught,na.rm=TRUE)
n.traps <- length(unique(species.data$TrapNumber))
first.year <- min(species.data$Year,na.rm=TRUE)
last.year <- max(species.data$Year,na.rm=TRUE)

species.data$Year <- factor(x = species.data$Year,levels = all.yrs)
```
In total, I have caught `r total.caught` individuals of Common carpet, in `r n.traps` traps, between `r first.year` and `r last.year`.

```{r,echo=FALSE,results=TRUE}
yr.avg.catches <- tapply(X = species.data$NumberCaught,INDEX = species.data$Year,FUN = mean)
yr.avg.catches[is.na(yr.avg.catches)] <- 0

par(mar=c(3.2,3.2,0.5,0.2))
par(mgp=c(2.0,0.2,0))
par(tck=-0.01)
par(las=1)

plot(all.yrs,yr.avg.catches,xaxp=c(min(all.yrs),max(all.yrs),length(all.yrs)-1),xlab="Year",ylab="Average Catch",pch=16,type="b",col="#A40122",bty="l")

dens <- hist(species.data$Month,breaks=seq(from=0.5,to=12.5,by=1),plot=FALSE)
      
barplot(dens$density,space = 0,border=NA,col="#A40122",ylab="Density")
axis(side = 1,at = seq(from=0.5,to=11.5,length.out=12),labels = month.abb)
title(xlab="Month")
box(bty="l")
```

## Common emerald

```{r,echo=FALSE,results=TRUE}
species.data <- moth.data[which(moth.data$Species=="Common emerald"),]
total.caught <- sum(species.data$NumberCaught,na.rm=TRUE)
n.traps <- length(unique(species.data$TrapNumber))
first.year <- min(species.data$Year,na.rm=TRUE)
last.year <- max(species.data$Year,na.rm=TRUE)

species.data$Year <- factor(x = species.data$Year,levels = all.yrs)
```
In total, I have caught `r total.caught` individuals of Common emerald, in `r n.traps` traps, between `r first.year` and `r last.year`.

```{r,echo=FALSE,results=TRUE}
yr.avg.catches <- tapply(X = species.data$NumberCaught,INDEX = species.data$Year,FUN = mean)
yr.avg.catches[is.na(yr.avg.catches)] <- 0

par(mar=c(3.2,3.2,0.5,0.2))
par(mgp=c(2.0,0.2,0))
par(tck=-0.01)
par(las=1)

plot(all.yrs,yr.avg.catches,xaxp=c(min(all.yrs),max(all.yrs),length(all.yrs)-1),xlab="Year",ylab="Average Catch",pch=16,type="b",col="#A40122",bty="l")

dens <- hist(species.data$Month,breaks=seq(from=0.5,to=12.5,by=1),plot=FALSE)
      
barplot(dens$density,space = 0,border=NA,col="#A40122",ylab="Density")
axis(side = 1,at = seq(from=0.5,to=11.5,length.out=12),labels = month.abb)
title(xlab="Month")
box(bty="l")
```

## Common footman

```{r,echo=FALSE,results=TRUE}
species.data <- moth.data[which(moth.data$Species=="Common footman"),]
total.caught <- sum(species.data$NumberCaught,na.rm=TRUE)
n.traps <- length(unique(species.data$TrapNumber))
first.year <- min(species.data$Year,na.rm=TRUE)
last.year <- max(species.data$Year,na.rm=TRUE)

species.data$Year <- factor(x = species.data$Year,levels = all.yrs)
```
In total, I have caught `r total.caught` individuals of Common footman, in `r n.traps` traps, between `r first.year` and `r last.year`.

```{r,echo=FALSE,results=TRUE}
yr.avg.catches <- tapply(X = species.data$NumberCaught,INDEX = species.data$Year,FUN = mean)
yr.avg.catches[is.na(yr.avg.catches)] <- 0

par(mar=c(3.2,3.2,0.5,0.2))
par(mgp=c(2.0,0.2,0))
par(tck=-0.01)
par(las=1)

plot(all.yrs,yr.avg.catches,xaxp=c(min(all.yrs),max(all.yrs),length(all.yrs)-1),xlab="Year",ylab="Average Catch",pch=16,type="b",col="#A40122",bty="l")

dens <- hist(species.data$Month,breaks=seq(from=0.5,to=12.5,by=1),plot=FALSE)
      
barplot(dens$density,space = 0,border=NA,col="#A40122",ylab="Density")
axis(side = 1,at = seq(from=0.5,to=11.5,length.out=12),labels = month.abb)
title(xlab="Month")
box(bty="l")
```

## Common marbled carpet

```{r,echo=FALSE,results=TRUE}
species.data <- moth.data[which(moth.data$Species=="Common marbled carpet"),]
total.caught <- sum(species.data$NumberCaught,na.rm=TRUE)
n.traps <- length(unique(species.data$TrapNumber))
first.year <- min(species.data$Year,na.rm=TRUE)
last.year <- max(species.data$Year,na.rm=TRUE)

species.data$Year <- factor(x = species.data$Year,levels = all.yrs)
```
In total, I have caught `r total.caught` individuals of Common marbled carpet, in `r n.traps` traps, between `r first.year` and `r last.year`.

```{r,echo=FALSE,results=TRUE}
yr.avg.catches <- tapply(X = species.data$NumberCaught,INDEX = species.data$Year,FUN = mean)
yr.avg.catches[is.na(yr.avg.catches)] <- 0

par(mar=c(3.2,3.2,0.5,0.2))
par(mgp=c(2.0,0.2,0))
par(tck=-0.01)
par(las=1)

plot(all.yrs,yr.avg.catches,xaxp=c(min(all.yrs),max(all.yrs),length(all.yrs)-1),xlab="Year",ylab="Average Catch",pch=16,type="b",col="#A40122",bty="l")

dens <- hist(species.data$Month,breaks=seq(from=0.5,to=12.5,by=1),plot=FALSE)
      
barplot(dens$density,space = 0,border=NA,col="#A40122",ylab="Density")
axis(side = 1,at = seq(from=0.5,to=11.5,length.out=12),labels = month.abb)
title(xlab="Month")
box(bty="l")
```

## Common pug

```{r,echo=FALSE,results=TRUE}
species.data <- moth.data[which(moth.data$Species=="Common pug"),]
total.caught <- sum(species.data$NumberCaught,na.rm=TRUE)
n.traps <- length(unique(species.data$TrapNumber))
first.year <- min(species.data$Year,na.rm=TRUE)
last.year <- max(species.data$Year,na.rm=TRUE)

species.data$Year <- factor(x = species.data$Year,levels = all.yrs)
```
In total, I have caught `r total.caught` individuals of Common pug, in `r n.traps` traps, between `r first.year` and `r last.year`.

```{r,echo=FALSE,results=TRUE}
yr.avg.catches <- tapply(X = species.data$NumberCaught,INDEX = species.data$Year,FUN = mean)
yr.avg.catches[is.na(yr.avg.catches)] <- 0

par(mar=c(3.2,3.2,0.5,0.2))
par(mgp=c(2.0,0.2,0))
par(tck=-0.01)
par(las=1)

plot(all.yrs,yr.avg.catches,xaxp=c(min(all.yrs),max(all.yrs),length(all.yrs)-1),xlab="Year",ylab="Average Catch",pch=16,type="b",col="#A40122",bty="l")

dens <- hist(species.data$Month,breaks=seq(from=0.5,to=12.5,by=1),plot=FALSE)
      
barplot(dens$density,space = 0,border=NA,col="#A40122",ylab="Density")
axis(side = 1,at = seq(from=0.5,to=11.5,length.out=12),labels = month.abb)
title(xlab="Month")
box(bty="l")
```

## Common Quaker

```{r,echo=FALSE,results=TRUE}
species.data <- moth.data[which(moth.data$Species=="Common Quaker"),]
total.caught <- sum(species.data$NumberCaught,na.rm=TRUE)
n.traps <- length(unique(species.data$TrapNumber))
first.year <- min(species.data$Year,na.rm=TRUE)
last.year <- max(species.data$Year,na.rm=TRUE)

species.data$Year <- factor(x = species.data$Year,levels = all.yrs)
```
In total, I have caught `r total.caught` individuals of Common Quaker, in `r n.traps` traps, between `r first.year` and `r last.year`.

```{r,echo=FALSE,results=TRUE}
yr.avg.catches <- tapply(X = species.data$NumberCaught,INDEX = species.data$Year,FUN = mean)
yr.avg.catches[is.na(yr.avg.catches)] <- 0

par(mar=c(3.2,3.2,0.5,0.2))
par(mgp=c(2.0,0.2,0))
par(tck=-0.01)
par(las=1)

plot(all.yrs,yr.avg.catches,xaxp=c(min(all.yrs),max(all.yrs),length(all.yrs)-1),xlab="Year",ylab="Average Catch",pch=16,type="b",col="#A40122",bty="l")

dens <- hist(species.data$Month,breaks=seq(from=0.5,to=12.5,by=1),plot=FALSE)
      
barplot(dens$density,space = 0,border=NA,col="#A40122",ylab="Density")
axis(side = 1,at = seq(from=0.5,to=11.5,length.out=12),labels = month.abb)
title(xlab="Month")
box(bty="l")
```

## Common rustic

```{r,echo=FALSE,results=TRUE}
species.data <- moth.data[which(moth.data$Species=="Common rustic"),]
total.caught <- sum(species.data$NumberCaught,na.rm=TRUE)
n.traps <- length(unique(species.data$TrapNumber))
first.year <- min(species.data$Year,na.rm=TRUE)
last.year <- max(species.data$Year,na.rm=TRUE)

species.data$Year <- factor(x = species.data$Year,levels = all.yrs)
```
In total, I have caught `r total.caught` individuals of Common rustic, in `r n.traps` traps, between `r first.year` and `r last.year`.

```{r,echo=FALSE,results=TRUE}
yr.avg.catches <- tapply(X = species.data$NumberCaught,INDEX = species.data$Year,FUN = mean)
yr.avg.catches[is.na(yr.avg.catches)] <- 0

par(mar=c(3.2,3.2,0.5,0.2))
par(mgp=c(2.0,0.2,0))
par(tck=-0.01)
par(las=1)

plot(all.yrs,yr.avg.catches,xaxp=c(min(all.yrs),max(all.yrs),length(all.yrs)-1),xlab="Year",ylab="Average Catch",pch=16,type="b",col="#A40122",bty="l")

dens <- hist(species.data$Month,breaks=seq(from=0.5,to=12.5,by=1),plot=FALSE)
      
barplot(dens$density,space = 0,border=NA,col="#A40122",ylab="Density")
axis(side = 1,at = seq(from=0.5,to=11.5,length.out=12),labels = month.abb)
title(xlab="Month")
box(bty="l")
```

## Common swift

```{r,echo=FALSE,results=TRUE}
species.data <- moth.data[which(moth.data$Species=="Common swift"),]
total.caught <- sum(species.data$NumberCaught,na.rm=TRUE)
n.traps <- length(unique(species.data$TrapNumber))
first.year <- min(species.data$Year,na.rm=TRUE)
last.year <- max(species.data$Year,na.rm=TRUE)

species.data$Year <- factor(x = species.data$Year,levels = all.yrs)
```
In total, I have caught `r total.caught` individuals of Common swift, in `r n.traps` traps, between `r first.year` and `r last.year`.

```{r,echo=FALSE,results=TRUE}
yr.avg.catches <- tapply(X = species.data$NumberCaught,INDEX = species.data$Year,FUN = mean)
yr.avg.catches[is.na(yr.avg.catches)] <- 0

par(mar=c(3.2,3.2,0.5,0.2))
par(mgp=c(2.0,0.2,0))
par(tck=-0.01)
par(las=1)

plot(all.yrs,yr.avg.catches,xaxp=c(min(all.yrs),max(all.yrs),length(all.yrs)-1),xlab="Year",ylab="Average Catch",pch=16,type="b",col="#A40122",bty="l")

dens <- hist(species.data$Month,breaks=seq(from=0.5,to=12.5,by=1),plot=FALSE)
      
barplot(dens$density,space = 0,border=NA,col="#A40122",ylab="Density")
axis(side = 1,at = seq(from=0.5,to=11.5,length.out=12),labels = month.abb)
title(xlab="Month")
box(bty="l")
```

## Common wainscot

```{r,echo=FALSE,results=TRUE}
species.data <- moth.data[which(moth.data$Species=="Common wainscot"),]
total.caught <- sum(species.data$NumberCaught,na.rm=TRUE)
n.traps <- length(unique(species.data$TrapNumber))
first.year <- min(species.data$Year,na.rm=TRUE)
last.year <- max(species.data$Year,na.rm=TRUE)

species.data$Year <- factor(x = species.data$Year,levels = all.yrs)
```
In total, I have caught `r total.caught` individuals of Common wainscot, in `r n.traps` traps, between `r first.year` and `r last.year`.

```{r,echo=FALSE,results=TRUE}
yr.avg.catches <- tapply(X = species.data$NumberCaught,INDEX = species.data$Year,FUN = mean)
yr.avg.catches[is.na(yr.avg.catches)] <- 0

par(mar=c(3.2,3.2,0.5,0.2))
par(mgp=c(2.0,0.2,0))
par(tck=-0.01)
par(las=1)

plot(all.yrs,yr.avg.catches,xaxp=c(min(all.yrs),max(all.yrs),length(all.yrs)-1),xlab="Year",ylab="Average Catch",pch=16,type="b",col="#A40122",bty="l")

dens <- hist(species.data$Month,breaks=seq(from=0.5,to=12.5,by=1),plot=FALSE)
      
barplot(dens$density,space = 0,border=NA,col="#A40122",ylab="Density")
axis(side = 1,at = seq(from=0.5,to=11.5,length.out=12),labels = month.abb)
title(xlab="Month")
box(bty="l")
```

## Copper underwing

```{r,echo=FALSE,results=TRUE}
species.data <- moth.data[which(moth.data$Species=="Copper underwing"),]
total.caught <- sum(species.data$NumberCaught,na.rm=TRUE)
n.traps <- length(unique(species.data$TrapNumber))
first.year <- min(species.data$Year,na.rm=TRUE)
last.year <- max(species.data$Year,na.rm=TRUE)

species.data$Year <- factor(x = species.data$Year,levels = all.yrs)
```
In total, I have caught `r total.caught` individuals of Copper underwing, in `r n.traps` traps, between `r first.year` and `r last.year`.

```{r,echo=FALSE,results=TRUE}
yr.avg.catches <- tapply(X = species.data$NumberCaught,INDEX = species.data$Year,FUN = mean)
yr.avg.catches[is.na(yr.avg.catches)] <- 0

par(mar=c(3.2,3.2,0.5,0.2))
par(mgp=c(2.0,0.2,0))
par(tck=-0.01)
par(las=1)

plot(all.yrs,yr.avg.catches,xaxp=c(min(all.yrs),max(all.yrs),length(all.yrs)-1),xlab="Year",ylab="Average Catch",pch=16,type="b",col="#A40122",bty="l")

dens <- hist(species.data$Month,breaks=seq(from=0.5,to=12.5,by=1),plot=FALSE)
      
barplot(dens$density,space = 0,border=NA,col="#A40122",ylab="Density")
axis(side = 1,at = seq(from=0.5,to=11.5,length.out=12),labels = month.abb)
title(xlab="Month")
box(bty="l")
```

## Coronet

```{r,echo=FALSE,results=TRUE}
species.data <- moth.data[which(moth.data$Species=="Coronet"),]
total.caught <- sum(species.data$NumberCaught,na.rm=TRUE)
n.traps <- length(unique(species.data$TrapNumber))
first.year <- min(species.data$Year,na.rm=TRUE)
last.year <- max(species.data$Year,na.rm=TRUE)

species.data$Year <- factor(x = species.data$Year,levels = all.yrs)
```
In total, I have caught `r total.caught` individuals of Coronet, in `r n.traps` traps, between `r first.year` and `r last.year`.

```{r,echo=FALSE,results=TRUE}
yr.avg.catches <- tapply(X = species.data$NumberCaught,INDEX = species.data$Year,FUN = mean)
yr.avg.catches[is.na(yr.avg.catches)] <- 0

par(mar=c(3.2,3.2,0.5,0.2))
par(mgp=c(2.0,0.2,0))
par(tck=-0.01)
par(las=1)

plot(all.yrs,yr.avg.catches,xaxp=c(min(all.yrs),max(all.yrs),length(all.yrs)-1),xlab="Year",ylab="Average Catch",pch=16,type="b",col="#A40122",bty="l")

dens <- hist(species.data$Month,breaks=seq(from=0.5,to=12.5,by=1),plot=FALSE)
      
barplot(dens$density,space = 0,border=NA,col="#A40122",ylab="Density")
axis(side = 1,at = seq(from=0.5,to=11.5,length.out=12),labels = month.abb)
title(xlab="Month")
box(bty="l")
```

## Currant pug

```{r,echo=FALSE,results=TRUE}
species.data <- moth.data[which(moth.data$Species=="Currant pug"),]
total.caught <- sum(species.data$NumberCaught,na.rm=TRUE)
n.traps <- length(unique(species.data$TrapNumber))
first.year <- min(species.data$Year,na.rm=TRUE)
last.year <- max(species.data$Year,na.rm=TRUE)

species.data$Year <- factor(x = species.data$Year,levels = all.yrs)
```
In total, I have caught `r total.caught` individuals of Currant pug, in `r n.traps` traps, between `r first.year` and `r last.year`.

```{r,echo=FALSE,results=TRUE}
yr.avg.catches <- tapply(X = species.data$NumberCaught,INDEX = species.data$Year,FUN = mean)
yr.avg.catches[is.na(yr.avg.catches)] <- 0

par(mar=c(3.2,3.2,0.5,0.2))
par(mgp=c(2.0,0.2,0))
par(tck=-0.01)
par(las=1)

plot(all.yrs,yr.avg.catches,xaxp=c(min(all.yrs),max(all.yrs),length(all.yrs)-1),xlab="Year",ylab="Average Catch",pch=16,type="b",col="#A40122",bty="l")

dens <- hist(species.data$Month,breaks=seq(from=0.5,to=12.5,by=1),plot=FALSE)
      
barplot(dens$density,space = 0,border=NA,col="#A40122",ylab="Density")
axis(side = 1,at = seq(from=0.5,to=11.5,length.out=12),labels = month.abb)
title(xlab="Month")
box(bty="l")
```

## Cypress pug

```{r,echo=FALSE,results=TRUE}
species.data <- moth.data[which(moth.data$Species=="Cypress pug"),]
total.caught <- sum(species.data$NumberCaught,na.rm=TRUE)
n.traps <- length(unique(species.data$TrapNumber))
first.year <- min(species.data$Year,na.rm=TRUE)
last.year <- max(species.data$Year,na.rm=TRUE)

species.data$Year <- factor(x = species.data$Year,levels = all.yrs)
```
In total, I have caught `r total.caught` individuals of Cypress pug, in `r n.traps` traps, between `r first.year` and `r last.year`.

```{r,echo=FALSE,results=TRUE}
yr.avg.catches <- tapply(X = species.data$NumberCaught,INDEX = species.data$Year,FUN = mean)
yr.avg.catches[is.na(yr.avg.catches)] <- 0

par(mar=c(3.2,3.2,0.5,0.2))
par(mgp=c(2.0,0.2,0))
par(tck=-0.01)
par(las=1)

plot(all.yrs,yr.avg.catches,xaxp=c(min(all.yrs),max(all.yrs),length(all.yrs)-1),xlab="Year",ylab="Average Catch",pch=16,type="b",col="#A40122",bty="l")

dens <- hist(species.data$Month,breaks=seq(from=0.5,to=12.5,by=1),plot=FALSE)
      
barplot(dens$density,space = 0,border=NA,col="#A40122",ylab="Density")
axis(side = 1,at = seq(from=0.5,to=11.5,length.out=12),labels = month.abb)
title(xlab="Month")
box(bty="l")
```

## Dagger

```{r,echo=FALSE,results=TRUE}
species.data <- moth.data[which(moth.data$Species=="Dagger"),]
total.caught <- sum(species.data$NumberCaught,na.rm=TRUE)
n.traps <- length(unique(species.data$TrapNumber))
first.year <- min(species.data$Year,na.rm=TRUE)
last.year <- max(species.data$Year,na.rm=TRUE)

species.data$Year <- factor(x = species.data$Year,levels = all.yrs)
```
In total, I have caught `r total.caught` individuals of Dagger, in `r n.traps` traps, between `r first.year` and `r last.year`.

```{r,echo=FALSE,results=TRUE}
yr.avg.catches <- tapply(X = species.data$NumberCaught,INDEX = species.data$Year,FUN = mean)
yr.avg.catches[is.na(yr.avg.catches)] <- 0

par(mar=c(3.2,3.2,0.5,0.2))
par(mgp=c(2.0,0.2,0))
par(tck=-0.01)
par(las=1)

plot(all.yrs,yr.avg.catches,xaxp=c(min(all.yrs),max(all.yrs),length(all.yrs)-1),xlab="Year",ylab="Average Catch",pch=16,type="b",col="#A40122",bty="l")

dens <- hist(species.data$Month,breaks=seq(from=0.5,to=12.5,by=1),plot=FALSE)
      
barplot(dens$density,space = 0,border=NA,col="#A40122",ylab="Density")
axis(side = 1,at = seq(from=0.5,to=11.5,length.out=12),labels = month.abb)
title(xlab="Month")
box(bty="l")
```

## Dark arches

```{r,echo=FALSE,results=TRUE}
species.data <- moth.data[which(moth.data$Species=="Dark arches"),]
total.caught <- sum(species.data$NumberCaught,na.rm=TRUE)
n.traps <- length(unique(species.data$TrapNumber))
first.year <- min(species.data$Year,na.rm=TRUE)
last.year <- max(species.data$Year,na.rm=TRUE)

species.data$Year <- factor(x = species.data$Year,levels = all.yrs)
```
In total, I have caught `r total.caught` individuals of Dark arches, in `r n.traps` traps, between `r first.year` and `r last.year`.

```{r,echo=FALSE,results=TRUE}
yr.avg.catches <- tapply(X = species.data$NumberCaught,INDEX = species.data$Year,FUN = mean)
yr.avg.catches[is.na(yr.avg.catches)] <- 0

par(mar=c(3.2,3.2,0.5,0.2))
par(mgp=c(2.0,0.2,0))
par(tck=-0.01)
par(las=1)

plot(all.yrs,yr.avg.catches,xaxp=c(min(all.yrs),max(all.yrs),length(all.yrs)-1),xlab="Year",ylab="Average Catch",pch=16,type="b",col="#A40122",bty="l")

dens <- hist(species.data$Month,breaks=seq(from=0.5,to=12.5,by=1),plot=FALSE)
      
barplot(dens$density,space = 0,border=NA,col="#A40122",ylab="Density")
axis(side = 1,at = seq(from=0.5,to=11.5,length.out=12),labels = month.abb)
title(xlab="Month")
box(bty="l")
```

## Dark chestnut

```{r,echo=FALSE,results=TRUE}
species.data <- moth.data[which(moth.data$Species=="Dark chestnut"),]
total.caught <- sum(species.data$NumberCaught,na.rm=TRUE)
n.traps <- length(unique(species.data$TrapNumber))
first.year <- min(species.data$Year,na.rm=TRUE)
last.year <- max(species.data$Year,na.rm=TRUE)

species.data$Year <- factor(x = species.data$Year,levels = all.yrs)
```
In total, I have caught `r total.caught` individuals of Dark chestnut, in `r n.traps` traps, between `r first.year` and `r last.year`.

```{r,echo=FALSE,results=TRUE}
yr.avg.catches <- tapply(X = species.data$NumberCaught,INDEX = species.data$Year,FUN = mean)
yr.avg.catches[is.na(yr.avg.catches)] <- 0

par(mar=c(3.2,3.2,0.5,0.2))
par(mgp=c(2.0,0.2,0))
par(tck=-0.01)
par(las=1)

plot(all.yrs,yr.avg.catches,xaxp=c(min(all.yrs),max(all.yrs),length(all.yrs)-1),xlab="Year",ylab="Average Catch",pch=16,type="b",col="#A40122",bty="l")

dens <- hist(species.data$Month,breaks=seq(from=0.5,to=12.5,by=1),plot=FALSE)
      
barplot(dens$density,space = 0,border=NA,col="#A40122",ylab="Density")
axis(side = 1,at = seq(from=0.5,to=11.5,length.out=12),labels = month.abb)
title(xlab="Month")
box(bty="l")
```

## Dark spectacle

```{r,echo=FALSE,results=TRUE}
species.data <- moth.data[which(moth.data$Species=="Dark spectacle"),]
total.caught <- sum(species.data$NumberCaught,na.rm=TRUE)
n.traps <- length(unique(species.data$TrapNumber))
first.year <- min(species.data$Year,na.rm=TRUE)
last.year <- max(species.data$Year,na.rm=TRUE)

species.data$Year <- factor(x = species.data$Year,levels = all.yrs)
```
In total, I have caught `r total.caught` individuals of Dark spectacle, in `r n.traps` traps, between `r first.year` and `r last.year`.

```{r,echo=FALSE,results=TRUE}
yr.avg.catches <- tapply(X = species.data$NumberCaught,INDEX = species.data$Year,FUN = mean)
yr.avg.catches[is.na(yr.avg.catches)] <- 0

par(mar=c(3.2,3.2,0.5,0.2))
par(mgp=c(2.0,0.2,0))
par(tck=-0.01)
par(las=1)

plot(all.yrs,yr.avg.catches,xaxp=c(min(all.yrs),max(all.yrs),length(all.yrs)-1),xlab="Year",ylab="Average Catch",pch=16,type="b",col="#A40122",bty="l")

dens <- hist(species.data$Month,breaks=seq(from=0.5,to=12.5,by=1),plot=FALSE)
      
barplot(dens$density,space = 0,border=NA,col="#A40122",ylab="Density")
axis(side = 1,at = seq(from=0.5,to=11.5,length.out=12),labels = month.abb)
title(xlab="Month")
box(bty="l")
```

## Dark sword-grass

```{r,echo=FALSE,results=TRUE}
species.data <- moth.data[which(moth.data$Species=="Dark sword-grass"),]
total.caught <- sum(species.data$NumberCaught,na.rm=TRUE)
n.traps <- length(unique(species.data$TrapNumber))
first.year <- min(species.data$Year,na.rm=TRUE)
last.year <- max(species.data$Year,na.rm=TRUE)

species.data$Year <- factor(x = species.data$Year,levels = all.yrs)
```
In total, I have caught `r total.caught` individuals of Dark sword-grass, in `r n.traps` traps, between `r first.year` and `r last.year`.

```{r,echo=FALSE,results=TRUE}
yr.avg.catches <- tapply(X = species.data$NumberCaught,INDEX = species.data$Year,FUN = mean)
yr.avg.catches[is.na(yr.avg.catches)] <- 0

par(mar=c(3.2,3.2,0.5,0.2))
par(mgp=c(2.0,0.2,0))
par(tck=-0.01)
par(las=1)

plot(all.yrs,yr.avg.catches,xaxp=c(min(all.yrs),max(all.yrs),length(all.yrs)-1),xlab="Year",ylab="Average Catch",pch=16,type="b",col="#A40122",bty="l")

dens <- hist(species.data$Month,breaks=seq(from=0.5,to=12.5,by=1),plot=FALSE)
      
barplot(dens$density,space = 0,border=NA,col="#A40122",ylab="Density")
axis(side = 1,at = seq(from=0.5,to=11.5,length.out=12),labels = month.abb)
title(xlab="Month")
box(bty="l")
```

## December moth

```{r,echo=FALSE,results=TRUE}
species.data <- moth.data[which(moth.data$Species=="December moth"),]
total.caught <- sum(species.data$NumberCaught,na.rm=TRUE)
n.traps <- length(unique(species.data$TrapNumber))
first.year <- min(species.data$Year,na.rm=TRUE)
last.year <- max(species.data$Year,na.rm=TRUE)

species.data$Year <- factor(x = species.data$Year,levels = all.yrs)
```
In total, I have caught `r total.caught` individuals of December moth, in `r n.traps` traps, between `r first.year` and `r last.year`.

```{r,echo=FALSE,results=TRUE}
yr.avg.catches <- tapply(X = species.data$NumberCaught,INDEX = species.data$Year,FUN = mean)
yr.avg.catches[is.na(yr.avg.catches)] <- 0

par(mar=c(3.2,3.2,0.5,0.2))
par(mgp=c(2.0,0.2,0))
par(tck=-0.01)
par(las=1)

plot(all.yrs,yr.avg.catches,xaxp=c(min(all.yrs),max(all.yrs),length(all.yrs)-1),xlab="Year",ylab="Average Catch",pch=16,type="b",col="#A40122",bty="l")

dens <- hist(species.data$Month,breaks=seq(from=0.5,to=12.5,by=1),plot=FALSE)
      
barplot(dens$density,space = 0,border=NA,col="#A40122",ylab="Density")
axis(side = 1,at = seq(from=0.5,to=11.5,length.out=12),labels = month.abb)
title(xlab="Month")
box(bty="l")
```

## Delicate

```{r,echo=FALSE,results=TRUE}
species.data <- moth.data[which(moth.data$Species=="Delicate"),]
total.caught <- sum(species.data$NumberCaught,na.rm=TRUE)
n.traps <- length(unique(species.data$TrapNumber))
first.year <- min(species.data$Year,na.rm=TRUE)
last.year <- max(species.data$Year,na.rm=TRUE)

species.data$Year <- factor(x = species.data$Year,levels = all.yrs)
```
In total, I have caught `r total.caught` individuals of Delicate, in `r n.traps` traps, between `r first.year` and `r last.year`.

```{r,echo=FALSE,results=TRUE}
yr.avg.catches <- tapply(X = species.data$NumberCaught,INDEX = species.data$Year,FUN = mean)
yr.avg.catches[is.na(yr.avg.catches)] <- 0

par(mar=c(3.2,3.2,0.5,0.2))
par(mgp=c(2.0,0.2,0))
par(tck=-0.01)
par(las=1)

plot(all.yrs,yr.avg.catches,xaxp=c(min(all.yrs),max(all.yrs),length(all.yrs)-1),xlab="Year",ylab="Average Catch",pch=16,type="b",col="#A40122",bty="l")

dens <- hist(species.data$Month,breaks=seq(from=0.5,to=12.5,by=1),plot=FALSE)
      
barplot(dens$density,space = 0,border=NA,col="#A40122",ylab="Density")
axis(side = 1,at = seq(from=0.5,to=11.5,length.out=12),labels = month.abb)
title(xlab="Month")
box(bty="l")
```

## Dewick's plusia

```{r,echo=FALSE,results=TRUE}
species.data <- moth.data[which(moth.data$Species=="Dewick's plusia"),]
total.caught <- sum(species.data$NumberCaught,na.rm=TRUE)
n.traps <- length(unique(species.data$TrapNumber))
first.year <- min(species.data$Year,na.rm=TRUE)
last.year <- max(species.data$Year,na.rm=TRUE)

species.data$Year <- factor(x = species.data$Year,levels = all.yrs)
```
In total, I have caught `r total.caught` individuals of Dewick's plusia, in `r n.traps` traps, between `r first.year` and `r last.year`.

```{r,echo=FALSE,results=TRUE}
yr.avg.catches <- tapply(X = species.data$NumberCaught,INDEX = species.data$Year,FUN = mean)
yr.avg.catches[is.na(yr.avg.catches)] <- 0

par(mar=c(3.2,3.2,0.5,0.2))
par(mgp=c(2.0,0.2,0))
par(tck=-0.01)
par(las=1)

plot(all.yrs,yr.avg.catches,xaxp=c(min(all.yrs),max(all.yrs),length(all.yrs)-1),xlab="Year",ylab="Average Catch",pch=16,type="b",col="#A40122",bty="l")

dens <- hist(species.data$Month,breaks=seq(from=0.5,to=12.5,by=1),plot=FALSE)
      
barplot(dens$density,space = 0,border=NA,col="#A40122",ylab="Density")
axis(side = 1,at = seq(from=0.5,to=11.5,length.out=12),labels = month.abb)
title(xlab="Month")
box(bty="l")
```

## Dingy footman

```{r,echo=FALSE,results=TRUE}
species.data <- moth.data[which(moth.data$Species=="Dingy footman"),]
total.caught <- sum(species.data$NumberCaught,na.rm=TRUE)
n.traps <- length(unique(species.data$TrapNumber))
first.year <- min(species.data$Year,na.rm=TRUE)
last.year <- max(species.data$Year,na.rm=TRUE)

species.data$Year <- factor(x = species.data$Year,levels = all.yrs)
```
In total, I have caught `r total.caught` individuals of Dingy footman, in `r n.traps` traps, between `r first.year` and `r last.year`.

```{r,echo=FALSE,results=TRUE}
yr.avg.catches <- tapply(X = species.data$NumberCaught,INDEX = species.data$Year,FUN = mean)
yr.avg.catches[is.na(yr.avg.catches)] <- 0

par(mar=c(3.2,3.2,0.5,0.2))
par(mgp=c(2.0,0.2,0))
par(tck=-0.01)
par(las=1)

plot(all.yrs,yr.avg.catches,xaxp=c(min(all.yrs),max(all.yrs),length(all.yrs)-1),xlab="Year",ylab="Average Catch",pch=16,type="b",col="#A40122",bty="l")

dens <- hist(species.data$Month,breaks=seq(from=0.5,to=12.5,by=1),plot=FALSE)
      
barplot(dens$density,space = 0,border=NA,col="#A40122",ylab="Density")
axis(side = 1,at = seq(from=0.5,to=11.5,length.out=12),labels = month.abb)
title(xlab="Month")
box(bty="l")
```

## Dog's tooth

```{r,echo=FALSE,results=TRUE}
species.data <- moth.data[which(moth.data$Species=="Dog's tooth"),]
total.caught <- sum(species.data$NumberCaught,na.rm=TRUE)
n.traps <- length(unique(species.data$TrapNumber))
first.year <- min(species.data$Year,na.rm=TRUE)
last.year <- max(species.data$Year,na.rm=TRUE)

species.data$Year <- factor(x = species.data$Year,levels = all.yrs)
```
In total, I have caught `r total.caught` individuals of Dog's tooth, in `r n.traps` traps, between `r first.year` and `r last.year`.

```{r,echo=FALSE,results=TRUE}
yr.avg.catches <- tapply(X = species.data$NumberCaught,INDEX = species.data$Year,FUN = mean)
yr.avg.catches[is.na(yr.avg.catches)] <- 0

par(mar=c(3.2,3.2,0.5,0.2))
par(mgp=c(2.0,0.2,0))
par(tck=-0.01)
par(las=1)

plot(all.yrs,yr.avg.catches,xaxp=c(min(all.yrs),max(all.yrs),length(all.yrs)-1),xlab="Year",ylab="Average Catch",pch=16,type="b",col="#A40122",bty="l")

dens <- hist(species.data$Month,breaks=seq(from=0.5,to=12.5,by=1),plot=FALSE)
      
barplot(dens$density,space = 0,border=NA,col="#A40122",ylab="Density")
axis(side = 1,at = seq(from=0.5,to=11.5,length.out=12),labels = month.abb)
title(xlab="Month")
box(bty="l")
```

## Dot moth

```{r,echo=FALSE,results=TRUE}
species.data <- moth.data[which(moth.data$Species=="Dot moth"),]
total.caught <- sum(species.data$NumberCaught,na.rm=TRUE)
n.traps <- length(unique(species.data$TrapNumber))
first.year <- min(species.data$Year,na.rm=TRUE)
last.year <- max(species.data$Year,na.rm=TRUE)

species.data$Year <- factor(x = species.data$Year,levels = all.yrs)
```
In total, I have caught `r total.caught` individuals of Dot moth, in `r n.traps` traps, between `r first.year` and `r last.year`.

```{r,echo=FALSE,results=TRUE}
yr.avg.catches <- tapply(X = species.data$NumberCaught,INDEX = species.data$Year,FUN = mean)
yr.avg.catches[is.na(yr.avg.catches)] <- 0

par(mar=c(3.2,3.2,0.5,0.2))
par(mgp=c(2.0,0.2,0))
par(tck=-0.01)
par(las=1)

plot(all.yrs,yr.avg.catches,xaxp=c(min(all.yrs),max(all.yrs),length(all.yrs)-1),xlab="Year",ylab="Average Catch",pch=16,type="b",col="#A40122",bty="l")

dens <- hist(species.data$Month,breaks=seq(from=0.5,to=12.5,by=1),plot=FALSE)
      
barplot(dens$density,space = 0,border=NA,col="#A40122",ylab="Density")
axis(side = 1,at = seq(from=0.5,to=11.5,length.out=12),labels = month.abb)
title(xlab="Month")
box(bty="l")
```

## Dotted border

```{r,echo=FALSE,results=TRUE}
species.data <- moth.data[which(moth.data$Species=="Dotted border"),]
total.caught <- sum(species.data$NumberCaught,na.rm=TRUE)
n.traps <- length(unique(species.data$TrapNumber))
first.year <- min(species.data$Year,na.rm=TRUE)
last.year <- max(species.data$Year,na.rm=TRUE)

species.data$Year <- factor(x = species.data$Year,levels = all.yrs)
```
In total, I have caught `r total.caught` individuals of Dotted border, in `r n.traps` traps, between `r first.year` and `r last.year`.

```{r,echo=FALSE,results=TRUE}
yr.avg.catches <- tapply(X = species.data$NumberCaught,INDEX = species.data$Year,FUN = mean)
yr.avg.catches[is.na(yr.avg.catches)] <- 0

par(mar=c(3.2,3.2,0.5,0.2))
par(mgp=c(2.0,0.2,0))
par(tck=-0.01)
par(las=1)

plot(all.yrs,yr.avg.catches,xaxp=c(min(all.yrs),max(all.yrs),length(all.yrs)-1),xlab="Year",ylab="Average Catch",pch=16,type="b",col="#A40122",bty="l")

dens <- hist(species.data$Month,breaks=seq(from=0.5,to=12.5,by=1),plot=FALSE)
      
barplot(dens$density,space = 0,border=NA,col="#A40122",ylab="Density")
axis(side = 1,at = seq(from=0.5,to=11.5,length.out=12),labels = month.abb)
title(xlab="Month")
box(bty="l")
```

## Double-striped pug

```{r,echo=FALSE,results=TRUE}
species.data <- moth.data[which(moth.data$Species=="Double-striped pug"),]
total.caught <- sum(species.data$NumberCaught,na.rm=TRUE)
n.traps <- length(unique(species.data$TrapNumber))
first.year <- min(species.data$Year,na.rm=TRUE)
last.year <- max(species.data$Year,na.rm=TRUE)

species.data$Year <- factor(x = species.data$Year,levels = all.yrs)
```
In total, I have caught `r total.caught` individuals of Double-striped pug, in `r n.traps` traps, between `r first.year` and `r last.year`.

```{r,echo=FALSE,results=TRUE}
yr.avg.catches <- tapply(X = species.data$NumberCaught,INDEX = species.data$Year,FUN = mean)
yr.avg.catches[is.na(yr.avg.catches)] <- 0

par(mar=c(3.2,3.2,0.5,0.2))
par(mgp=c(2.0,0.2,0))
par(tck=-0.01)
par(las=1)

plot(all.yrs,yr.avg.catches,xaxp=c(min(all.yrs),max(all.yrs),length(all.yrs)-1),xlab="Year",ylab="Average Catch",pch=16,type="b",col="#A40122",bty="l")

dens <- hist(species.data$Month,breaks=seq(from=0.5,to=12.5,by=1),plot=FALSE)
      
barplot(dens$density,space = 0,border=NA,col="#A40122",ylab="Density")
axis(side = 1,at = seq(from=0.5,to=11.5,length.out=12),labels = month.abb)
title(xlab="Month")
box(bty="l")
```

## Double square-spot

```{r,echo=FALSE,results=TRUE}
species.data <- moth.data[which(moth.data$Species=="Double square-spot"),]
total.caught <- sum(species.data$NumberCaught,na.rm=TRUE)
n.traps <- length(unique(species.data$TrapNumber))
first.year <- min(species.data$Year,na.rm=TRUE)
last.year <- max(species.data$Year,na.rm=TRUE)

species.data$Year <- factor(x = species.data$Year,levels = all.yrs)
```
In total, I have caught `r total.caught` individuals of Double square-spot, in `r n.traps` traps, between `r first.year` and `r last.year`.

```{r,echo=FALSE,results=TRUE}
yr.avg.catches <- tapply(X = species.data$NumberCaught,INDEX = species.data$Year,FUN = mean)
yr.avg.catches[is.na(yr.avg.catches)] <- 0

par(mar=c(3.2,3.2,0.5,0.2))
par(mgp=c(2.0,0.2,0))
par(tck=-0.01)
par(las=1)

plot(all.yrs,yr.avg.catches,xaxp=c(min(all.yrs),max(all.yrs),length(all.yrs)-1),xlab="Year",ylab="Average Catch",pch=16,type="b",col="#A40122",bty="l")

dens <- hist(species.data$Month,breaks=seq(from=0.5,to=12.5,by=1),plot=FALSE)
      
barplot(dens$density,space = 0,border=NA,col="#A40122",ylab="Density")
axis(side = 1,at = seq(from=0.5,to=11.5,length.out=12),labels = month.abb)
title(xlab="Month")
box(bty="l")
```

## Drinker

```{r,echo=FALSE,results=TRUE}
species.data <- moth.data[which(moth.data$Species=="Drinker"),]
total.caught <- sum(species.data$NumberCaught,na.rm=TRUE)
n.traps <- length(unique(species.data$TrapNumber))
first.year <- min(species.data$Year,na.rm=TRUE)
last.year <- max(species.data$Year,na.rm=TRUE)

species.data$Year <- factor(x = species.data$Year,levels = all.yrs)
```
In total, I have caught `r total.caught` individuals of Drinker, in `r n.traps` traps, between `r first.year` and `r last.year`.

```{r,echo=FALSE,results=TRUE}
yr.avg.catches <- tapply(X = species.data$NumberCaught,INDEX = species.data$Year,FUN = mean)
yr.avg.catches[is.na(yr.avg.catches)] <- 0

par(mar=c(3.2,3.2,0.5,0.2))
par(mgp=c(2.0,0.2,0))
par(tck=-0.01)
par(las=1)

plot(all.yrs,yr.avg.catches,xaxp=c(min(all.yrs),max(all.yrs),length(all.yrs)-1),xlab="Year",ylab="Average Catch",pch=16,type="b",col="#A40122",bty="l")

dens <- hist(species.data$Month,breaks=seq(from=0.5,to=12.5,by=1),plot=FALSE)
      
barplot(dens$density,space = 0,border=NA,col="#A40122",ylab="Density")
axis(side = 1,at = seq(from=0.5,to=11.5,length.out=12),labels = month.abb)
title(xlab="Month")
box(bty="l")
```

## Dun-bar

```{r,echo=FALSE,results=TRUE}
species.data <- moth.data[which(moth.data$Species=="Dun-bar"),]
total.caught <- sum(species.data$NumberCaught,na.rm=TRUE)
n.traps <- length(unique(species.data$TrapNumber))
first.year <- min(species.data$Year,na.rm=TRUE)
last.year <- max(species.data$Year,na.rm=TRUE)

species.data$Year <- factor(x = species.data$Year,levels = all.yrs)
```
In total, I have caught `r total.caught` individuals of Dun-bar, in `r n.traps` traps, between `r first.year` and `r last.year`.

```{r,echo=FALSE,results=TRUE}
yr.avg.catches <- tapply(X = species.data$NumberCaught,INDEX = species.data$Year,FUN = mean)
yr.avg.catches[is.na(yr.avg.catches)] <- 0

par(mar=c(3.2,3.2,0.5,0.2))
par(mgp=c(2.0,0.2,0))
par(tck=-0.01)
par(las=1)

plot(all.yrs,yr.avg.catches,xaxp=c(min(all.yrs),max(all.yrs),length(all.yrs)-1),xlab="Year",ylab="Average Catch",pch=16,type="b",col="#A40122",bty="l")

dens <- hist(species.data$Month,breaks=seq(from=0.5,to=12.5,by=1),plot=FALSE)
      
barplot(dens$density,space = 0,border=NA,col="#A40122",ylab="Density")
axis(side = 1,at = seq(from=0.5,to=11.5,length.out=12),labels = month.abb)
title(xlab="Month")
box(bty="l")
```

## Dusky brocade

```{r,echo=FALSE,results=TRUE}
species.data <- moth.data[which(moth.data$Species=="Dusky brocade"),]
total.caught <- sum(species.data$NumberCaught,na.rm=TRUE)
n.traps <- length(unique(species.data$TrapNumber))
first.year <- min(species.data$Year,na.rm=TRUE)
last.year <- max(species.data$Year,na.rm=TRUE)

species.data$Year <- factor(x = species.data$Year,levels = all.yrs)
```
In total, I have caught `r total.caught` individuals of Dusky brocade, in `r n.traps` traps, between `r first.year` and `r last.year`.

```{r,echo=FALSE,results=TRUE}
yr.avg.catches <- tapply(X = species.data$NumberCaught,INDEX = species.data$Year,FUN = mean)
yr.avg.catches[is.na(yr.avg.catches)] <- 0

par(mar=c(3.2,3.2,0.5,0.2))
par(mgp=c(2.0,0.2,0))
par(tck=-0.01)
par(las=1)

plot(all.yrs,yr.avg.catches,xaxp=c(min(all.yrs),max(all.yrs),length(all.yrs)-1),xlab="Year",ylab="Average Catch",pch=16,type="b",col="#A40122",bty="l")

dens <- hist(species.data$Month,breaks=seq(from=0.5,to=12.5,by=1),plot=FALSE)
      
barplot(dens$density,space = 0,border=NA,col="#A40122",ylab="Density")
axis(side = 1,at = seq(from=0.5,to=11.5,length.out=12),labels = month.abb)
title(xlab="Month")
box(bty="l")
```

## Dusky sallow

```{r,echo=FALSE,results=TRUE}
species.data <- moth.data[which(moth.data$Species=="Dusky sallow"),]
total.caught <- sum(species.data$NumberCaught,na.rm=TRUE)
n.traps <- length(unique(species.data$TrapNumber))
first.year <- min(species.data$Year,na.rm=TRUE)
last.year <- max(species.data$Year,na.rm=TRUE)

species.data$Year <- factor(x = species.data$Year,levels = all.yrs)
```
In total, I have caught `r total.caught` individuals of Dusky sallow, in `r n.traps` traps, between `r first.year` and `r last.year`.

```{r,echo=FALSE,results=TRUE}
yr.avg.catches <- tapply(X = species.data$NumberCaught,INDEX = species.data$Year,FUN = mean)
yr.avg.catches[is.na(yr.avg.catches)] <- 0

par(mar=c(3.2,3.2,0.5,0.2))
par(mgp=c(2.0,0.2,0))
par(tck=-0.01)
par(las=1)

plot(all.yrs,yr.avg.catches,xaxp=c(min(all.yrs),max(all.yrs),length(all.yrs)-1),xlab="Year",ylab="Average Catch",pch=16,type="b",col="#A40122",bty="l")

dens <- hist(species.data$Month,breaks=seq(from=0.5,to=12.5,by=1),plot=FALSE)
      
barplot(dens$density,space = 0,border=NA,col="#A40122",ylab="Density")
axis(side = 1,at = seq(from=0.5,to=11.5,length.out=12),labels = month.abb)
title(xlab="Month")
box(bty="l")
```

## Dusky thorn

```{r,echo=FALSE,results=TRUE}
species.data <- moth.data[which(moth.data$Species=="Dusky thorn"),]
total.caught <- sum(species.data$NumberCaught,na.rm=TRUE)
n.traps <- length(unique(species.data$TrapNumber))
first.year <- min(species.data$Year,na.rm=TRUE)
last.year <- max(species.data$Year,na.rm=TRUE)

species.data$Year <- factor(x = species.data$Year,levels = all.yrs)
```
In total, I have caught `r total.caught` individuals of Dusky thorn, in `r n.traps` traps, between `r first.year` and `r last.year`.

```{r,echo=FALSE,results=TRUE}
yr.avg.catches <- tapply(X = species.data$NumberCaught,INDEX = species.data$Year,FUN = mean)
yr.avg.catches[is.na(yr.avg.catches)] <- 0

par(mar=c(3.2,3.2,0.5,0.2))
par(mgp=c(2.0,0.2,0))
par(tck=-0.01)
par(las=1)

plot(all.yrs,yr.avg.catches,xaxp=c(min(all.yrs),max(all.yrs),length(all.yrs)-1),xlab="Year",ylab="Average Catch",pch=16,type="b",col="#A40122",bty="l")

dens <- hist(species.data$Month,breaks=seq(from=0.5,to=12.5,by=1),plot=FALSE)
      
barplot(dens$density,space = 0,border=NA,col="#A40122",ylab="Density")
axis(side = 1,at = seq(from=0.5,to=11.5,length.out=12),labels = month.abb)
title(xlab="Month")
box(bty="l")
```

## Dwarf cream wave

```{r,echo=FALSE,results=TRUE}
species.data <- moth.data[which(moth.data$Species=="Dwarf cream wave"),]
total.caught <- sum(species.data$NumberCaught,na.rm=TRUE)
n.traps <- length(unique(species.data$TrapNumber))
first.year <- min(species.data$Year,na.rm=TRUE)
last.year <- max(species.data$Year,na.rm=TRUE)

species.data$Year <- factor(x = species.data$Year,levels = all.yrs)
```
In total, I have caught `r total.caught` individuals of Dwarf cream wave, in `r n.traps` traps, between `r first.year` and `r last.year`.

```{r,echo=FALSE,results=TRUE}
yr.avg.catches <- tapply(X = species.data$NumberCaught,INDEX = species.data$Year,FUN = mean)
yr.avg.catches[is.na(yr.avg.catches)] <- 0

par(mar=c(3.2,3.2,0.5,0.2))
par(mgp=c(2.0,0.2,0))
par(tck=-0.01)
par(las=1)

plot(all.yrs,yr.avg.catches,xaxp=c(min(all.yrs),max(all.yrs),length(all.yrs)-1),xlab="Year",ylab="Average Catch",pch=16,type="b",col="#A40122",bty="l")

dens <- hist(species.data$Month,breaks=seq(from=0.5,to=12.5,by=1),plot=FALSE)
      
barplot(dens$density,space = 0,border=NA,col="#A40122",ylab="Density")
axis(side = 1,at = seq(from=0.5,to=11.5,length.out=12),labels = month.abb)
title(xlab="Month")
box(bty="l")
```

## Early grey

```{r,echo=FALSE,results=TRUE}
species.data <- moth.data[which(moth.data$Species=="Early grey"),]
total.caught <- sum(species.data$NumberCaught,na.rm=TRUE)
n.traps <- length(unique(species.data$TrapNumber))
first.year <- min(species.data$Year,na.rm=TRUE)
last.year <- max(species.data$Year,na.rm=TRUE)

species.data$Year <- factor(x = species.data$Year,levels = all.yrs)
```
In total, I have caught `r total.caught` individuals of Early grey, in `r n.traps` traps, between `r first.year` and `r last.year`.

```{r,echo=FALSE,results=TRUE}
yr.avg.catches <- tapply(X = species.data$NumberCaught,INDEX = species.data$Year,FUN = mean)
yr.avg.catches[is.na(yr.avg.catches)] <- 0

par(mar=c(3.2,3.2,0.5,0.2))
par(mgp=c(2.0,0.2,0))
par(tck=-0.01)
par(las=1)

plot(all.yrs,yr.avg.catches,xaxp=c(min(all.yrs),max(all.yrs),length(all.yrs)-1),xlab="Year",ylab="Average Catch",pch=16,type="b",col="#A40122",bty="l")

dens <- hist(species.data$Month,breaks=seq(from=0.5,to=12.5,by=1),plot=FALSE)
      
barplot(dens$density,space = 0,border=NA,col="#A40122",ylab="Density")
axis(side = 1,at = seq(from=0.5,to=11.5,length.out=12),labels = month.abb)
title(xlab="Month")
box(bty="l")
```

## Early Thorn

```{r,echo=FALSE,results=TRUE}
species.data <- moth.data[which(moth.data$Species=="Early Thorn"),]
total.caught <- sum(species.data$NumberCaught,na.rm=TRUE)
n.traps <- length(unique(species.data$TrapNumber))
first.year <- min(species.data$Year,na.rm=TRUE)
last.year <- max(species.data$Year,na.rm=TRUE)

species.data$Year <- factor(x = species.data$Year,levels = all.yrs)
```
In total, I have caught `r total.caught` individuals of Early Thorn, in `r n.traps` traps, between `r first.year` and `r last.year`.

```{r,echo=FALSE,results=TRUE}
yr.avg.catches <- tapply(X = species.data$NumberCaught,INDEX = species.data$Year,FUN = mean)
yr.avg.catches[is.na(yr.avg.catches)] <- 0

par(mar=c(3.2,3.2,0.5,0.2))
par(mgp=c(2.0,0.2,0))
par(tck=-0.01)
par(las=1)

plot(all.yrs,yr.avg.catches,xaxp=c(min(all.yrs),max(all.yrs),length(all.yrs)-1),xlab="Year",ylab="Average Catch",pch=16,type="b",col="#A40122",bty="l")

dens <- hist(species.data$Month,breaks=seq(from=0.5,to=12.5,by=1),plot=FALSE)
      
barplot(dens$density,space = 0,border=NA,col="#A40122",ylab="Density")
axis(side = 1,at = seq(from=0.5,to=11.5,length.out=12),labels = month.abb)
title(xlab="Month")
box(bty="l")
```

## Elephant hawkmoth

```{r,echo=FALSE,results=TRUE}
species.data <- moth.data[which(moth.data$Species=="Elephant hawkmoth"),]
total.caught <- sum(species.data$NumberCaught,na.rm=TRUE)
n.traps <- length(unique(species.data$TrapNumber))
first.year <- min(species.data$Year,na.rm=TRUE)
last.year <- max(species.data$Year,na.rm=TRUE)

species.data$Year <- factor(x = species.data$Year,levels = all.yrs)
```
In total, I have caught `r total.caught` individuals of Elephant hawkmoth, in `r n.traps` traps, between `r first.year` and `r last.year`.

```{r,echo=FALSE,results=TRUE}
yr.avg.catches <- tapply(X = species.data$NumberCaught,INDEX = species.data$Year,FUN = mean)
yr.avg.catches[is.na(yr.avg.catches)] <- 0

par(mar=c(3.2,3.2,0.5,0.2))
par(mgp=c(2.0,0.2,0))
par(tck=-0.01)
par(las=1)

plot(all.yrs,yr.avg.catches,xaxp=c(min(all.yrs),max(all.yrs),length(all.yrs)-1),xlab="Year",ylab="Average Catch",pch=16,type="b",col="#A40122",bty="l")

dens <- hist(species.data$Month,breaks=seq(from=0.5,to=12.5,by=1),plot=FALSE)
      
barplot(dens$density,space = 0,border=NA,col="#A40122",ylab="Density")
axis(side = 1,at = seq(from=0.5,to=11.5,length.out=12),labels = month.abb)
title(xlab="Month")
box(bty="l")
```

## Eyed hawkmoth

```{r,echo=FALSE,results=TRUE}
species.data <- moth.data[which(moth.data$Species=="Eyed hawkmoth"),]
total.caught <- sum(species.data$NumberCaught,na.rm=TRUE)
n.traps <- length(unique(species.data$TrapNumber))
first.year <- min(species.data$Year,na.rm=TRUE)
last.year <- max(species.data$Year,na.rm=TRUE)

species.data$Year <- factor(x = species.data$Year,levels = all.yrs)
```
In total, I have caught `r total.caught` individuals of Eyed hawkmoth, in `r n.traps` traps, between `r first.year` and `r last.year`.

```{r,echo=FALSE,results=TRUE}
yr.avg.catches <- tapply(X = species.data$NumberCaught,INDEX = species.data$Year,FUN = mean)
yr.avg.catches[is.na(yr.avg.catches)] <- 0

par(mar=c(3.2,3.2,0.5,0.2))
par(mgp=c(2.0,0.2,0))
par(tck=-0.01)
par(las=1)

plot(all.yrs,yr.avg.catches,xaxp=c(min(all.yrs),max(all.yrs),length(all.yrs)-1),xlab="Year",ylab="Average Catch",pch=16,type="b",col="#A40122",bty="l")

dens <- hist(species.data$Month,breaks=seq(from=0.5,to=12.5,by=1),plot=FALSE)
      
barplot(dens$density,space = 0,border=NA,col="#A40122",ylab="Density")
axis(side = 1,at = seq(from=0.5,to=11.5,length.out=12),labels = month.abb)
title(xlab="Month")
box(bty="l")
```

## Fan-foot

```{r,echo=FALSE,results=TRUE}
species.data <- moth.data[which(moth.data$Species=="Fan-foot"),]
total.caught <- sum(species.data$NumberCaught,na.rm=TRUE)
n.traps <- length(unique(species.data$TrapNumber))
first.year <- min(species.data$Year,na.rm=TRUE)
last.year <- max(species.data$Year,na.rm=TRUE)

species.data$Year <- factor(x = species.data$Year,levels = all.yrs)
```
In total, I have caught `r total.caught` individuals of Fan-foot, in `r n.traps` traps, between `r first.year` and `r last.year`.

```{r,echo=FALSE,results=TRUE}
yr.avg.catches <- tapply(X = species.data$NumberCaught,INDEX = species.data$Year,FUN = mean)
yr.avg.catches[is.na(yr.avg.catches)] <- 0

par(mar=c(3.2,3.2,0.5,0.2))
par(mgp=c(2.0,0.2,0))
par(tck=-0.01)
par(las=1)

plot(all.yrs,yr.avg.catches,xaxp=c(min(all.yrs),max(all.yrs),length(all.yrs)-1),xlab="Year",ylab="Average Catch",pch=16,type="b",col="#A40122",bty="l")

dens <- hist(species.data$Month,breaks=seq(from=0.5,to=12.5,by=1),plot=FALSE)
      
barplot(dens$density,space = 0,border=NA,col="#A40122",ylab="Density")
axis(side = 1,at = seq(from=0.5,to=11.5,length.out=12),labels = month.abb)
title(xlab="Month")
box(bty="l")
```

## Feathered thorn

```{r,echo=FALSE,results=TRUE}
species.data <- moth.data[which(moth.data$Species=="Feathered thorn"),]
total.caught <- sum(species.data$NumberCaught,na.rm=TRUE)
n.traps <- length(unique(species.data$TrapNumber))
first.year <- min(species.data$Year,na.rm=TRUE)
last.year <- max(species.data$Year,na.rm=TRUE)

species.data$Year <- factor(x = species.data$Year,levels = all.yrs)
```
In total, I have caught `r total.caught` individuals of Feathered thorn, in `r n.traps` traps, between `r first.year` and `r last.year`.

```{r,echo=FALSE,results=TRUE}
yr.avg.catches <- tapply(X = species.data$NumberCaught,INDEX = species.data$Year,FUN = mean)
yr.avg.catches[is.na(yr.avg.catches)] <- 0

par(mar=c(3.2,3.2,0.5,0.2))
par(mgp=c(2.0,0.2,0))
par(tck=-0.01)
par(las=1)

plot(all.yrs,yr.avg.catches,xaxp=c(min(all.yrs),max(all.yrs),length(all.yrs)-1),xlab="Year",ylab="Average Catch",pch=16,type="b",col="#A40122",bty="l")

dens <- hist(species.data$Month,breaks=seq(from=0.5,to=12.5,by=1),plot=FALSE)
      
barplot(dens$density,space = 0,border=NA,col="#A40122",ylab="Density")
axis(side = 1,at = seq(from=0.5,to=11.5,length.out=12),labels = month.abb)
title(xlab="Month")
box(bty="l")
```

## Figure of eighty

```{r,echo=FALSE,results=TRUE}
species.data <- moth.data[which(moth.data$Species=="Figure of eighty"),]
total.caught <- sum(species.data$NumberCaught,na.rm=TRUE)
n.traps <- length(unique(species.data$TrapNumber))
first.year <- min(species.data$Year,na.rm=TRUE)
last.year <- max(species.data$Year,na.rm=TRUE)

species.data$Year <- factor(x = species.data$Year,levels = all.yrs)
```
In total, I have caught `r total.caught` individuals of Figure of eighty, in `r n.traps` traps, between `r first.year` and `r last.year`.

```{r,echo=FALSE,results=TRUE}
yr.avg.catches <- tapply(X = species.data$NumberCaught,INDEX = species.data$Year,FUN = mean)
yr.avg.catches[is.na(yr.avg.catches)] <- 0

par(mar=c(3.2,3.2,0.5,0.2))
par(mgp=c(2.0,0.2,0))
par(tck=-0.01)
par(las=1)

plot(all.yrs,yr.avg.catches,xaxp=c(min(all.yrs),max(all.yrs),length(all.yrs)-1),xlab="Year",ylab="Average Catch",pch=16,type="b",col="#A40122",bty="l")

dens <- hist(species.data$Month,breaks=seq(from=0.5,to=12.5,by=1),plot=FALSE)
      
barplot(dens$density,space = 0,border=NA,col="#A40122",ylab="Density")
axis(side = 1,at = seq(from=0.5,to=11.5,length.out=12),labels = month.abb)
title(xlab="Month")
box(bty="l")
```

## Flame

```{r,echo=FALSE,results=TRUE}
species.data <- moth.data[which(moth.data$Species=="Flame"),]
total.caught <- sum(species.data$NumberCaught,na.rm=TRUE)
n.traps <- length(unique(species.data$TrapNumber))
first.year <- min(species.data$Year,na.rm=TRUE)
last.year <- max(species.data$Year,na.rm=TRUE)

species.data$Year <- factor(x = species.data$Year,levels = all.yrs)
```
In total, I have caught `r total.caught` individuals of Flame, in `r n.traps` traps, between `r first.year` and `r last.year`.

```{r,echo=FALSE,results=TRUE}
yr.avg.catches <- tapply(X = species.data$NumberCaught,INDEX = species.data$Year,FUN = mean)
yr.avg.catches[is.na(yr.avg.catches)] <- 0

par(mar=c(3.2,3.2,0.5,0.2))
par(mgp=c(2.0,0.2,0))
par(tck=-0.01)
par(las=1)

plot(all.yrs,yr.avg.catches,xaxp=c(min(all.yrs),max(all.yrs),length(all.yrs)-1),xlab="Year",ylab="Average Catch",pch=16,type="b",col="#A40122",bty="l")

dens <- hist(species.data$Month,breaks=seq(from=0.5,to=12.5,by=1),plot=FALSE)
      
barplot(dens$density,space = 0,border=NA,col="#A40122",ylab="Density")
axis(side = 1,at = seq(from=0.5,to=11.5,length.out=12),labels = month.abb)
title(xlab="Month")
box(bty="l")
```

## Flame shoulder

```{r,echo=FALSE,results=TRUE}
species.data <- moth.data[which(moth.data$Species=="Flame shoulder"),]
total.caught <- sum(species.data$NumberCaught,na.rm=TRUE)
n.traps <- length(unique(species.data$TrapNumber))
first.year <- min(species.data$Year,na.rm=TRUE)
last.year <- max(species.data$Year,na.rm=TRUE)

species.data$Year <- factor(x = species.data$Year,levels = all.yrs)
```
In total, I have caught `r total.caught` individuals of Flame shoulder, in `r n.traps` traps, between `r first.year` and `r last.year`.

```{r,echo=FALSE,results=TRUE}
yr.avg.catches <- tapply(X = species.data$NumberCaught,INDEX = species.data$Year,FUN = mean)
yr.avg.catches[is.na(yr.avg.catches)] <- 0

par(mar=c(3.2,3.2,0.5,0.2))
par(mgp=c(2.0,0.2,0))
par(tck=-0.01)
par(las=1)

plot(all.yrs,yr.avg.catches,xaxp=c(min(all.yrs),max(all.yrs),length(all.yrs)-1),xlab="Year",ylab="Average Catch",pch=16,type="b",col="#A40122",bty="l")

dens <- hist(species.data$Month,breaks=seq(from=0.5,to=12.5,by=1),plot=FALSE)
      
barplot(dens$density,space = 0,border=NA,col="#A40122",ylab="Density")
axis(side = 1,at = seq(from=0.5,to=11.5,length.out=12),labels = month.abb)
title(xlab="Month")
box(bty="l")
```

## Flounced rustic

```{r,echo=FALSE,results=TRUE}
species.data <- moth.data[which(moth.data$Species=="Flounced rustic"),]
total.caught <- sum(species.data$NumberCaught,na.rm=TRUE)
n.traps <- length(unique(species.data$TrapNumber))
first.year <- min(species.data$Year,na.rm=TRUE)
last.year <- max(species.data$Year,na.rm=TRUE)

species.data$Year <- factor(x = species.data$Year,levels = all.yrs)
```
In total, I have caught `r total.caught` individuals of Flounced rustic, in `r n.traps` traps, between `r first.year` and `r last.year`.

```{r,echo=FALSE,results=TRUE}
yr.avg.catches <- tapply(X = species.data$NumberCaught,INDEX = species.data$Year,FUN = mean)
yr.avg.catches[is.na(yr.avg.catches)] <- 0

par(mar=c(3.2,3.2,0.5,0.2))
par(mgp=c(2.0,0.2,0))
par(tck=-0.01)
par(las=1)

plot(all.yrs,yr.avg.catches,xaxp=c(min(all.yrs),max(all.yrs),length(all.yrs)-1),xlab="Year",ylab="Average Catch",pch=16,type="b",col="#A40122",bty="l")

dens <- hist(species.data$Month,breaks=seq(from=0.5,to=12.5,by=1),plot=FALSE)
      
barplot(dens$density,space = 0,border=NA,col="#A40122",ylab="Density")
axis(side = 1,at = seq(from=0.5,to=11.5,length.out=12),labels = month.abb)
title(xlab="Month")
box(bty="l")
```

## Four-dotted footman

```{r,echo=FALSE,results=TRUE}
species.data <- moth.data[which(moth.data$Species=="Four-dotted footman"),]
total.caught <- sum(species.data$NumberCaught,na.rm=TRUE)
n.traps <- length(unique(species.data$TrapNumber))
first.year <- min(species.data$Year,na.rm=TRUE)
last.year <- max(species.data$Year,na.rm=TRUE)

species.data$Year <- factor(x = species.data$Year,levels = all.yrs)
```
In total, I have caught `r total.caught` individuals of Four-dotted footman, in `r n.traps` traps, between `r first.year` and `r last.year`.

```{r,echo=FALSE,results=TRUE}
yr.avg.catches <- tapply(X = species.data$NumberCaught,INDEX = species.data$Year,FUN = mean)
yr.avg.catches[is.na(yr.avg.catches)] <- 0

par(mar=c(3.2,3.2,0.5,0.2))
par(mgp=c(2.0,0.2,0))
par(tck=-0.01)
par(las=1)

plot(all.yrs,yr.avg.catches,xaxp=c(min(all.yrs),max(all.yrs),length(all.yrs)-1),xlab="Year",ylab="Average Catch",pch=16,type="b",col="#A40122",bty="l")

dens <- hist(species.data$Month,breaks=seq(from=0.5,to=12.5,by=1),plot=FALSE)
      
barplot(dens$density,space = 0,border=NA,col="#A40122",ylab="Density")
axis(side = 1,at = seq(from=0.5,to=11.5,length.out=12),labels = month.abb)
title(xlab="Month")
box(bty="l")
```

## Fox moth

```{r,echo=FALSE,results=TRUE}
species.data <- moth.data[which(moth.data$Species=="Fox moth"),]
total.caught <- sum(species.data$NumberCaught,na.rm=TRUE)
n.traps <- length(unique(species.data$TrapNumber))
first.year <- min(species.data$Year,na.rm=TRUE)
last.year <- max(species.data$Year,na.rm=TRUE)

species.data$Year <- factor(x = species.data$Year,levels = all.yrs)
```
In total, I have caught `r total.caught` individuals of Fox moth, in `r n.traps` traps, between `r first.year` and `r last.year`.

```{r,echo=FALSE,results=TRUE}
yr.avg.catches <- tapply(X = species.data$NumberCaught,INDEX = species.data$Year,FUN = mean)
yr.avg.catches[is.na(yr.avg.catches)] <- 0

par(mar=c(3.2,3.2,0.5,0.2))
par(mgp=c(2.0,0.2,0))
par(tck=-0.01)
par(las=1)

plot(all.yrs,yr.avg.catches,xaxp=c(min(all.yrs),max(all.yrs),length(all.yrs)-1),xlab="Year",ylab="Average Catch",pch=16,type="b",col="#A40122",bty="l")

dens <- hist(species.data$Month,breaks=seq(from=0.5,to=12.5,by=1),plot=FALSE)
      
barplot(dens$density,space = 0,border=NA,col="#A40122",ylab="Density")
axis(side = 1,at = seq(from=0.5,to=11.5,length.out=12),labels = month.abb)
title(xlab="Month")
box(bty="l")
```

## Foxglove pug

```{r,echo=FALSE,results=TRUE}
species.data <- moth.data[which(moth.data$Species=="Foxglove pug"),]
total.caught <- sum(species.data$NumberCaught,na.rm=TRUE)
n.traps <- length(unique(species.data$TrapNumber))
first.year <- min(species.data$Year,na.rm=TRUE)
last.year <- max(species.data$Year,na.rm=TRUE)

species.data$Year <- factor(x = species.data$Year,levels = all.yrs)
```
In total, I have caught `r total.caught` individuals of Foxglove pug, in `r n.traps` traps, between `r first.year` and `r last.year`.

```{r,echo=FALSE,results=TRUE}
yr.avg.catches <- tapply(X = species.data$NumberCaught,INDEX = species.data$Year,FUN = mean)
yr.avg.catches[is.na(yr.avg.catches)] <- 0

par(mar=c(3.2,3.2,0.5,0.2))
par(mgp=c(2.0,0.2,0))
par(tck=-0.01)
par(las=1)

plot(all.yrs,yr.avg.catches,xaxp=c(min(all.yrs),max(all.yrs),length(all.yrs)-1),xlab="Year",ylab="Average Catch",pch=16,type="b",col="#A40122",bty="l")

dens <- hist(species.data$Month,breaks=seq(from=0.5,to=12.5,by=1),plot=FALSE)
      
barplot(dens$density,space = 0,border=NA,col="#A40122",ylab="Density")
axis(side = 1,at = seq(from=0.5,to=11.5,length.out=12),labels = month.abb)
title(xlab="Month")
box(bty="l")
```

## Frosted orange

```{r,echo=FALSE,results=TRUE}
species.data <- moth.data[which(moth.data$Species=="Frosted orange"),]
total.caught <- sum(species.data$NumberCaught,na.rm=TRUE)
n.traps <- length(unique(species.data$TrapNumber))
first.year <- min(species.data$Year,na.rm=TRUE)
last.year <- max(species.data$Year,na.rm=TRUE)

species.data$Year <- factor(x = species.data$Year,levels = all.yrs)
```
In total, I have caught `r total.caught` individuals of Frosted orange, in `r n.traps` traps, between `r first.year` and `r last.year`.

```{r,echo=FALSE,results=TRUE}
yr.avg.catches <- tapply(X = species.data$NumberCaught,INDEX = species.data$Year,FUN = mean)
yr.avg.catches[is.na(yr.avg.catches)] <- 0

par(mar=c(3.2,3.2,0.5,0.2))
par(mgp=c(2.0,0.2,0))
par(tck=-0.01)
par(las=1)

plot(all.yrs,yr.avg.catches,xaxp=c(min(all.yrs),max(all.yrs),length(all.yrs)-1),xlab="Year",ylab="Average Catch",pch=16,type="b",col="#A40122",bty="l")

dens <- hist(species.data$Month,breaks=seq(from=0.5,to=12.5,by=1),plot=FALSE)
      
barplot(dens$density,space = 0,border=NA,col="#A40122",ylab="Density")
axis(side = 1,at = seq(from=0.5,to=11.5,length.out=12),labels = month.abb)
title(xlab="Month")
box(bty="l")
```

## Garden carpet

```{r,echo=FALSE,results=TRUE}
species.data <- moth.data[which(moth.data$Species=="Garden carpet"),]
total.caught <- sum(species.data$NumberCaught,na.rm=TRUE)
n.traps <- length(unique(species.data$TrapNumber))
first.year <- min(species.data$Year,na.rm=TRUE)
last.year <- max(species.data$Year,na.rm=TRUE)

species.data$Year <- factor(x = species.data$Year,levels = all.yrs)
```
In total, I have caught `r total.caught` individuals of Garden carpet, in `r n.traps` traps, between `r first.year` and `r last.year`.

```{r,echo=FALSE,results=TRUE}
yr.avg.catches <- tapply(X = species.data$NumberCaught,INDEX = species.data$Year,FUN = mean)
yr.avg.catches[is.na(yr.avg.catches)] <- 0

par(mar=c(3.2,3.2,0.5,0.2))
par(mgp=c(2.0,0.2,0))
par(tck=-0.01)
par(las=1)

plot(all.yrs,yr.avg.catches,xaxp=c(min(all.yrs),max(all.yrs),length(all.yrs)-1),xlab="Year",ylab="Average Catch",pch=16,type="b",col="#A40122",bty="l")

dens <- hist(species.data$Month,breaks=seq(from=0.5,to=12.5,by=1),plot=FALSE)
      
barplot(dens$density,space = 0,border=NA,col="#A40122",ylab="Density")
axis(side = 1,at = seq(from=0.5,to=11.5,length.out=12),labels = month.abb)
title(xlab="Month")
box(bty="l")
```

## Garden dart

```{r,echo=FALSE,results=TRUE}
species.data <- moth.data[which(moth.data$Species=="Garden dart"),]
total.caught <- sum(species.data$NumberCaught,na.rm=TRUE)
n.traps <- length(unique(species.data$TrapNumber))
first.year <- min(species.data$Year,na.rm=TRUE)
last.year <- max(species.data$Year,na.rm=TRUE)

species.data$Year <- factor(x = species.data$Year,levels = all.yrs)
```
In total, I have caught `r total.caught` individuals of Garden dart, in `r n.traps` traps, between `r first.year` and `r last.year`.

```{r,echo=FALSE,results=TRUE}
yr.avg.catches <- tapply(X = species.data$NumberCaught,INDEX = species.data$Year,FUN = mean)
yr.avg.catches[is.na(yr.avg.catches)] <- 0

par(mar=c(3.2,3.2,0.5,0.2))
par(mgp=c(2.0,0.2,0))
par(tck=-0.01)
par(las=1)

plot(all.yrs,yr.avg.catches,xaxp=c(min(all.yrs),max(all.yrs),length(all.yrs)-1),xlab="Year",ylab="Average Catch",pch=16,type="b",col="#A40122",bty="l")

dens <- hist(species.data$Month,breaks=seq(from=0.5,to=12.5,by=1),plot=FALSE)
      
barplot(dens$density,space = 0,border=NA,col="#A40122",ylab="Density")
axis(side = 1,at = seq(from=0.5,to=11.5,length.out=12),labels = month.abb)
title(xlab="Month")
box(bty="l")
```

## Green-brindled crescent

```{r,echo=FALSE,results=TRUE}
species.data <- moth.data[which(moth.data$Species=="Green-brindled crescent"),]
total.caught <- sum(species.data$NumberCaught,na.rm=TRUE)
n.traps <- length(unique(species.data$TrapNumber))
first.year <- min(species.data$Year,na.rm=TRUE)
last.year <- max(species.data$Year,na.rm=TRUE)

species.data$Year <- factor(x = species.data$Year,levels = all.yrs)
```
In total, I have caught `r total.caught` individuals of Green-brindled crescent, in `r n.traps` traps, between `r first.year` and `r last.year`.

```{r,echo=FALSE,results=TRUE}
yr.avg.catches <- tapply(X = species.data$NumberCaught,INDEX = species.data$Year,FUN = mean)
yr.avg.catches[is.na(yr.avg.catches)] <- 0

par(mar=c(3.2,3.2,0.5,0.2))
par(mgp=c(2.0,0.2,0))
par(tck=-0.01)
par(las=1)

plot(all.yrs,yr.avg.catches,xaxp=c(min(all.yrs),max(all.yrs),length(all.yrs)-1),xlab="Year",ylab="Average Catch",pch=16,type="b",col="#A40122",bty="l")

dens <- hist(species.data$Month,breaks=seq(from=0.5,to=12.5,by=1),plot=FALSE)
      
barplot(dens$density,space = 0,border=NA,col="#A40122",ylab="Density")
axis(side = 1,at = seq(from=0.5,to=11.5,length.out=12),labels = month.abb)
title(xlab="Month")
box(bty="l")
```

## Green carpet

```{r,echo=FALSE,results=TRUE}
species.data <- moth.data[which(moth.data$Species=="Green carpet"),]
total.caught <- sum(species.data$NumberCaught,na.rm=TRUE)
n.traps <- length(unique(species.data$TrapNumber))
first.year <- min(species.data$Year,na.rm=TRUE)
last.year <- max(species.data$Year,na.rm=TRUE)

species.data$Year <- factor(x = species.data$Year,levels = all.yrs)
```
In total, I have caught `r total.caught` individuals of Green carpet, in `r n.traps` traps, between `r first.year` and `r last.year`.

```{r,echo=FALSE,results=TRUE}
yr.avg.catches <- tapply(X = species.data$NumberCaught,INDEX = species.data$Year,FUN = mean)
yr.avg.catches[is.na(yr.avg.catches)] <- 0

par(mar=c(3.2,3.2,0.5,0.2))
par(mgp=c(2.0,0.2,0))
par(tck=-0.01)
par(las=1)

plot(all.yrs,yr.avg.catches,xaxp=c(min(all.yrs),max(all.yrs),length(all.yrs)-1),xlab="Year",ylab="Average Catch",pch=16,type="b",col="#A40122",bty="l")

dens <- hist(species.data$Month,breaks=seq(from=0.5,to=12.5,by=1),plot=FALSE)
      
barplot(dens$density,space = 0,border=NA,col="#A40122",ylab="Density")
axis(side = 1,at = seq(from=0.5,to=11.5,length.out=12),labels = month.abb)
title(xlab="Month")
box(bty="l")
```

## Green pug

```{r,echo=FALSE,results=TRUE}
species.data <- moth.data[which(moth.data$Species=="Green pug"),]
total.caught <- sum(species.data$NumberCaught,na.rm=TRUE)
n.traps <- length(unique(species.data$TrapNumber))
first.year <- min(species.data$Year,na.rm=TRUE)
last.year <- max(species.data$Year,na.rm=TRUE)

species.data$Year <- factor(x = species.data$Year,levels = all.yrs)
```
In total, I have caught `r total.caught` individuals of Green pug, in `r n.traps` traps, between `r first.year` and `r last.year`.

```{r,echo=FALSE,results=TRUE}
yr.avg.catches <- tapply(X = species.data$NumberCaught,INDEX = species.data$Year,FUN = mean)
yr.avg.catches[is.na(yr.avg.catches)] <- 0

par(mar=c(3.2,3.2,0.5,0.2))
par(mgp=c(2.0,0.2,0))
par(tck=-0.01)
par(las=1)

plot(all.yrs,yr.avg.catches,xaxp=c(min(all.yrs),max(all.yrs),length(all.yrs)-1),xlab="Year",ylab="Average Catch",pch=16,type="b",col="#A40122",bty="l")

dens <- hist(species.data$Month,breaks=seq(from=0.5,to=12.5,by=1),plot=FALSE)
      
barplot(dens$density,space = 0,border=NA,col="#A40122",ylab="Density")
axis(side = 1,at = seq(from=0.5,to=11.5,length.out=12),labels = month.abb)
title(xlab="Month")
box(bty="l")
```

## Heart and club

```{r,echo=FALSE,results=TRUE}
species.data <- moth.data[which(moth.data$Species=="Heart and club"),]
total.caught <- sum(species.data$NumberCaught,na.rm=TRUE)
n.traps <- length(unique(species.data$TrapNumber))
first.year <- min(species.data$Year,na.rm=TRUE)
last.year <- max(species.data$Year,na.rm=TRUE)

species.data$Year <- factor(x = species.data$Year,levels = all.yrs)
```
In total, I have caught `r total.caught` individuals of Heart and club, in `r n.traps` traps, between `r first.year` and `r last.year`.

```{r,echo=FALSE,results=TRUE}
yr.avg.catches <- tapply(X = species.data$NumberCaught,INDEX = species.data$Year,FUN = mean)
yr.avg.catches[is.na(yr.avg.catches)] <- 0

par(mar=c(3.2,3.2,0.5,0.2))
par(mgp=c(2.0,0.2,0))
par(tck=-0.01)
par(las=1)

plot(all.yrs,yr.avg.catches,xaxp=c(min(all.yrs),max(all.yrs),length(all.yrs)-1),xlab="Year",ylab="Average Catch",pch=16,type="b",col="#A40122",bty="l")

dens <- hist(species.data$Month,breaks=seq(from=0.5,to=12.5,by=1),plot=FALSE)
      
barplot(dens$density,space = 0,border=NA,col="#A40122",ylab="Density")
axis(side = 1,at = seq(from=0.5,to=11.5,length.out=12),labels = month.abb)
title(xlab="Month")
box(bty="l")
```

## Heart and dart

```{r,echo=FALSE,results=TRUE}
species.data <- moth.data[which(moth.data$Species=="Heart and dart"),]
total.caught <- sum(species.data$NumberCaught,na.rm=TRUE)
n.traps <- length(unique(species.data$TrapNumber))
first.year <- min(species.data$Year,na.rm=TRUE)
last.year <- max(species.data$Year,na.rm=TRUE)

species.data$Year <- factor(x = species.data$Year,levels = all.yrs)
```
In total, I have caught `r total.caught` individuals of Heart and dart, in `r n.traps` traps, between `r first.year` and `r last.year`.

```{r,echo=FALSE,results=TRUE}
yr.avg.catches <- tapply(X = species.data$NumberCaught,INDEX = species.data$Year,FUN = mean)
yr.avg.catches[is.na(yr.avg.catches)] <- 0

par(mar=c(3.2,3.2,0.5,0.2))
par(mgp=c(2.0,0.2,0))
par(tck=-0.01)
par(las=1)

plot(all.yrs,yr.avg.catches,xaxp=c(min(all.yrs),max(all.yrs),length(all.yrs)-1),xlab="Year",ylab="Average Catch",pch=16,type="b",col="#A40122",bty="l")

dens <- hist(species.data$Month,breaks=seq(from=0.5,to=12.5,by=1),plot=FALSE)
      
barplot(dens$density,space = 0,border=NA,col="#A40122",ylab="Density")
axis(side = 1,at = seq(from=0.5,to=11.5,length.out=12),labels = month.abb)
title(xlab="Month")
box(bty="l")
```

## Hebrew character

```{r,echo=FALSE,results=TRUE}
species.data <- moth.data[which(moth.data$Species=="Hebrew character"),]
total.caught <- sum(species.data$NumberCaught,na.rm=TRUE)
n.traps <- length(unique(species.data$TrapNumber))
first.year <- min(species.data$Year,na.rm=TRUE)
last.year <- max(species.data$Year,na.rm=TRUE)

species.data$Year <- factor(x = species.data$Year,levels = all.yrs)
```
In total, I have caught `r total.caught` individuals of Hebrew character, in `r n.traps` traps, between `r first.year` and `r last.year`.

```{r,echo=FALSE,results=TRUE}
yr.avg.catches <- tapply(X = species.data$NumberCaught,INDEX = species.data$Year,FUN = mean)
yr.avg.catches[is.na(yr.avg.catches)] <- 0

par(mar=c(3.2,3.2,0.5,0.2))
par(mgp=c(2.0,0.2,0))
par(tck=-0.01)
par(las=1)

plot(all.yrs,yr.avg.catches,xaxp=c(min(all.yrs),max(all.yrs),length(all.yrs)-1),xlab="Year",ylab="Average Catch",pch=16,type="b",col="#A40122",bty="l")

dens <- hist(species.data$Month,breaks=seq(from=0.5,to=12.5,by=1),plot=FALSE)
      
barplot(dens$density,space = 0,border=NA,col="#A40122",ylab="Density")
axis(side = 1,at = seq(from=0.5,to=11.5,length.out=12),labels = month.abb)
title(xlab="Month")
box(bty="l")
```

## Hornet moth

```{r,echo=FALSE,results=TRUE}
species.data <- moth.data[which(moth.data$Species=="Hornet moth"),]
total.caught <- sum(species.data$NumberCaught,na.rm=TRUE)
n.traps <- length(unique(species.data$TrapNumber))
first.year <- min(species.data$Year,na.rm=TRUE)
last.year <- max(species.data$Year,na.rm=TRUE)

species.data$Year <- factor(x = species.data$Year,levels = all.yrs)
```
In total, I have caught `r total.caught` individuals of Hornet moth, in `r n.traps` traps, between `r first.year` and `r last.year`.

```{r,echo=FALSE,results=TRUE}
yr.avg.catches <- tapply(X = species.data$NumberCaught,INDEX = species.data$Year,FUN = mean)
yr.avg.catches[is.na(yr.avg.catches)] <- 0

par(mar=c(3.2,3.2,0.5,0.2))
par(mgp=c(2.0,0.2,0))
par(tck=-0.01)
par(las=1)

plot(all.yrs,yr.avg.catches,xaxp=c(min(all.yrs),max(all.yrs),length(all.yrs)-1),xlab="Year",ylab="Average Catch",pch=16,type="b",col="#A40122",bty="l")

dens <- hist(species.data$Month,breaks=seq(from=0.5,to=12.5,by=1),plot=FALSE)
      
barplot(dens$density,space = 0,border=NA,col="#A40122",ylab="Density")
axis(side = 1,at = seq(from=0.5,to=11.5,length.out=12),labels = month.abb)
title(xlab="Month")
box(bty="l")
```

## Ingrailed clay

```{r,echo=FALSE,results=TRUE}
species.data <- moth.data[which(moth.data$Species=="Ingrailed clay"),]
total.caught <- sum(species.data$NumberCaught,na.rm=TRUE)
n.traps <- length(unique(species.data$TrapNumber))
first.year <- min(species.data$Year,na.rm=TRUE)
last.year <- max(species.data$Year,na.rm=TRUE)

species.data$Year <- factor(x = species.data$Year,levels = all.yrs)
```
In total, I have caught `r total.caught` individuals of Ingrailed clay, in `r n.traps` traps, between `r first.year` and `r last.year`.

```{r,echo=FALSE,results=TRUE}
yr.avg.catches <- tapply(X = species.data$NumberCaught,INDEX = species.data$Year,FUN = mean)
yr.avg.catches[is.na(yr.avg.catches)] <- 0

par(mar=c(3.2,3.2,0.5,0.2))
par(mgp=c(2.0,0.2,0))
par(tck=-0.01)
par(las=1)

plot(all.yrs,yr.avg.catches,xaxp=c(min(all.yrs),max(all.yrs),length(all.yrs)-1),xlab="Year",ylab="Average Catch",pch=16,type="b",col="#A40122",bty="l")

dens <- hist(species.data$Month,breaks=seq(from=0.5,to=12.5,by=1),plot=FALSE)
      
barplot(dens$density,space = 0,border=NA,col="#A40122",ylab="Density")
axis(side = 1,at = seq(from=0.5,to=11.5,length.out=12),labels = month.abb)
title(xlab="Month")
box(bty="l")
```

## Knot-grass

```{r,echo=FALSE,results=TRUE}
species.data <- moth.data[which(moth.data$Species=="Knot-grass"),]
total.caught <- sum(species.data$NumberCaught,na.rm=TRUE)
n.traps <- length(unique(species.data$TrapNumber))
first.year <- min(species.data$Year,na.rm=TRUE)
last.year <- max(species.data$Year,na.rm=TRUE)

species.data$Year <- factor(x = species.data$Year,levels = all.yrs)
```
In total, I have caught `r total.caught` individuals of Knot-grass, in `r n.traps` traps, between `r first.year` and `r last.year`.

```{r,echo=FALSE,results=TRUE}
yr.avg.catches <- tapply(X = species.data$NumberCaught,INDEX = species.data$Year,FUN = mean)
yr.avg.catches[is.na(yr.avg.catches)] <- 0

par(mar=c(3.2,3.2,0.5,0.2))
par(mgp=c(2.0,0.2,0))
par(tck=-0.01)
par(las=1)

plot(all.yrs,yr.avg.catches,xaxp=c(min(all.yrs),max(all.yrs),length(all.yrs)-1),xlab="Year",ylab="Average Catch",pch=16,type="b",col="#A40122",bty="l")

dens <- hist(species.data$Month,breaks=seq(from=0.5,to=12.5,by=1),plot=FALSE)
      
barplot(dens$density,space = 0,border=NA,col="#A40122",ylab="Density")
axis(side = 1,at = seq(from=0.5,to=11.5,length.out=12),labels = month.abb)
title(xlab="Month")
box(bty="l")
```

## Lackey

```{r,echo=FALSE,results=TRUE}
species.data <- moth.data[which(moth.data$Species=="Lackey"),]
total.caught <- sum(species.data$NumberCaught,na.rm=TRUE)
n.traps <- length(unique(species.data$TrapNumber))
first.year <- min(species.data$Year,na.rm=TRUE)
last.year <- max(species.data$Year,na.rm=TRUE)

species.data$Year <- factor(x = species.data$Year,levels = all.yrs)
```
In total, I have caught `r total.caught` individuals of Lackey, in `r n.traps` traps, between `r first.year` and `r last.year`.

```{r,echo=FALSE,results=TRUE}
yr.avg.catches <- tapply(X = species.data$NumberCaught,INDEX = species.data$Year,FUN = mean)
yr.avg.catches[is.na(yr.avg.catches)] <- 0

par(mar=c(3.2,3.2,0.5,0.2))
par(mgp=c(2.0,0.2,0))
par(tck=-0.01)
par(las=1)

plot(all.yrs,yr.avg.catches,xaxp=c(min(all.yrs),max(all.yrs),length(all.yrs)-1),xlab="Year",ylab="Average Catch",pch=16,type="b",col="#A40122",bty="l")

dens <- hist(species.data$Month,breaks=seq(from=0.5,to=12.5,by=1),plot=FALSE)
      
barplot(dens$density,space = 0,border=NA,col="#A40122",ylab="Density")
axis(side = 1,at = seq(from=0.5,to=11.5,length.out=12),labels = month.abb)
title(xlab="Month")
box(bty="l")
```

## Large nutmeg

```{r,echo=FALSE,results=TRUE}
species.data <- moth.data[which(moth.data$Species=="Large nutmeg"),]
total.caught <- sum(species.data$NumberCaught,na.rm=TRUE)
n.traps <- length(unique(species.data$TrapNumber))
first.year <- min(species.data$Year,na.rm=TRUE)
last.year <- max(species.data$Year,na.rm=TRUE)

species.data$Year <- factor(x = species.data$Year,levels = all.yrs)
```
In total, I have caught `r total.caught` individuals of Large nutmeg, in `r n.traps` traps, between `r first.year` and `r last.year`.

```{r,echo=FALSE,results=TRUE}
yr.avg.catches <- tapply(X = species.data$NumberCaught,INDEX = species.data$Year,FUN = mean)
yr.avg.catches[is.na(yr.avg.catches)] <- 0

par(mar=c(3.2,3.2,0.5,0.2))
par(mgp=c(2.0,0.2,0))
par(tck=-0.01)
par(las=1)

plot(all.yrs,yr.avg.catches,xaxp=c(min(all.yrs),max(all.yrs),length(all.yrs)-1),xlab="Year",ylab="Average Catch",pch=16,type="b",col="#A40122",bty="l")

dens <- hist(species.data$Month,breaks=seq(from=0.5,to=12.5,by=1),plot=FALSE)
      
barplot(dens$density,space = 0,border=NA,col="#A40122",ylab="Density")
axis(side = 1,at = seq(from=0.5,to=11.5,length.out=12),labels = month.abb)
title(xlab="Month")
box(bty="l")
```

## Large thorn

```{r,echo=FALSE,results=TRUE}
species.data <- moth.data[which(moth.data$Species=="Large thorn"),]
total.caught <- sum(species.data$NumberCaught,na.rm=TRUE)
n.traps <- length(unique(species.data$TrapNumber))
first.year <- min(species.data$Year,na.rm=TRUE)
last.year <- max(species.data$Year,na.rm=TRUE)

species.data$Year <- factor(x = species.data$Year,levels = all.yrs)
```
In total, I have caught `r total.caught` individuals of Large thorn, in `r n.traps` traps, between `r first.year` and `r last.year`.

```{r,echo=FALSE,results=TRUE}
yr.avg.catches <- tapply(X = species.data$NumberCaught,INDEX = species.data$Year,FUN = mean)
yr.avg.catches[is.na(yr.avg.catches)] <- 0

par(mar=c(3.2,3.2,0.5,0.2))
par(mgp=c(2.0,0.2,0))
par(tck=-0.01)
par(las=1)

plot(all.yrs,yr.avg.catches,xaxp=c(min(all.yrs),max(all.yrs),length(all.yrs)-1),xlab="Year",ylab="Average Catch",pch=16,type="b",col="#A40122",bty="l")

dens <- hist(species.data$Month,breaks=seq(from=0.5,to=12.5,by=1),plot=FALSE)
      
barplot(dens$density,space = 0,border=NA,col="#A40122",ylab="Density")
axis(side = 1,at = seq(from=0.5,to=11.5,length.out=12),labels = month.abb)
title(xlab="Month")
box(bty="l")
```

## Large twin-spot carpet

```{r,echo=FALSE,results=TRUE}
species.data <- moth.data[which(moth.data$Species=="Large twin-spot carpet"),]
total.caught <- sum(species.data$NumberCaught,na.rm=TRUE)
n.traps <- length(unique(species.data$TrapNumber))
first.year <- min(species.data$Year,na.rm=TRUE)
last.year <- max(species.data$Year,na.rm=TRUE)

species.data$Year <- factor(x = species.data$Year,levels = all.yrs)
```
In total, I have caught `r total.caught` individuals of Large twin-spot carpet, in `r n.traps` traps, between `r first.year` and `r last.year`.

```{r,echo=FALSE,results=TRUE}
yr.avg.catches <- tapply(X = species.data$NumberCaught,INDEX = species.data$Year,FUN = mean)
yr.avg.catches[is.na(yr.avg.catches)] <- 0

par(mar=c(3.2,3.2,0.5,0.2))
par(mgp=c(2.0,0.2,0))
par(tck=-0.01)
par(las=1)

plot(all.yrs,yr.avg.catches,xaxp=c(min(all.yrs),max(all.yrs),length(all.yrs)-1),xlab="Year",ylab="Average Catch",pch=16,type="b",col="#A40122",bty="l")

dens <- hist(species.data$Month,breaks=seq(from=0.5,to=12.5,by=1),plot=FALSE)
      
barplot(dens$density,space = 0,border=NA,col="#A40122",ylab="Density")
axis(side = 1,at = seq(from=0.5,to=11.5,length.out=12),labels = month.abb)
title(xlab="Month")
box(bty="l")
```

## Large wainscot

```{r,echo=FALSE,results=TRUE}
species.data <- moth.data[which(moth.data$Species=="Large wainscot"),]
total.caught <- sum(species.data$NumberCaught,na.rm=TRUE)
n.traps <- length(unique(species.data$TrapNumber))
first.year <- min(species.data$Year,na.rm=TRUE)
last.year <- max(species.data$Year,na.rm=TRUE)

species.data$Year <- factor(x = species.data$Year,levels = all.yrs)
```
In total, I have caught `r total.caught` individuals of Large wainscot, in `r n.traps` traps, between `r first.year` and `r last.year`.

```{r,echo=FALSE,results=TRUE}
yr.avg.catches <- tapply(X = species.data$NumberCaught,INDEX = species.data$Year,FUN = mean)
yr.avg.catches[is.na(yr.avg.catches)] <- 0

par(mar=c(3.2,3.2,0.5,0.2))
par(mgp=c(2.0,0.2,0))
par(tck=-0.01)
par(las=1)

plot(all.yrs,yr.avg.catches,xaxp=c(min(all.yrs),max(all.yrs),length(all.yrs)-1),xlab="Year",ylab="Average Catch",pch=16,type="b",col="#A40122",bty="l")

dens <- hist(species.data$Month,breaks=seq(from=0.5,to=12.5,by=1),plot=FALSE)
      
barplot(dens$density,space = 0,border=NA,col="#A40122",ylab="Density")
axis(side = 1,at = seq(from=0.5,to=11.5,length.out=12),labels = month.abb)
title(xlab="Month")
box(bty="l")
```

## Large yellow underwing

```{r,echo=FALSE,results=TRUE}
species.data <- moth.data[which(moth.data$Species=="Large yellow underwing"),]
total.caught <- sum(species.data$NumberCaught,na.rm=TRUE)
n.traps <- length(unique(species.data$TrapNumber))
first.year <- min(species.data$Year,na.rm=TRUE)
last.year <- max(species.data$Year,na.rm=TRUE)

species.data$Year <- factor(x = species.data$Year,levels = all.yrs)
```
In total, I have caught `r total.caught` individuals of Large yellow underwing, in `r n.traps` traps, between `r first.year` and `r last.year`.

```{r,echo=FALSE,results=TRUE}
yr.avg.catches <- tapply(X = species.data$NumberCaught,INDEX = species.data$Year,FUN = mean)
yr.avg.catches[is.na(yr.avg.catches)] <- 0

par(mar=c(3.2,3.2,0.5,0.2))
par(mgp=c(2.0,0.2,0))
par(tck=-0.01)
par(las=1)

plot(all.yrs,yr.avg.catches,xaxp=c(min(all.yrs),max(all.yrs),length(all.yrs)-1),xlab="Year",ylab="Average Catch",pch=16,type="b",col="#A40122",bty="l")

dens <- hist(species.data$Month,breaks=seq(from=0.5,to=12.5,by=1),plot=FALSE)
      
barplot(dens$density,space = 0,border=NA,col="#A40122",ylab="Density")
axis(side = 1,at = seq(from=0.5,to=11.5,length.out=12),labels = month.abb)
title(xlab="Month")
box(bty="l")
```

## Least carpet

```{r,echo=FALSE,results=TRUE}
species.data <- moth.data[which(moth.data$Species=="Least carpet"),]
total.caught <- sum(species.data$NumberCaught,na.rm=TRUE)
n.traps <- length(unique(species.data$TrapNumber))
first.year <- min(species.data$Year,na.rm=TRUE)
last.year <- max(species.data$Year,na.rm=TRUE)

species.data$Year <- factor(x = species.data$Year,levels = all.yrs)
```
In total, I have caught `r total.caught` individuals of Least carpet, in `r n.traps` traps, between `r first.year` and `r last.year`.

```{r,echo=FALSE,results=TRUE}
yr.avg.catches <- tapply(X = species.data$NumberCaught,INDEX = species.data$Year,FUN = mean)
yr.avg.catches[is.na(yr.avg.catches)] <- 0

par(mar=c(3.2,3.2,0.5,0.2))
par(mgp=c(2.0,0.2,0))
par(tck=-0.01)
par(las=1)

plot(all.yrs,yr.avg.catches,xaxp=c(min(all.yrs),max(all.yrs),length(all.yrs)-1),xlab="Year",ylab="Average Catch",pch=16,type="b",col="#A40122",bty="l")

dens <- hist(species.data$Month,breaks=seq(from=0.5,to=12.5,by=1),plot=FALSE)
      
barplot(dens$density,space = 0,border=NA,col="#A40122",ylab="Density")
axis(side = 1,at = seq(from=0.5,to=11.5,length.out=12),labels = month.abb)
title(xlab="Month")
box(bty="l")
```

## Least yellow underwing

```{r,echo=FALSE,results=TRUE}
species.data <- moth.data[which(moth.data$Species=="Least yellow underwing"),]
total.caught <- sum(species.data$NumberCaught,na.rm=TRUE)
n.traps <- length(unique(species.data$TrapNumber))
first.year <- min(species.data$Year,na.rm=TRUE)
last.year <- max(species.data$Year,na.rm=TRUE)

species.data$Year <- factor(x = species.data$Year,levels = all.yrs)
```
In total, I have caught `r total.caught` individuals of Least yellow underwing, in `r n.traps` traps, between `r first.year` and `r last.year`.

```{r,echo=FALSE,results=TRUE}
yr.avg.catches <- tapply(X = species.data$NumberCaught,INDEX = species.data$Year,FUN = mean)
yr.avg.catches[is.na(yr.avg.catches)] <- 0

par(mar=c(3.2,3.2,0.5,0.2))
par(mgp=c(2.0,0.2,0))
par(tck=-0.01)
par(las=1)

plot(all.yrs,yr.avg.catches,xaxp=c(min(all.yrs),max(all.yrs),length(all.yrs)-1),xlab="Year",ylab="Average Catch",pch=16,type="b",col="#A40122",bty="l")

dens <- hist(species.data$Month,breaks=seq(from=0.5,to=12.5,by=1),plot=FALSE)
      
barplot(dens$density,space = 0,border=NA,col="#A40122",ylab="Density")
axis(side = 1,at = seq(from=0.5,to=11.5,length.out=12),labels = month.abb)
title(xlab="Month")
box(bty="l")
```

## Lesser broad-bordered yellow underwing

```{r,echo=FALSE,results=TRUE}
species.data <- moth.data[which(moth.data$Species=="Lesser broad-bordered yellow underwing"),]
total.caught <- sum(species.data$NumberCaught,na.rm=TRUE)
n.traps <- length(unique(species.data$TrapNumber))
first.year <- min(species.data$Year,na.rm=TRUE)
last.year <- max(species.data$Year,na.rm=TRUE)

species.data$Year <- factor(x = species.data$Year,levels = all.yrs)
```
In total, I have caught `r total.caught` individuals of Lesser broad-bordered yellow underwing, in `r n.traps` traps, between `r first.year` and `r last.year`.

```{r,echo=FALSE,results=TRUE}
yr.avg.catches <- tapply(X = species.data$NumberCaught,INDEX = species.data$Year,FUN = mean)
yr.avg.catches[is.na(yr.avg.catches)] <- 0

par(mar=c(3.2,3.2,0.5,0.2))
par(mgp=c(2.0,0.2,0))
par(tck=-0.01)
par(las=1)

plot(all.yrs,yr.avg.catches,xaxp=c(min(all.yrs),max(all.yrs),length(all.yrs)-1),xlab="Year",ylab="Average Catch",pch=16,type="b",col="#A40122",bty="l")

dens <- hist(species.data$Month,breaks=seq(from=0.5,to=12.5,by=1),plot=FALSE)
      
barplot(dens$density,space = 0,border=NA,col="#A40122",ylab="Density")
axis(side = 1,at = seq(from=0.5,to=11.5,length.out=12),labels = month.abb)
title(xlab="Month")
box(bty="l")
```

## Lesser common rustic

```{r,echo=FALSE,results=TRUE}
species.data <- moth.data[which(moth.data$Species=="Lesser common rustic"),]
total.caught <- sum(species.data$NumberCaught,na.rm=TRUE)
n.traps <- length(unique(species.data$TrapNumber))
first.year <- min(species.data$Year,na.rm=TRUE)
last.year <- max(species.data$Year,na.rm=TRUE)

species.data$Year <- factor(x = species.data$Year,levels = all.yrs)
```
In total, I have caught `r total.caught` individuals of Lesser common rustic, in `r n.traps` traps, between `r first.year` and `r last.year`.

```{r,echo=FALSE,results=TRUE}
yr.avg.catches <- tapply(X = species.data$NumberCaught,INDEX = species.data$Year,FUN = mean)
yr.avg.catches[is.na(yr.avg.catches)] <- 0

par(mar=c(3.2,3.2,0.5,0.2))
par(mgp=c(2.0,0.2,0))
par(tck=-0.01)
par(las=1)

plot(all.yrs,yr.avg.catches,xaxp=c(min(all.yrs),max(all.yrs),length(all.yrs)-1),xlab="Year",ylab="Average Catch",pch=16,type="b",col="#A40122",bty="l")

dens <- hist(species.data$Month,breaks=seq(from=0.5,to=12.5,by=1),plot=FALSE)
      
barplot(dens$density,space = 0,border=NA,col="#A40122",ylab="Density")
axis(side = 1,at = seq(from=0.5,to=11.5,length.out=12),labels = month.abb)
title(xlab="Month")
box(bty="l")
```

## Lesser yellow underwing

```{r,echo=FALSE,results=TRUE}
species.data <- moth.data[which(moth.data$Species=="Lesser yellow underwing"),]
total.caught <- sum(species.data$NumberCaught,na.rm=TRUE)
n.traps <- length(unique(species.data$TrapNumber))
first.year <- min(species.data$Year,na.rm=TRUE)
last.year <- max(species.data$Year,na.rm=TRUE)

species.data$Year <- factor(x = species.data$Year,levels = all.yrs)
```
In total, I have caught `r total.caught` individuals of Lesser yellow underwing, in `r n.traps` traps, between `r first.year` and `r last.year`.

```{r,echo=FALSE,results=TRUE}
yr.avg.catches <- tapply(X = species.data$NumberCaught,INDEX = species.data$Year,FUN = mean)
yr.avg.catches[is.na(yr.avg.catches)] <- 0

par(mar=c(3.2,3.2,0.5,0.2))
par(mgp=c(2.0,0.2,0))
par(tck=-0.01)
par(las=1)

plot(all.yrs,yr.avg.catches,xaxp=c(min(all.yrs),max(all.yrs),length(all.yrs)-1),xlab="Year",ylab="Average Catch",pch=16,type="b",col="#A40122",bty="l")

dens <- hist(species.data$Month,breaks=seq(from=0.5,to=12.5,by=1),plot=FALSE)
      
barplot(dens$density,space = 0,border=NA,col="#A40122",ylab="Density")
axis(side = 1,at = seq(from=0.5,to=11.5,length.out=12),labels = month.abb)
title(xlab="Month")
box(bty="l")
```

## Light arches

```{r,echo=FALSE,results=TRUE}
species.data <- moth.data[which(moth.data$Species=="Light arches"),]
total.caught <- sum(species.data$NumberCaught,na.rm=TRUE)
n.traps <- length(unique(species.data$TrapNumber))
first.year <- min(species.data$Year,na.rm=TRUE)
last.year <- max(species.data$Year,na.rm=TRUE)

species.data$Year <- factor(x = species.data$Year,levels = all.yrs)
```
In total, I have caught `r total.caught` individuals of Light arches, in `r n.traps` traps, between `r first.year` and `r last.year`.

```{r,echo=FALSE,results=TRUE}
yr.avg.catches <- tapply(X = species.data$NumberCaught,INDEX = species.data$Year,FUN = mean)
yr.avg.catches[is.na(yr.avg.catches)] <- 0

par(mar=c(3.2,3.2,0.5,0.2))
par(mgp=c(2.0,0.2,0))
par(tck=-0.01)
par(las=1)

plot(all.yrs,yr.avg.catches,xaxp=c(min(all.yrs),max(all.yrs),length(all.yrs)-1),xlab="Year",ylab="Average Catch",pch=16,type="b",col="#A40122",bty="l")

dens <- hist(species.data$Month,breaks=seq(from=0.5,to=12.5,by=1),plot=FALSE)
      
barplot(dens$density,space = 0,border=NA,col="#A40122",ylab="Density")
axis(side = 1,at = seq(from=0.5,to=11.5,length.out=12),labels = month.abb)
title(xlab="Month")
box(bty="l")
```

## Light emerald

```{r,echo=FALSE,results=TRUE}
species.data <- moth.data[which(moth.data$Species=="Light emerald"),]
total.caught <- sum(species.data$NumberCaught,na.rm=TRUE)
n.traps <- length(unique(species.data$TrapNumber))
first.year <- min(species.data$Year,na.rm=TRUE)
last.year <- max(species.data$Year,na.rm=TRUE)

species.data$Year <- factor(x = species.data$Year,levels = all.yrs)
```
In total, I have caught `r total.caught` individuals of Light emerald, in `r n.traps` traps, between `r first.year` and `r last.year`.

```{r,echo=FALSE,results=TRUE}
yr.avg.catches <- tapply(X = species.data$NumberCaught,INDEX = species.data$Year,FUN = mean)
yr.avg.catches[is.na(yr.avg.catches)] <- 0

par(mar=c(3.2,3.2,0.5,0.2))
par(mgp=c(2.0,0.2,0))
par(tck=-0.01)
par(las=1)

plot(all.yrs,yr.avg.catches,xaxp=c(min(all.yrs),max(all.yrs),length(all.yrs)-1),xlab="Year",ylab="Average Catch",pch=16,type="b",col="#A40122",bty="l")

dens <- hist(species.data$Month,breaks=seq(from=0.5,to=12.5,by=1),plot=FALSE)
      
barplot(dens$density,space = 0,border=NA,col="#A40122",ylab="Density")
axis(side = 1,at = seq(from=0.5,to=11.5,length.out=12),labels = month.abb)
title(xlab="Month")
box(bty="l")
```

## Lilac beauty

```{r,echo=FALSE,results=TRUE}
species.data <- moth.data[which(moth.data$Species=="Lilac beauty"),]
total.caught <- sum(species.data$NumberCaught,na.rm=TRUE)
n.traps <- length(unique(species.data$TrapNumber))
first.year <- min(species.data$Year,na.rm=TRUE)
last.year <- max(species.data$Year,na.rm=TRUE)

species.data$Year <- factor(x = species.data$Year,levels = all.yrs)
```
In total, I have caught `r total.caught` individuals of Lilac beauty, in `r n.traps` traps, between `r first.year` and `r last.year`.

```{r,echo=FALSE,results=TRUE}
yr.avg.catches <- tapply(X = species.data$NumberCaught,INDEX = species.data$Year,FUN = mean)
yr.avg.catches[is.na(yr.avg.catches)] <- 0

par(mar=c(3.2,3.2,0.5,0.2))
par(mgp=c(2.0,0.2,0))
par(tck=-0.01)
par(las=1)

plot(all.yrs,yr.avg.catches,xaxp=c(min(all.yrs),max(all.yrs),length(all.yrs)-1),xlab="Year",ylab="Average Catch",pch=16,type="b",col="#A40122",bty="l")

dens <- hist(species.data$Month,breaks=seq(from=0.5,to=12.5,by=1),plot=FALSE)
      
barplot(dens$density,space = 0,border=NA,col="#A40122",ylab="Density")
axis(side = 1,at = seq(from=0.5,to=11.5,length.out=12),labels = month.abb)
title(xlab="Month")
box(bty="l")
```

## Lime-speck pug

```{r,echo=FALSE,results=TRUE}
species.data <- moth.data[which(moth.data$Species=="Lime-speck pug"),]
total.caught <- sum(species.data$NumberCaught,na.rm=TRUE)
n.traps <- length(unique(species.data$TrapNumber))
first.year <- min(species.data$Year,na.rm=TRUE)
last.year <- max(species.data$Year,na.rm=TRUE)

species.data$Year <- factor(x = species.data$Year,levels = all.yrs)
```
In total, I have caught `r total.caught` individuals of Lime-speck pug, in `r n.traps` traps, between `r first.year` and `r last.year`.

```{r,echo=FALSE,results=TRUE}
yr.avg.catches <- tapply(X = species.data$NumberCaught,INDEX = species.data$Year,FUN = mean)
yr.avg.catches[is.na(yr.avg.catches)] <- 0

par(mar=c(3.2,3.2,0.5,0.2))
par(mgp=c(2.0,0.2,0))
par(tck=-0.01)
par(las=1)

plot(all.yrs,yr.avg.catches,xaxp=c(min(all.yrs),max(all.yrs),length(all.yrs)-1),xlab="Year",ylab="Average Catch",pch=16,type="b",col="#A40122",bty="l")

dens <- hist(species.data$Month,breaks=seq(from=0.5,to=12.5,by=1),plot=FALSE)
      
barplot(dens$density,space = 0,border=NA,col="#A40122",ylab="Density")
axis(side = 1,at = seq(from=0.5,to=11.5,length.out=12),labels = month.abb)
title(xlab="Month")
box(bty="l")
```

## Lunar hornet moth

```{r,echo=FALSE,results=TRUE}
species.data <- moth.data[which(moth.data$Species=="Lunar hornet moth"),]
total.caught <- sum(species.data$NumberCaught,na.rm=TRUE)
n.traps <- length(unique(species.data$TrapNumber))
first.year <- min(species.data$Year,na.rm=TRUE)
last.year <- max(species.data$Year,na.rm=TRUE)

species.data$Year <- factor(x = species.data$Year,levels = all.yrs)
```
In total, I have caught `r total.caught` individuals of Lunar hornet moth, in `r n.traps` traps, between `r first.year` and `r last.year`.

```{r,echo=FALSE,results=TRUE}
yr.avg.catches <- tapply(X = species.data$NumberCaught,INDEX = species.data$Year,FUN = mean)
yr.avg.catches[is.na(yr.avg.catches)] <- 0

par(mar=c(3.2,3.2,0.5,0.2))
par(mgp=c(2.0,0.2,0))
par(tck=-0.01)
par(las=1)

plot(all.yrs,yr.avg.catches,xaxp=c(min(all.yrs),max(all.yrs),length(all.yrs)-1),xlab="Year",ylab="Average Catch",pch=16,type="b",col="#A40122",bty="l")

dens <- hist(species.data$Month,breaks=seq(from=0.5,to=12.5,by=1),plot=FALSE)
      
barplot(dens$density,space = 0,border=NA,col="#A40122",ylab="Density")
axis(side = 1,at = seq(from=0.5,to=11.5,length.out=12),labels = month.abb)
title(xlab="Month")
box(bty="l")
```

## Lunar underwing

```{r,echo=FALSE,results=TRUE}
species.data <- moth.data[which(moth.data$Species=="Lunar underwing"),]
total.caught <- sum(species.data$NumberCaught,na.rm=TRUE)
n.traps <- length(unique(species.data$TrapNumber))
first.year <- min(species.data$Year,na.rm=TRUE)
last.year <- max(species.data$Year,na.rm=TRUE)

species.data$Year <- factor(x = species.data$Year,levels = all.yrs)
```
In total, I have caught `r total.caught` individuals of Lunar underwing, in `r n.traps` traps, between `r first.year` and `r last.year`.

```{r,echo=FALSE,results=TRUE}
yr.avg.catches <- tapply(X = species.data$NumberCaught,INDEX = species.data$Year,FUN = mean)
yr.avg.catches[is.na(yr.avg.catches)] <- 0

par(mar=c(3.2,3.2,0.5,0.2))
par(mgp=c(2.0,0.2,0))
par(tck=-0.01)
par(las=1)

plot(all.yrs,yr.avg.catches,xaxp=c(min(all.yrs),max(all.yrs),length(all.yrs)-1),xlab="Year",ylab="Average Catch",pch=16,type="b",col="#A40122",bty="l")

dens <- hist(species.data$Month,breaks=seq(from=0.5,to=12.5,by=1),plot=FALSE)
      
barplot(dens$density,space = 0,border=NA,col="#A40122",ylab="Density")
axis(side = 1,at = seq(from=0.5,to=11.5,length.out=12),labels = month.abb)
title(xlab="Month")
box(bty="l")
```

## Lychnis

```{r,echo=FALSE,results=TRUE}
species.data <- moth.data[which(moth.data$Species=="Lychnis"),]
total.caught <- sum(species.data$NumberCaught,na.rm=TRUE)
n.traps <- length(unique(species.data$TrapNumber))
first.year <- min(species.data$Year,na.rm=TRUE)
last.year <- max(species.data$Year,na.rm=TRUE)

species.data$Year <- factor(x = species.data$Year,levels = all.yrs)
```
In total, I have caught `r total.caught` individuals of Lychnis, in `r n.traps` traps, between `r first.year` and `r last.year`.

```{r,echo=FALSE,results=TRUE}
yr.avg.catches <- tapply(X = species.data$NumberCaught,INDEX = species.data$Year,FUN = mean)
yr.avg.catches[is.na(yr.avg.catches)] <- 0

par(mar=c(3.2,3.2,0.5,0.2))
par(mgp=c(2.0,0.2,0))
par(tck=-0.01)
par(las=1)

plot(all.yrs,yr.avg.catches,xaxp=c(min(all.yrs),max(all.yrs),length(all.yrs)-1),xlab="Year",ylab="Average Catch",pch=16,type="b",col="#A40122",bty="l")

dens <- hist(species.data$Month,breaks=seq(from=0.5,to=12.5,by=1),plot=FALSE)
      
barplot(dens$density,space = 0,border=NA,col="#A40122",ylab="Density")
axis(side = 1,at = seq(from=0.5,to=11.5,length.out=12),labels = month.abb)
title(xlab="Month")
box(bty="l")
```

## Magpie

```{r,echo=FALSE,results=TRUE}
species.data <- moth.data[which(moth.data$Species=="Magpie"),]
total.caught <- sum(species.data$NumberCaught,na.rm=TRUE)
n.traps <- length(unique(species.data$TrapNumber))
first.year <- min(species.data$Year,na.rm=TRUE)
last.year <- max(species.data$Year,na.rm=TRUE)

species.data$Year <- factor(x = species.data$Year,levels = all.yrs)
```
In total, I have caught `r total.caught` individuals of Magpie, in `r n.traps` traps, between `r first.year` and `r last.year`.

```{r,echo=FALSE,results=TRUE}
yr.avg.catches <- tapply(X = species.data$NumberCaught,INDEX = species.data$Year,FUN = mean)
yr.avg.catches[is.na(yr.avg.catches)] <- 0

par(mar=c(3.2,3.2,0.5,0.2))
par(mgp=c(2.0,0.2,0))
par(tck=-0.01)
par(las=1)

plot(all.yrs,yr.avg.catches,xaxp=c(min(all.yrs),max(all.yrs),length(all.yrs)-1),xlab="Year",ylab="Average Catch",pch=16,type="b",col="#A40122",bty="l")

dens <- hist(species.data$Month,breaks=seq(from=0.5,to=12.5,by=1),plot=FALSE)
      
barplot(dens$density,space = 0,border=NA,col="#A40122",ylab="Density")
axis(side = 1,at = seq(from=0.5,to=11.5,length.out=12),labels = month.abb)
title(xlab="Month")
box(bty="l")
```

## Mallow

```{r,echo=FALSE,results=TRUE}
species.data <- moth.data[which(moth.data$Species=="Mallow"),]
total.caught <- sum(species.data$NumberCaught,na.rm=TRUE)
n.traps <- length(unique(species.data$TrapNumber))
first.year <- min(species.data$Year,na.rm=TRUE)
last.year <- max(species.data$Year,na.rm=TRUE)

species.data$Year <- factor(x = species.data$Year,levels = all.yrs)
```
In total, I have caught `r total.caught` individuals of Mallow, in `r n.traps` traps, between `r first.year` and `r last.year`.

```{r,echo=FALSE,results=TRUE}
yr.avg.catches <- tapply(X = species.data$NumberCaught,INDEX = species.data$Year,FUN = mean)
yr.avg.catches[is.na(yr.avg.catches)] <- 0

par(mar=c(3.2,3.2,0.5,0.2))
par(mgp=c(2.0,0.2,0))
par(tck=-0.01)
par(las=1)

plot(all.yrs,yr.avg.catches,xaxp=c(min(all.yrs),max(all.yrs),length(all.yrs)-1),xlab="Year",ylab="Average Catch",pch=16,type="b",col="#A40122",bty="l")

dens <- hist(species.data$Month,breaks=seq(from=0.5,to=12.5,by=1),plot=FALSE)
      
barplot(dens$density,space = 0,border=NA,col="#A40122",ylab="Density")
axis(side = 1,at = seq(from=0.5,to=11.5,length.out=12),labels = month.abb)
title(xlab="Month")
box(bty="l")
```

## Marbled beauty

```{r,echo=FALSE,results=TRUE}
species.data <- moth.data[which(moth.data$Species=="Marbled beauty"),]
total.caught <- sum(species.data$NumberCaught,na.rm=TRUE)
n.traps <- length(unique(species.data$TrapNumber))
first.year <- min(species.data$Year,na.rm=TRUE)
last.year <- max(species.data$Year,na.rm=TRUE)

species.data$Year <- factor(x = species.data$Year,levels = all.yrs)
```
In total, I have caught `r total.caught` individuals of Marbled beauty, in `r n.traps` traps, between `r first.year` and `r last.year`.

```{r,echo=FALSE,results=TRUE}
yr.avg.catches <- tapply(X = species.data$NumberCaught,INDEX = species.data$Year,FUN = mean)
yr.avg.catches[is.na(yr.avg.catches)] <- 0

par(mar=c(3.2,3.2,0.5,0.2))
par(mgp=c(2.0,0.2,0))
par(tck=-0.01)
par(las=1)

plot(all.yrs,yr.avg.catches,xaxp=c(min(all.yrs),max(all.yrs),length(all.yrs)-1),xlab="Year",ylab="Average Catch",pch=16,type="b",col="#A40122",bty="l")

dens <- hist(species.data$Month,breaks=seq(from=0.5,to=12.5,by=1),plot=FALSE)
      
barplot(dens$density,space = 0,border=NA,col="#A40122",ylab="Density")
axis(side = 1,at = seq(from=0.5,to=11.5,length.out=12),labels = month.abb)
title(xlab="Month")
box(bty="l")
```

## Marbled minor

```{r,echo=FALSE,results=TRUE}
species.data <- moth.data[which(moth.data$Species=="Marbled minor"),]
total.caught <- sum(species.data$NumberCaught,na.rm=TRUE)
n.traps <- length(unique(species.data$TrapNumber))
first.year <- min(species.data$Year,na.rm=TRUE)
last.year <- max(species.data$Year,na.rm=TRUE)

species.data$Year <- factor(x = species.data$Year,levels = all.yrs)
```
In total, I have caught `r total.caught` individuals of Marbled minor, in `r n.traps` traps, between `r first.year` and `r last.year`.

```{r,echo=FALSE,results=TRUE}
yr.avg.catches <- tapply(X = species.data$NumberCaught,INDEX = species.data$Year,FUN = mean)
yr.avg.catches[is.na(yr.avg.catches)] <- 0

par(mar=c(3.2,3.2,0.5,0.2))
par(mgp=c(2.0,0.2,0))
par(tck=-0.01)
par(las=1)

plot(all.yrs,yr.avg.catches,xaxp=c(min(all.yrs),max(all.yrs),length(all.yrs)-1),xlab="Year",ylab="Average Catch",pch=16,type="b",col="#A40122",bty="l")

dens <- hist(species.data$Month,breaks=seq(from=0.5,to=12.5,by=1),plot=FALSE)
      
barplot(dens$density,space = 0,border=NA,col="#A40122",ylab="Density")
axis(side = 1,at = seq(from=0.5,to=11.5,length.out=12),labels = month.abb)
title(xlab="Month")
box(bty="l")
```

## March moth

```{r,echo=FALSE,results=TRUE}
species.data <- moth.data[which(moth.data$Species=="March moth"),]
total.caught <- sum(species.data$NumberCaught,na.rm=TRUE)
n.traps <- length(unique(species.data$TrapNumber))
first.year <- min(species.data$Year,na.rm=TRUE)
last.year <- max(species.data$Year,na.rm=TRUE)

species.data$Year <- factor(x = species.data$Year,levels = all.yrs)
```
In total, I have caught `r total.caught` individuals of March moth, in `r n.traps` traps, between `r first.year` and `r last.year`.

```{r,echo=FALSE,results=TRUE}
yr.avg.catches <- tapply(X = species.data$NumberCaught,INDEX = species.data$Year,FUN = mean)
yr.avg.catches[is.na(yr.avg.catches)] <- 0

par(mar=c(3.2,3.2,0.5,0.2))
par(mgp=c(2.0,0.2,0))
par(tck=-0.01)
par(las=1)

plot(all.yrs,yr.avg.catches,xaxp=c(min(all.yrs),max(all.yrs),length(all.yrs)-1),xlab="Year",ylab="Average Catch",pch=16,type="b",col="#A40122",bty="l")

dens <- hist(species.data$Month,breaks=seq(from=0.5,to=12.5,by=1),plot=FALSE)
      
barplot(dens$density,space = 0,border=NA,col="#A40122",ylab="Density")
axis(side = 1,at = seq(from=0.5,to=11.5,length.out=12),labels = month.abb)
title(xlab="Month")
box(bty="l")
```

## Merveille du jour

```{r,echo=FALSE,results=TRUE}
species.data <- moth.data[which(moth.data$Species=="Merveille du jour"),]
total.caught <- sum(species.data$NumberCaught,na.rm=TRUE)
n.traps <- length(unique(species.data$TrapNumber))
first.year <- min(species.data$Year,na.rm=TRUE)
last.year <- max(species.data$Year,na.rm=TRUE)

species.data$Year <- factor(x = species.data$Year,levels = all.yrs)
```
In total, I have caught `r total.caught` individuals of Merveille du jour, in `r n.traps` traps, between `r first.year` and `r last.year`.

```{r,echo=FALSE,results=TRUE}
yr.avg.catches <- tapply(X = species.data$NumberCaught,INDEX = species.data$Year,FUN = mean)
yr.avg.catches[is.na(yr.avg.catches)] <- 0

par(mar=c(3.2,3.2,0.5,0.2))
par(mgp=c(2.0,0.2,0))
par(tck=-0.01)
par(las=1)

plot(all.yrs,yr.avg.catches,xaxp=c(min(all.yrs),max(all.yrs),length(all.yrs)-1),xlab="Year",ylab="Average Catch",pch=16,type="b",col="#A40122",bty="l")

dens <- hist(species.data$Month,breaks=seq(from=0.5,to=12.5,by=1),plot=FALSE)
      
barplot(dens$density,space = 0,border=NA,col="#A40122",ylab="Density")
axis(side = 1,at = seq(from=0.5,to=11.5,length.out=12),labels = month.abb)
title(xlab="Month")
box(bty="l")
```

## Middle-barred minor

```{r,echo=FALSE,results=TRUE}
species.data <- moth.data[which(moth.data$Species=="Middle-barred minor"),]
total.caught <- sum(species.data$NumberCaught,na.rm=TRUE)
n.traps <- length(unique(species.data$TrapNumber))
first.year <- min(species.data$Year,na.rm=TRUE)
last.year <- max(species.data$Year,na.rm=TRUE)

species.data$Year <- factor(x = species.data$Year,levels = all.yrs)
```
In total, I have caught `r total.caught` individuals of Middle-barred minor, in `r n.traps` traps, between `r first.year` and `r last.year`.

```{r,echo=FALSE,results=TRUE}
yr.avg.catches <- tapply(X = species.data$NumberCaught,INDEX = species.data$Year,FUN = mean)
yr.avg.catches[is.na(yr.avg.catches)] <- 0

par(mar=c(3.2,3.2,0.5,0.2))
par(mgp=c(2.0,0.2,0))
par(tck=-0.01)
par(las=1)

plot(all.yrs,yr.avg.catches,xaxp=c(min(all.yrs),max(all.yrs),length(all.yrs)-1),xlab="Year",ylab="Average Catch",pch=16,type="b",col="#A40122",bty="l")

dens <- hist(species.data$Month,breaks=seq(from=0.5,to=12.5,by=1),plot=FALSE)
      
barplot(dens$density,space = 0,border=NA,col="#A40122",ylab="Density")
axis(side = 1,at = seq(from=0.5,to=11.5,length.out=12),labels = month.abb)
title(xlab="Month")
box(bty="l")
```

## Miller

```{r,echo=FALSE,results=TRUE}
species.data <- moth.data[which(moth.data$Species=="Miller"),]
total.caught <- sum(species.data$NumberCaught,na.rm=TRUE)
n.traps <- length(unique(species.data$TrapNumber))
first.year <- min(species.data$Year,na.rm=TRUE)
last.year <- max(species.data$Year,na.rm=TRUE)

species.data$Year <- factor(x = species.data$Year,levels = all.yrs)
```
In total, I have caught `r total.caught` individuals of Miller, in `r n.traps` traps, between `r first.year` and `r last.year`.

```{r,echo=FALSE,results=TRUE}
yr.avg.catches <- tapply(X = species.data$NumberCaught,INDEX = species.data$Year,FUN = mean)
yr.avg.catches[is.na(yr.avg.catches)] <- 0

par(mar=c(3.2,3.2,0.5,0.2))
par(mgp=c(2.0,0.2,0))
par(tck=-0.01)
par(las=1)

plot(all.yrs,yr.avg.catches,xaxp=c(min(all.yrs),max(all.yrs),length(all.yrs)-1),xlab="Year",ylab="Average Catch",pch=16,type="b",col="#A40122",bty="l")

dens <- hist(species.data$Month,breaks=seq(from=0.5,to=12.5,by=1),plot=FALSE)
      
barplot(dens$density,space = 0,border=NA,col="#A40122",ylab="Density")
axis(side = 1,at = seq(from=0.5,to=11.5,length.out=12),labels = month.abb)
title(xlab="Month")
box(bty="l")
```

## Mottled pug

```{r,echo=FALSE,results=TRUE}
species.data <- moth.data[which(moth.data$Species=="Mottled pug"),]
total.caught <- sum(species.data$NumberCaught,na.rm=TRUE)
n.traps <- length(unique(species.data$TrapNumber))
first.year <- min(species.data$Year,na.rm=TRUE)
last.year <- max(species.data$Year,na.rm=TRUE)

species.data$Year <- factor(x = species.data$Year,levels = all.yrs)
```
In total, I have caught `r total.caught` individuals of Mottled pug, in `r n.traps` traps, between `r first.year` and `r last.year`.

```{r,echo=FALSE,results=TRUE}
yr.avg.catches <- tapply(X = species.data$NumberCaught,INDEX = species.data$Year,FUN = mean)
yr.avg.catches[is.na(yr.avg.catches)] <- 0

par(mar=c(3.2,3.2,0.5,0.2))
par(mgp=c(2.0,0.2,0))
par(tck=-0.01)
par(las=1)

plot(all.yrs,yr.avg.catches,xaxp=c(min(all.yrs),max(all.yrs),length(all.yrs)-1),xlab="Year",ylab="Average Catch",pch=16,type="b",col="#A40122",bty="l")

dens <- hist(species.data$Month,breaks=seq(from=0.5,to=12.5,by=1),plot=FALSE)
      
barplot(dens$density,space = 0,border=NA,col="#A40122",ylab="Density")
axis(side = 1,at = seq(from=0.5,to=11.5,length.out=12),labels = month.abb)
title(xlab="Month")
box(bty="l")
```

## Mottled rustic

```{r,echo=FALSE,results=TRUE}
species.data <- moth.data[which(moth.data$Species=="Mottled rustic"),]
total.caught <- sum(species.data$NumberCaught,na.rm=TRUE)
n.traps <- length(unique(species.data$TrapNumber))
first.year <- min(species.data$Year,na.rm=TRUE)
last.year <- max(species.data$Year,na.rm=TRUE)

species.data$Year <- factor(x = species.data$Year,levels = all.yrs)
```
In total, I have caught `r total.caught` individuals of Mottled rustic, in `r n.traps` traps, between `r first.year` and `r last.year`.

```{r,echo=FALSE,results=TRUE}
yr.avg.catches <- tapply(X = species.data$NumberCaught,INDEX = species.data$Year,FUN = mean)
yr.avg.catches[is.na(yr.avg.catches)] <- 0

par(mar=c(3.2,3.2,0.5,0.2))
par(mgp=c(2.0,0.2,0))
par(tck=-0.01)
par(las=1)

plot(all.yrs,yr.avg.catches,xaxp=c(min(all.yrs),max(all.yrs),length(all.yrs)-1),xlab="Year",ylab="Average Catch",pch=16,type="b",col="#A40122",bty="l")

dens <- hist(species.data$Month,breaks=seq(from=0.5,to=12.5,by=1),plot=FALSE)
      
barplot(dens$density,space = 0,border=NA,col="#A40122",ylab="Density")
axis(side = 1,at = seq(from=0.5,to=11.5,length.out=12),labels = month.abb)
title(xlab="Month")
box(bty="l")
```

## Mottled Umber

```{r,echo=FALSE,results=TRUE}
species.data <- moth.data[which(moth.data$Species=="Mottled Umber"),]
total.caught <- sum(species.data$NumberCaught,na.rm=TRUE)
n.traps <- length(unique(species.data$TrapNumber))
first.year <- min(species.data$Year,na.rm=TRUE)
last.year <- max(species.data$Year,na.rm=TRUE)

species.data$Year <- factor(x = species.data$Year,levels = all.yrs)
```
In total, I have caught `r total.caught` individuals of Mottled Umber, in `r n.traps` traps, between `r first.year` and `r last.year`.

```{r,echo=FALSE,results=TRUE}
yr.avg.catches <- tapply(X = species.data$NumberCaught,INDEX = species.data$Year,FUN = mean)
yr.avg.catches[is.na(yr.avg.catches)] <- 0

par(mar=c(3.2,3.2,0.5,0.2))
par(mgp=c(2.0,0.2,0))
par(tck=-0.01)
par(las=1)

plot(all.yrs,yr.avg.catches,xaxp=c(min(all.yrs),max(all.yrs),length(all.yrs)-1),xlab="Year",ylab="Average Catch",pch=16,type="b",col="#A40122",bty="l")

dens <- hist(species.data$Month,breaks=seq(from=0.5,to=12.5,by=1),plot=FALSE)
      
barplot(dens$density,space = 0,border=NA,col="#A40122",ylab="Density")
axis(side = 1,at = seq(from=0.5,to=11.5,length.out=12),labels = month.abb)
title(xlab="Month")
box(bty="l")
```

## Mouse

```{r,echo=FALSE,results=TRUE}
species.data <- moth.data[which(moth.data$Species=="Mouse"),]
total.caught <- sum(species.data$NumberCaught,na.rm=TRUE)
n.traps <- length(unique(species.data$TrapNumber))
first.year <- min(species.data$Year,na.rm=TRUE)
last.year <- max(species.data$Year,na.rm=TRUE)

species.data$Year <- factor(x = species.data$Year,levels = all.yrs)
```
In total, I have caught `r total.caught` individuals of Mouse, in `r n.traps` traps, between `r first.year` and `r last.year`.

```{r,echo=FALSE,results=TRUE}
yr.avg.catches <- tapply(X = species.data$NumberCaught,INDEX = species.data$Year,FUN = mean)
yr.avg.catches[is.na(yr.avg.catches)] <- 0

par(mar=c(3.2,3.2,0.5,0.2))
par(mgp=c(2.0,0.2,0))
par(tck=-0.01)
par(las=1)

plot(all.yrs,yr.avg.catches,xaxp=c(min(all.yrs),max(all.yrs),length(all.yrs)-1),xlab="Year",ylab="Average Catch",pch=16,type="b",col="#A40122",bty="l")

dens <- hist(species.data$Month,breaks=seq(from=0.5,to=12.5,by=1),plot=FALSE)
      
barplot(dens$density,space = 0,border=NA,col="#A40122",ylab="Density")
axis(side = 1,at = seq(from=0.5,to=11.5,length.out=12),labels = month.abb)
title(xlab="Month")
box(bty="l")
```

## Muslin moth

```{r,echo=FALSE,results=TRUE}
species.data <- moth.data[which(moth.data$Species=="Muslin moth"),]
total.caught <- sum(species.data$NumberCaught,na.rm=TRUE)
n.traps <- length(unique(species.data$TrapNumber))
first.year <- min(species.data$Year,na.rm=TRUE)
last.year <- max(species.data$Year,na.rm=TRUE)

species.data$Year <- factor(x = species.data$Year,levels = all.yrs)
```
In total, I have caught `r total.caught` individuals of Muslin moth, in `r n.traps` traps, between `r first.year` and `r last.year`.

```{r,echo=FALSE,results=TRUE}
yr.avg.catches <- tapply(X = species.data$NumberCaught,INDEX = species.data$Year,FUN = mean)
yr.avg.catches[is.na(yr.avg.catches)] <- 0

par(mar=c(3.2,3.2,0.5,0.2))
par(mgp=c(2.0,0.2,0))
par(tck=-0.01)
par(las=1)

plot(all.yrs,yr.avg.catches,xaxp=c(min(all.yrs),max(all.yrs),length(all.yrs)-1),xlab="Year",ylab="Average Catch",pch=16,type="b",col="#A40122",bty="l")

dens <- hist(species.data$Month,breaks=seq(from=0.5,to=12.5,by=1),plot=FALSE)
      
barplot(dens$density,space = 0,border=NA,col="#A40122",ylab="Density")
axis(side = 1,at = seq(from=0.5,to=11.5,length.out=12),labels = month.abb)
title(xlab="Month")
box(bty="l")
```

## November moth

```{r,echo=FALSE,results=TRUE}
species.data <- moth.data[which(moth.data$Species=="November moth"),]
total.caught <- sum(species.data$NumberCaught,na.rm=TRUE)
n.traps <- length(unique(species.data$TrapNumber))
first.year <- min(species.data$Year,na.rm=TRUE)
last.year <- max(species.data$Year,na.rm=TRUE)

species.data$Year <- factor(x = species.data$Year,levels = all.yrs)
```
In total, I have caught `r total.caught` individuals of November moth, in `r n.traps` traps, between `r first.year` and `r last.year`.

```{r,echo=FALSE,results=TRUE}
yr.avg.catches <- tapply(X = species.data$NumberCaught,INDEX = species.data$Year,FUN = mean)
yr.avg.catches[is.na(yr.avg.catches)] <- 0

par(mar=c(3.2,3.2,0.5,0.2))
par(mgp=c(2.0,0.2,0))
par(tck=-0.01)
par(las=1)

plot(all.yrs,yr.avg.catches,xaxp=c(min(all.yrs),max(all.yrs),length(all.yrs)-1),xlab="Year",ylab="Average Catch",pch=16,type="b",col="#A40122",bty="l")

dens <- hist(species.data$Month,breaks=seq(from=0.5,to=12.5,by=1),plot=FALSE)
      
barplot(dens$density,space = 0,border=NA,col="#A40122",ylab="Density")
axis(side = 1,at = seq(from=0.5,to=11.5,length.out=12),labels = month.abb)
title(xlab="Month")
box(bty="l")
```

## Nutmeg

```{r,echo=FALSE,results=TRUE}
species.data <- moth.data[which(moth.data$Species=="Nutmeg"),]
total.caught <- sum(species.data$NumberCaught,na.rm=TRUE)
n.traps <- length(unique(species.data$TrapNumber))
first.year <- min(species.data$Year,na.rm=TRUE)
last.year <- max(species.data$Year,na.rm=TRUE)

species.data$Year <- factor(x = species.data$Year,levels = all.yrs)
```
In total, I have caught `r total.caught` individuals of Nutmeg, in `r n.traps` traps, between `r first.year` and `r last.year`.

```{r,echo=FALSE,results=TRUE}
yr.avg.catches <- tapply(X = species.data$NumberCaught,INDEX = species.data$Year,FUN = mean)
yr.avg.catches[is.na(yr.avg.catches)] <- 0

par(mar=c(3.2,3.2,0.5,0.2))
par(mgp=c(2.0,0.2,0))
par(tck=-0.01)
par(las=1)

plot(all.yrs,yr.avg.catches,xaxp=c(min(all.yrs),max(all.yrs),length(all.yrs)-1),xlab="Year",ylab="Average Catch",pch=16,type="b",col="#A40122",bty="l")

dens <- hist(species.data$Month,breaks=seq(from=0.5,to=12.5,by=1),plot=FALSE)
      
barplot(dens$density,space = 0,border=NA,col="#A40122",ylab="Density")
axis(side = 1,at = seq(from=0.5,to=11.5,length.out=12),labels = month.abb)
title(xlab="Month")
box(bty="l")
```

## Oak-tree pug

```{r,echo=FALSE,results=TRUE}
species.data <- moth.data[which(moth.data$Species=="Oak-tree pug"),]
total.caught <- sum(species.data$NumberCaught,na.rm=TRUE)
n.traps <- length(unique(species.data$TrapNumber))
first.year <- min(species.data$Year,na.rm=TRUE)
last.year <- max(species.data$Year,na.rm=TRUE)

species.data$Year <- factor(x = species.data$Year,levels = all.yrs)
```
In total, I have caught `r total.caught` individuals of Oak-tree pug, in `r n.traps` traps, between `r first.year` and `r last.year`.

```{r,echo=FALSE,results=TRUE}
yr.avg.catches <- tapply(X = species.data$NumberCaught,INDEX = species.data$Year,FUN = mean)
yr.avg.catches[is.na(yr.avg.catches)] <- 0

par(mar=c(3.2,3.2,0.5,0.2))
par(mgp=c(2.0,0.2,0))
par(tck=-0.01)
par(las=1)

plot(all.yrs,yr.avg.catches,xaxp=c(min(all.yrs),max(all.yrs),length(all.yrs)-1),xlab="Year",ylab="Average Catch",pch=16,type="b",col="#A40122",bty="l")

dens <- hist(species.data$Month,breaks=seq(from=0.5,to=12.5,by=1),plot=FALSE)
      
barplot(dens$density,space = 0,border=NA,col="#A40122",ylab="Density")
axis(side = 1,at = seq(from=0.5,to=11.5,length.out=12),labels = month.abb)
title(xlab="Month")
box(bty="l")
```

## Oak beauty

```{r,echo=FALSE,results=TRUE}
species.data <- moth.data[which(moth.data$Species=="Oak beauty"),]
total.caught <- sum(species.data$NumberCaught,na.rm=TRUE)
n.traps <- length(unique(species.data$TrapNumber))
first.year <- min(species.data$Year,na.rm=TRUE)
last.year <- max(species.data$Year,na.rm=TRUE)

species.data$Year <- factor(x = species.data$Year,levels = all.yrs)
```
In total, I have caught `r total.caught` individuals of Oak beauty, in `r n.traps` traps, between `r first.year` and `r last.year`.

```{r,echo=FALSE,results=TRUE}
yr.avg.catches <- tapply(X = species.data$NumberCaught,INDEX = species.data$Year,FUN = mean)
yr.avg.catches[is.na(yr.avg.catches)] <- 0

par(mar=c(3.2,3.2,0.5,0.2))
par(mgp=c(2.0,0.2,0))
par(tck=-0.01)
par(las=1)

plot(all.yrs,yr.avg.catches,xaxp=c(min(all.yrs),max(all.yrs),length(all.yrs)-1),xlab="Year",ylab="Average Catch",pch=16,type="b",col="#A40122",bty="l")

dens <- hist(species.data$Month,breaks=seq(from=0.5,to=12.5,by=1),plot=FALSE)
      
barplot(dens$density,space = 0,border=NA,col="#A40122",ylab="Density")
axis(side = 1,at = seq(from=0.5,to=11.5,length.out=12),labels = month.abb)
title(xlab="Month")
box(bty="l")
```

## Old lady

```{r,echo=FALSE,results=TRUE}
species.data <- moth.data[which(moth.data$Species=="Old lady"),]
total.caught <- sum(species.data$NumberCaught,na.rm=TRUE)
n.traps <- length(unique(species.data$TrapNumber))
first.year <- min(species.data$Year,na.rm=TRUE)
last.year <- max(species.data$Year,na.rm=TRUE)

species.data$Year <- factor(x = species.data$Year,levels = all.yrs)
```
In total, I have caught `r total.caught` individuals of Old lady, in `r n.traps` traps, between `r first.year` and `r last.year`.

```{r,echo=FALSE,results=TRUE}
yr.avg.catches <- tapply(X = species.data$NumberCaught,INDEX = species.data$Year,FUN = mean)
yr.avg.catches[is.na(yr.avg.catches)] <- 0

par(mar=c(3.2,3.2,0.5,0.2))
par(mgp=c(2.0,0.2,0))
par(tck=-0.01)
par(las=1)

plot(all.yrs,yr.avg.catches,xaxp=c(min(all.yrs),max(all.yrs),length(all.yrs)-1),xlab="Year",ylab="Average Catch",pch=16,type="b",col="#A40122",bty="l")

dens <- hist(species.data$Month,breaks=seq(from=0.5,to=12.5,by=1),plot=FALSE)
      
barplot(dens$density,space = 0,border=NA,col="#A40122",ylab="Density")
axis(side = 1,at = seq(from=0.5,to=11.5,length.out=12),labels = month.abb)
title(xlab="Month")
box(bty="l")
```

## Orange swift

```{r,echo=FALSE,results=TRUE}
species.data <- moth.data[which(moth.data$Species=="Orange swift"),]
total.caught <- sum(species.data$NumberCaught,na.rm=TRUE)
n.traps <- length(unique(species.data$TrapNumber))
first.year <- min(species.data$Year,na.rm=TRUE)
last.year <- max(species.data$Year,na.rm=TRUE)

species.data$Year <- factor(x = species.data$Year,levels = all.yrs)
```
In total, I have caught `r total.caught` individuals of Orange swift, in `r n.traps` traps, between `r first.year` and `r last.year`.

```{r,echo=FALSE,results=TRUE}
yr.avg.catches <- tapply(X = species.data$NumberCaught,INDEX = species.data$Year,FUN = mean)
yr.avg.catches[is.na(yr.avg.catches)] <- 0

par(mar=c(3.2,3.2,0.5,0.2))
par(mgp=c(2.0,0.2,0))
par(tck=-0.01)
par(las=1)

plot(all.yrs,yr.avg.catches,xaxp=c(min(all.yrs),max(all.yrs),length(all.yrs)-1),xlab="Year",ylab="Average Catch",pch=16,type="b",col="#A40122",bty="l")

dens <- hist(species.data$Month,breaks=seq(from=0.5,to=12.5,by=1),plot=FALSE)
      
barplot(dens$density,space = 0,border=NA,col="#A40122",ylab="Density")
axis(side = 1,at = seq(from=0.5,to=11.5,length.out=12),labels = month.abb)
title(xlab="Month")
box(bty="l")
```

## Pale brindled beauty

```{r,echo=FALSE,results=TRUE}
species.data <- moth.data[which(moth.data$Species=="Pale brindled beauty"),]
total.caught <- sum(species.data$NumberCaught,na.rm=TRUE)
n.traps <- length(unique(species.data$TrapNumber))
first.year <- min(species.data$Year,na.rm=TRUE)
last.year <- max(species.data$Year,na.rm=TRUE)

species.data$Year <- factor(x = species.data$Year,levels = all.yrs)
```
In total, I have caught `r total.caught` individuals of Pale brindled beauty, in `r n.traps` traps, between `r first.year` and `r last.year`.

```{r,echo=FALSE,results=TRUE}
yr.avg.catches <- tapply(X = species.data$NumberCaught,INDEX = species.data$Year,FUN = mean)
yr.avg.catches[is.na(yr.avg.catches)] <- 0

par(mar=c(3.2,3.2,0.5,0.2))
par(mgp=c(2.0,0.2,0))
par(tck=-0.01)
par(las=1)

plot(all.yrs,yr.avg.catches,xaxp=c(min(all.yrs),max(all.yrs),length(all.yrs)-1),xlab="Year",ylab="Average Catch",pch=16,type="b",col="#A40122",bty="l")

dens <- hist(species.data$Month,breaks=seq(from=0.5,to=12.5,by=1),plot=FALSE)
      
barplot(dens$density,space = 0,border=NA,col="#A40122",ylab="Density")
axis(side = 1,at = seq(from=0.5,to=11.5,length.out=12),labels = month.abb)
title(xlab="Month")
box(bty="l")
```

## Pale mottled willow

```{r,echo=FALSE,results=TRUE}
species.data <- moth.data[which(moth.data$Species=="Pale mottled willow"),]
total.caught <- sum(species.data$NumberCaught,na.rm=TRUE)
n.traps <- length(unique(species.data$TrapNumber))
first.year <- min(species.data$Year,na.rm=TRUE)
last.year <- max(species.data$Year,na.rm=TRUE)

species.data$Year <- factor(x = species.data$Year,levels = all.yrs)
```
In total, I have caught `r total.caught` individuals of Pale mottled willow, in `r n.traps` traps, between `r first.year` and `r last.year`.

```{r,echo=FALSE,results=TRUE}
yr.avg.catches <- tapply(X = species.data$NumberCaught,INDEX = species.data$Year,FUN = mean)
yr.avg.catches[is.na(yr.avg.catches)] <- 0

par(mar=c(3.2,3.2,0.5,0.2))
par(mgp=c(2.0,0.2,0))
par(tck=-0.01)
par(las=1)

plot(all.yrs,yr.avg.catches,xaxp=c(min(all.yrs),max(all.yrs),length(all.yrs)-1),xlab="Year",ylab="Average Catch",pch=16,type="b",col="#A40122",bty="l")

dens <- hist(species.data$Month,breaks=seq(from=0.5,to=12.5,by=1),plot=FALSE)
      
barplot(dens$density,space = 0,border=NA,col="#A40122",ylab="Density")
axis(side = 1,at = seq(from=0.5,to=11.5,length.out=12),labels = month.abb)
title(xlab="Month")
box(bty="l")
```

## Pale tussock

```{r,echo=FALSE,results=TRUE}
species.data <- moth.data[which(moth.data$Species=="Pale tussock"),]
total.caught <- sum(species.data$NumberCaught,na.rm=TRUE)
n.traps <- length(unique(species.data$TrapNumber))
first.year <- min(species.data$Year,na.rm=TRUE)
last.year <- max(species.data$Year,na.rm=TRUE)

species.data$Year <- factor(x = species.data$Year,levels = all.yrs)
```
In total, I have caught `r total.caught` individuals of Pale tussock, in `r n.traps` traps, between `r first.year` and `r last.year`.

```{r,echo=FALSE,results=TRUE}
yr.avg.catches <- tapply(X = species.data$NumberCaught,INDEX = species.data$Year,FUN = mean)
yr.avg.catches[is.na(yr.avg.catches)] <- 0

par(mar=c(3.2,3.2,0.5,0.2))
par(mgp=c(2.0,0.2,0))
par(tck=-0.01)
par(las=1)

plot(all.yrs,yr.avg.catches,xaxp=c(min(all.yrs),max(all.yrs),length(all.yrs)-1),xlab="Year",ylab="Average Catch",pch=16,type="b",col="#A40122",bty="l")

dens <- hist(species.data$Month,breaks=seq(from=0.5,to=12.5,by=1),plot=FALSE)
      
barplot(dens$density,space = 0,border=NA,col="#A40122",ylab="Density")
axis(side = 1,at = seq(from=0.5,to=11.5,length.out=12),labels = month.abb)
title(xlab="Month")
box(bty="l")
```

## Pebble hook-tip

```{r,echo=FALSE,results=TRUE}
species.data <- moth.data[which(moth.data$Species=="Pebble hook-tip"),]
total.caught <- sum(species.data$NumberCaught,na.rm=TRUE)
n.traps <- length(unique(species.data$TrapNumber))
first.year <- min(species.data$Year,na.rm=TRUE)
last.year <- max(species.data$Year,na.rm=TRUE)

species.data$Year <- factor(x = species.data$Year,levels = all.yrs)
```
In total, I have caught `r total.caught` individuals of Pebble hook-tip, in `r n.traps` traps, between `r first.year` and `r last.year`.

```{r,echo=FALSE,results=TRUE}
yr.avg.catches <- tapply(X = species.data$NumberCaught,INDEX = species.data$Year,FUN = mean)
yr.avg.catches[is.na(yr.avg.catches)] <- 0

par(mar=c(3.2,3.2,0.5,0.2))
par(mgp=c(2.0,0.2,0))
par(tck=-0.01)
par(las=1)

plot(all.yrs,yr.avg.catches,xaxp=c(min(all.yrs),max(all.yrs),length(all.yrs)-1),xlab="Year",ylab="Average Catch",pch=16,type="b",col="#A40122",bty="l")

dens <- hist(species.data$Month,breaks=seq(from=0.5,to=12.5,by=1),plot=FALSE)
      
barplot(dens$density,space = 0,border=NA,col="#A40122",ylab="Density")
axis(side = 1,at = seq(from=0.5,to=11.5,length.out=12),labels = month.abb)
title(xlab="Month")
box(bty="l")
```

## Pebble prominent

```{r,echo=FALSE,results=TRUE}
species.data <- moth.data[which(moth.data$Species=="Pebble prominent"),]
total.caught <- sum(species.data$NumberCaught,na.rm=TRUE)
n.traps <- length(unique(species.data$TrapNumber))
first.year <- min(species.data$Year,na.rm=TRUE)
last.year <- max(species.data$Year,na.rm=TRUE)

species.data$Year <- factor(x = species.data$Year,levels = all.yrs)
```
In total, I have caught `r total.caught` individuals of Pebble prominent, in `r n.traps` traps, between `r first.year` and `r last.year`.

```{r,echo=FALSE,results=TRUE}
yr.avg.catches <- tapply(X = species.data$NumberCaught,INDEX = species.data$Year,FUN = mean)
yr.avg.catches[is.na(yr.avg.catches)] <- 0

par(mar=c(3.2,3.2,0.5,0.2))
par(mgp=c(2.0,0.2,0))
par(tck=-0.01)
par(las=1)

plot(all.yrs,yr.avg.catches,xaxp=c(min(all.yrs),max(all.yrs),length(all.yrs)-1),xlab="Year",ylab="Average Catch",pch=16,type="b",col="#A40122",bty="l")

dens <- hist(species.data$Month,breaks=seq(from=0.5,to=12.5,by=1),plot=FALSE)
      
barplot(dens$density,space = 0,border=NA,col="#A40122",ylab="Density")
axis(side = 1,at = seq(from=0.5,to=11.5,length.out=12),labels = month.abb)
title(xlab="Month")
box(bty="l")
```

## Peppered moth

```{r,echo=FALSE,results=TRUE}
species.data <- moth.data[which(moth.data$Species=="Peppered moth"),]
total.caught <- sum(species.data$NumberCaught,na.rm=TRUE)
n.traps <- length(unique(species.data$TrapNumber))
first.year <- min(species.data$Year,na.rm=TRUE)
last.year <- max(species.data$Year,na.rm=TRUE)

species.data$Year <- factor(x = species.data$Year,levels = all.yrs)
```
In total, I have caught `r total.caught` individuals of Peppered moth, in `r n.traps` traps, between `r first.year` and `r last.year`.

```{r,echo=FALSE,results=TRUE}
yr.avg.catches <- tapply(X = species.data$NumberCaught,INDEX = species.data$Year,FUN = mean)
yr.avg.catches[is.na(yr.avg.catches)] <- 0

par(mar=c(3.2,3.2,0.5,0.2))
par(mgp=c(2.0,0.2,0))
par(tck=-0.01)
par(las=1)

plot(all.yrs,yr.avg.catches,xaxp=c(min(all.yrs),max(all.yrs),length(all.yrs)-1),xlab="Year",ylab="Average Catch",pch=16,type="b",col="#A40122",bty="l")

dens <- hist(species.data$Month,breaks=seq(from=0.5,to=12.5,by=1),plot=FALSE)
      
barplot(dens$density,space = 0,border=NA,col="#A40122",ylab="Density")
axis(side = 1,at = seq(from=0.5,to=11.5,length.out=12),labels = month.abb)
title(xlab="Month")
box(bty="l")
```

## Pine hawkmoth

```{r,echo=FALSE,results=TRUE}
species.data <- moth.data[which(moth.data$Species=="Pine hawkmoth"),]
total.caught <- sum(species.data$NumberCaught,na.rm=TRUE)
n.traps <- length(unique(species.data$TrapNumber))
first.year <- min(species.data$Year,na.rm=TRUE)
last.year <- max(species.data$Year,na.rm=TRUE)

species.data$Year <- factor(x = species.data$Year,levels = all.yrs)
```
In total, I have caught `r total.caught` individuals of Pine hawkmoth, in `r n.traps` traps, between `r first.year` and `r last.year`.

```{r,echo=FALSE,results=TRUE}
yr.avg.catches <- tapply(X = species.data$NumberCaught,INDEX = species.data$Year,FUN = mean)
yr.avg.catches[is.na(yr.avg.catches)] <- 0

par(mar=c(3.2,3.2,0.5,0.2))
par(mgp=c(2.0,0.2,0))
par(tck=-0.01)
par(las=1)

plot(all.yrs,yr.avg.catches,xaxp=c(min(all.yrs),max(all.yrs),length(all.yrs)-1),xlab="Year",ylab="Average Catch",pch=16,type="b",col="#A40122",bty="l")

dens <- hist(species.data$Month,breaks=seq(from=0.5,to=12.5,by=1),plot=FALSE)
      
barplot(dens$density,space = 0,border=NA,col="#A40122",ylab="Density")
axis(side = 1,at = seq(from=0.5,to=11.5,length.out=12),labels = month.abb)
title(xlab="Month")
box(bty="l")
```

## Poplar grey

```{r,echo=FALSE,results=TRUE}
species.data <- moth.data[which(moth.data$Species=="Poplar grey"),]
total.caught <- sum(species.data$NumberCaught,na.rm=TRUE)
n.traps <- length(unique(species.data$TrapNumber))
first.year <- min(species.data$Year,na.rm=TRUE)
last.year <- max(species.data$Year,na.rm=TRUE)

species.data$Year <- factor(x = species.data$Year,levels = all.yrs)
```
In total, I have caught `r total.caught` individuals of Poplar grey, in `r n.traps` traps, between `r first.year` and `r last.year`.

```{r,echo=FALSE,results=TRUE}
yr.avg.catches <- tapply(X = species.data$NumberCaught,INDEX = species.data$Year,FUN = mean)
yr.avg.catches[is.na(yr.avg.catches)] <- 0

par(mar=c(3.2,3.2,0.5,0.2))
par(mgp=c(2.0,0.2,0))
par(tck=-0.01)
par(las=1)

plot(all.yrs,yr.avg.catches,xaxp=c(min(all.yrs),max(all.yrs),length(all.yrs)-1),xlab="Year",ylab="Average Catch",pch=16,type="b",col="#A40122",bty="l")

dens <- hist(species.data$Month,breaks=seq(from=0.5,to=12.5,by=1),plot=FALSE)
      
barplot(dens$density,space = 0,border=NA,col="#A40122",ylab="Density")
axis(side = 1,at = seq(from=0.5,to=11.5,length.out=12),labels = month.abb)
title(xlab="Month")
box(bty="l")
```

## Poplar hawkmoth

```{r,echo=FALSE,results=TRUE}
species.data <- moth.data[which(moth.data$Species=="Poplar hawkmoth"),]
total.caught <- sum(species.data$NumberCaught,na.rm=TRUE)
n.traps <- length(unique(species.data$TrapNumber))
first.year <- min(species.data$Year,na.rm=TRUE)
last.year <- max(species.data$Year,na.rm=TRUE)

species.data$Year <- factor(x = species.data$Year,levels = all.yrs)
```
In total, I have caught `r total.caught` individuals of Poplar hawkmoth, in `r n.traps` traps, between `r first.year` and `r last.year`.

```{r,echo=FALSE,results=TRUE}
yr.avg.catches <- tapply(X = species.data$NumberCaught,INDEX = species.data$Year,FUN = mean)
yr.avg.catches[is.na(yr.avg.catches)] <- 0

par(mar=c(3.2,3.2,0.5,0.2))
par(mgp=c(2.0,0.2,0))
par(tck=-0.01)
par(las=1)

plot(all.yrs,yr.avg.catches,xaxp=c(min(all.yrs),max(all.yrs),length(all.yrs)-1),xlab="Year",ylab="Average Catch",pch=16,type="b",col="#A40122",bty="l")

dens <- hist(species.data$Month,breaks=seq(from=0.5,to=12.5,by=1),plot=FALSE)
      
barplot(dens$density,space = 0,border=NA,col="#A40122",ylab="Density")
axis(side = 1,at = seq(from=0.5,to=11.5,length.out=12),labels = month.abb)
title(xlab="Month")
box(bty="l")
```

## Powdered Quaker

```{r,echo=FALSE,results=TRUE}
species.data <- moth.data[which(moth.data$Species=="Powdered Quaker"),]
total.caught <- sum(species.data$NumberCaught,na.rm=TRUE)
n.traps <- length(unique(species.data$TrapNumber))
first.year <- min(species.data$Year,na.rm=TRUE)
last.year <- max(species.data$Year,na.rm=TRUE)

species.data$Year <- factor(x = species.data$Year,levels = all.yrs)
```
In total, I have caught `r total.caught` individuals of Powdered Quaker, in `r n.traps` traps, between `r first.year` and `r last.year`.

```{r,echo=FALSE,results=TRUE}
yr.avg.catches <- tapply(X = species.data$NumberCaught,INDEX = species.data$Year,FUN = mean)
yr.avg.catches[is.na(yr.avg.catches)] <- 0

par(mar=c(3.2,3.2,0.5,0.2))
par(mgp=c(2.0,0.2,0))
par(tck=-0.01)
par(las=1)

plot(all.yrs,yr.avg.catches,xaxp=c(min(all.yrs),max(all.yrs),length(all.yrs)-1),xlab="Year",ylab="Average Catch",pch=16,type="b",col="#A40122",bty="l")

dens <- hist(species.data$Month,breaks=seq(from=0.5,to=12.5,by=1),plot=FALSE)
      
barplot(dens$density,space = 0,border=NA,col="#A40122",ylab="Density")
axis(side = 1,at = seq(from=0.5,to=11.5,length.out=12),labels = month.abb)
title(xlab="Month")
box(bty="l")
```

## Privet hawkmoth

```{r,echo=FALSE,results=TRUE}
species.data <- moth.data[which(moth.data$Species=="Privet hawkmoth"),]
total.caught <- sum(species.data$NumberCaught,na.rm=TRUE)
n.traps <- length(unique(species.data$TrapNumber))
first.year <- min(species.data$Year,na.rm=TRUE)
last.year <- max(species.data$Year,na.rm=TRUE)

species.data$Year <- factor(x = species.data$Year,levels = all.yrs)
```
In total, I have caught `r total.caught` individuals of Privet hawkmoth, in `r n.traps` traps, between `r first.year` and `r last.year`.

```{r,echo=FALSE,results=TRUE}
yr.avg.catches <- tapply(X = species.data$NumberCaught,INDEX = species.data$Year,FUN = mean)
yr.avg.catches[is.na(yr.avg.catches)] <- 0

par(mar=c(3.2,3.2,0.5,0.2))
par(mgp=c(2.0,0.2,0))
par(tck=-0.01)
par(las=1)

plot(all.yrs,yr.avg.catches,xaxp=c(min(all.yrs),max(all.yrs),length(all.yrs)-1),xlab="Year",ylab="Average Catch",pch=16,type="b",col="#A40122",bty="l")

dens <- hist(species.data$Month,breaks=seq(from=0.5,to=12.5,by=1),plot=FALSE)
      
barplot(dens$density,space = 0,border=NA,col="#A40122",ylab="Density")
axis(side = 1,at = seq(from=0.5,to=11.5,length.out=12),labels = month.abb)
title(xlab="Month")
box(bty="l")
```

## Purple bar

```{r,echo=FALSE,results=TRUE}
species.data <- moth.data[which(moth.data$Species=="Purple bar"),]
total.caught <- sum(species.data$NumberCaught,na.rm=TRUE)
n.traps <- length(unique(species.data$TrapNumber))
first.year <- min(species.data$Year,na.rm=TRUE)
last.year <- max(species.data$Year,na.rm=TRUE)

species.data$Year <- factor(x = species.data$Year,levels = all.yrs)
```
In total, I have caught `r total.caught` individuals of Purple bar, in `r n.traps` traps, between `r first.year` and `r last.year`.

```{r,echo=FALSE,results=TRUE}
yr.avg.catches <- tapply(X = species.data$NumberCaught,INDEX = species.data$Year,FUN = mean)
yr.avg.catches[is.na(yr.avg.catches)] <- 0

par(mar=c(3.2,3.2,0.5,0.2))
par(mgp=c(2.0,0.2,0))
par(tck=-0.01)
par(las=1)

plot(all.yrs,yr.avg.catches,xaxp=c(min(all.yrs),max(all.yrs),length(all.yrs)-1),xlab="Year",ylab="Average Catch",pch=16,type="b",col="#A40122",bty="l")

dens <- hist(species.data$Month,breaks=seq(from=0.5,to=12.5,by=1),plot=FALSE)
      
barplot(dens$density,space = 0,border=NA,col="#A40122",ylab="Density")
axis(side = 1,at = seq(from=0.5,to=11.5,length.out=12),labels = month.abb)
title(xlab="Month")
box(bty="l")
```

## Red-green carpet

```{r,echo=FALSE,results=TRUE}
species.data <- moth.data[which(moth.data$Species=="Red-green carpet"),]
total.caught <- sum(species.data$NumberCaught,na.rm=TRUE)
n.traps <- length(unique(species.data$TrapNumber))
first.year <- min(species.data$Year,na.rm=TRUE)
last.year <- max(species.data$Year,na.rm=TRUE)

species.data$Year <- factor(x = species.data$Year,levels = all.yrs)
```
In total, I have caught `r total.caught` individuals of Red-green carpet, in `r n.traps` traps, between `r first.year` and `r last.year`.

```{r,echo=FALSE,results=TRUE}
yr.avg.catches <- tapply(X = species.data$NumberCaught,INDEX = species.data$Year,FUN = mean)
yr.avg.catches[is.na(yr.avg.catches)] <- 0

par(mar=c(3.2,3.2,0.5,0.2))
par(mgp=c(2.0,0.2,0))
par(tck=-0.01)
par(las=1)

plot(all.yrs,yr.avg.catches,xaxp=c(min(all.yrs),max(all.yrs),length(all.yrs)-1),xlab="Year",ylab="Average Catch",pch=16,type="b",col="#A40122",bty="l")

dens <- hist(species.data$Month,breaks=seq(from=0.5,to=12.5,by=1),plot=FALSE)
      
barplot(dens$density,space = 0,border=NA,col="#A40122",ylab="Density")
axis(side = 1,at = seq(from=0.5,to=11.5,length.out=12),labels = month.abb)
title(xlab="Month")
box(bty="l")
```

## Red-line Quaker

```{r,echo=FALSE,results=TRUE}
species.data <- moth.data[which(moth.data$Species=="Red-line Quaker"),]
total.caught <- sum(species.data$NumberCaught,na.rm=TRUE)
n.traps <- length(unique(species.data$TrapNumber))
first.year <- min(species.data$Year,na.rm=TRUE)
last.year <- max(species.data$Year,na.rm=TRUE)

species.data$Year <- factor(x = species.data$Year,levels = all.yrs)
```
In total, I have caught `r total.caught` individuals of Red-line Quaker, in `r n.traps` traps, between `r first.year` and `r last.year`.

```{r,echo=FALSE,results=TRUE}
yr.avg.catches <- tapply(X = species.data$NumberCaught,INDEX = species.data$Year,FUN = mean)
yr.avg.catches[is.na(yr.avg.catches)] <- 0

par(mar=c(3.2,3.2,0.5,0.2))
par(mgp=c(2.0,0.2,0))
par(tck=-0.01)
par(las=1)

plot(all.yrs,yr.avg.catches,xaxp=c(min(all.yrs),max(all.yrs),length(all.yrs)-1),xlab="Year",ylab="Average Catch",pch=16,type="b",col="#A40122",bty="l")

dens <- hist(species.data$Month,breaks=seq(from=0.5,to=12.5,by=1),plot=FALSE)
      
barplot(dens$density,space = 0,border=NA,col="#A40122",ylab="Density")
axis(side = 1,at = seq(from=0.5,to=11.5,length.out=12),labels = month.abb)
title(xlab="Month")
box(bty="l")
```

## Red chestnut

```{r,echo=FALSE,results=TRUE}
species.data <- moth.data[which(moth.data$Species=="Red chestnut"),]
total.caught <- sum(species.data$NumberCaught,na.rm=TRUE)
n.traps <- length(unique(species.data$TrapNumber))
first.year <- min(species.data$Year,na.rm=TRUE)
last.year <- max(species.data$Year,na.rm=TRUE)

species.data$Year <- factor(x = species.data$Year,levels = all.yrs)
```
In total, I have caught `r total.caught` individuals of Red chestnut, in `r n.traps` traps, between `r first.year` and `r last.year`.

```{r,echo=FALSE,results=TRUE}
yr.avg.catches <- tapply(X = species.data$NumberCaught,INDEX = species.data$Year,FUN = mean)
yr.avg.catches[is.na(yr.avg.catches)] <- 0

par(mar=c(3.2,3.2,0.5,0.2))
par(mgp=c(2.0,0.2,0))
par(tck=-0.01)
par(las=1)

plot(all.yrs,yr.avg.catches,xaxp=c(min(all.yrs),max(all.yrs),length(all.yrs)-1),xlab="Year",ylab="Average Catch",pch=16,type="b",col="#A40122",bty="l")

dens <- hist(species.data$Month,breaks=seq(from=0.5,to=12.5,by=1),plot=FALSE)
      
barplot(dens$density,space = 0,border=NA,col="#A40122",ylab="Density")
axis(side = 1,at = seq(from=0.5,to=11.5,length.out=12),labels = month.abb)
title(xlab="Month")
box(bty="l")
```

## Red twin-spot carpet

```{r,echo=FALSE,results=TRUE}
species.data <- moth.data[which(moth.data$Species=="Red twin-spot carpet"),]
total.caught <- sum(species.data$NumberCaught,na.rm=TRUE)
n.traps <- length(unique(species.data$TrapNumber))
first.year <- min(species.data$Year,na.rm=TRUE)
last.year <- max(species.data$Year,na.rm=TRUE)

species.data$Year <- factor(x = species.data$Year,levels = all.yrs)
```
In total, I have caught `r total.caught` individuals of Red twin-spot carpet, in `r n.traps` traps, between `r first.year` and `r last.year`.

```{r,echo=FALSE,results=TRUE}
yr.avg.catches <- tapply(X = species.data$NumberCaught,INDEX = species.data$Year,FUN = mean)
yr.avg.catches[is.na(yr.avg.catches)] <- 0

par(mar=c(3.2,3.2,0.5,0.2))
par(mgp=c(2.0,0.2,0))
par(tck=-0.01)
par(las=1)

plot(all.yrs,yr.avg.catches,xaxp=c(min(all.yrs),max(all.yrs),length(all.yrs)-1),xlab="Year",ylab="Average Catch",pch=16,type="b",col="#A40122",bty="l")

dens <- hist(species.data$Month,breaks=seq(from=0.5,to=12.5,by=1),plot=FALSE)
      
barplot(dens$density,space = 0,border=NA,col="#A40122",ylab="Density")
axis(side = 1,at = seq(from=0.5,to=11.5,length.out=12),labels = month.abb)
title(xlab="Month")
box(bty="l")
```

## Red/dark-barred twin-spot carpet

```{r,echo=FALSE,results=TRUE}
species.data <- moth.data[which(moth.data$Species=="Red/dark-barred twin-spot carpet"),]
total.caught <- sum(species.data$NumberCaught,na.rm=TRUE)
n.traps <- length(unique(species.data$TrapNumber))
first.year <- min(species.data$Year,na.rm=TRUE)
last.year <- max(species.data$Year,na.rm=TRUE)

species.data$Year <- factor(x = species.data$Year,levels = all.yrs)
```
In total, I have caught `r total.caught` individuals of Red/dark-barred twin-spot carpet, in `r n.traps` traps, between `r first.year` and `r last.year`.

```{r,echo=FALSE,results=TRUE}
yr.avg.catches <- tapply(X = species.data$NumberCaught,INDEX = species.data$Year,FUN = mean)
yr.avg.catches[is.na(yr.avg.catches)] <- 0

par(mar=c(3.2,3.2,0.5,0.2))
par(mgp=c(2.0,0.2,0))
par(tck=-0.01)
par(las=1)

plot(all.yrs,yr.avg.catches,xaxp=c(min(all.yrs),max(all.yrs),length(all.yrs)-1),xlab="Year",ylab="Average Catch",pch=16,type="b",col="#A40122",bty="l")

dens <- hist(species.data$Month,breaks=seq(from=0.5,to=12.5,by=1),plot=FALSE)
      
barplot(dens$density,space = 0,border=NA,col="#A40122",ylab="Density")
axis(side = 1,at = seq(from=0.5,to=11.5,length.out=12),labels = month.abb)
title(xlab="Month")
box(bty="l")
```

## Riband wave

```{r,echo=FALSE,results=TRUE}
species.data <- moth.data[which(moth.data$Species=="Riband wave"),]
total.caught <- sum(species.data$NumberCaught,na.rm=TRUE)
n.traps <- length(unique(species.data$TrapNumber))
first.year <- min(species.data$Year,na.rm=TRUE)
last.year <- max(species.data$Year,na.rm=TRUE)

species.data$Year <- factor(x = species.data$Year,levels = all.yrs)
```
In total, I have caught `r total.caught` individuals of Riband wave, in `r n.traps` traps, between `r first.year` and `r last.year`.

```{r,echo=FALSE,results=TRUE}
yr.avg.catches <- tapply(X = species.data$NumberCaught,INDEX = species.data$Year,FUN = mean)
yr.avg.catches[is.na(yr.avg.catches)] <- 0

par(mar=c(3.2,3.2,0.5,0.2))
par(mgp=c(2.0,0.2,0))
par(tck=-0.01)
par(las=1)

plot(all.yrs,yr.avg.catches,xaxp=c(min(all.yrs),max(all.yrs),length(all.yrs)-1),xlab="Year",ylab="Average Catch",pch=16,type="b",col="#A40122",bty="l")

dens <- hist(species.data$Month,breaks=seq(from=0.5,to=12.5,by=1),plot=FALSE)
      
barplot(dens$density,space = 0,border=NA,col="#A40122",ylab="Density")
axis(side = 1,at = seq(from=0.5,to=11.5,length.out=12),labels = month.abb)
title(xlab="Month")
box(bty="l")
```

## Rosy rustic

```{r,echo=FALSE,results=TRUE}
species.data <- moth.data[which(moth.data$Species=="Rosy rustic"),]
total.caught <- sum(species.data$NumberCaught,na.rm=TRUE)
n.traps <- length(unique(species.data$TrapNumber))
first.year <- min(species.data$Year,na.rm=TRUE)
last.year <- max(species.data$Year,na.rm=TRUE)

species.data$Year <- factor(x = species.data$Year,levels = all.yrs)
```
In total, I have caught `r total.caught` individuals of Rosy rustic, in `r n.traps` traps, between `r first.year` and `r last.year`.

```{r,echo=FALSE,results=TRUE}
yr.avg.catches <- tapply(X = species.data$NumberCaught,INDEX = species.data$Year,FUN = mean)
yr.avg.catches[is.na(yr.avg.catches)] <- 0

par(mar=c(3.2,3.2,0.5,0.2))
par(mgp=c(2.0,0.2,0))
par(tck=-0.01)
par(las=1)

plot(all.yrs,yr.avg.catches,xaxp=c(min(all.yrs),max(all.yrs),length(all.yrs)-1),xlab="Year",ylab="Average Catch",pch=16,type="b",col="#A40122",bty="l")

dens <- hist(species.data$Month,breaks=seq(from=0.5,to=12.5,by=1),plot=FALSE)
      
barplot(dens$density,space = 0,border=NA,col="#A40122",ylab="Density")
axis(side = 1,at = seq(from=0.5,to=11.5,length.out=12),labels = month.abb)
title(xlab="Month")
box(bty="l")
```

## Ruby tiger

```{r,echo=FALSE,results=TRUE}
species.data <- moth.data[which(moth.data$Species=="Ruby tiger"),]
total.caught <- sum(species.data$NumberCaught,na.rm=TRUE)
n.traps <- length(unique(species.data$TrapNumber))
first.year <- min(species.data$Year,na.rm=TRUE)
last.year <- max(species.data$Year,na.rm=TRUE)

species.data$Year <- factor(x = species.data$Year,levels = all.yrs)
```
In total, I have caught `r total.caught` individuals of Ruby tiger, in `r n.traps` traps, between `r first.year` and `r last.year`.

```{r,echo=FALSE,results=TRUE}
yr.avg.catches <- tapply(X = species.data$NumberCaught,INDEX = species.data$Year,FUN = mean)
yr.avg.catches[is.na(yr.avg.catches)] <- 0

par(mar=c(3.2,3.2,0.5,0.2))
par(mgp=c(2.0,0.2,0))
par(tck=-0.01)
par(las=1)

plot(all.yrs,yr.avg.catches,xaxp=c(min(all.yrs),max(all.yrs),length(all.yrs)-1),xlab="Year",ylab="Average Catch",pch=16,type="b",col="#A40122",bty="l")

dens <- hist(species.data$Month,breaks=seq(from=0.5,to=12.5,by=1),plot=FALSE)
      
barplot(dens$density,space = 0,border=NA,col="#A40122",ylab="Density")
axis(side = 1,at = seq(from=0.5,to=11.5,length.out=12),labels = month.abb)
title(xlab="Month")
box(bty="l")
```

## Rustic

```{r,echo=FALSE,results=TRUE}
species.data <- moth.data[which(moth.data$Species=="Rustic"),]
total.caught <- sum(species.data$NumberCaught,na.rm=TRUE)
n.traps <- length(unique(species.data$TrapNumber))
first.year <- min(species.data$Year,na.rm=TRUE)
last.year <- max(species.data$Year,na.rm=TRUE)

species.data$Year <- factor(x = species.data$Year,levels = all.yrs)
```
In total, I have caught `r total.caught` individuals of Rustic, in `r n.traps` traps, between `r first.year` and `r last.year`.

```{r,echo=FALSE,results=TRUE}
yr.avg.catches <- tapply(X = species.data$NumberCaught,INDEX = species.data$Year,FUN = mean)
yr.avg.catches[is.na(yr.avg.catches)] <- 0

par(mar=c(3.2,3.2,0.5,0.2))
par(mgp=c(2.0,0.2,0))
par(tck=-0.01)
par(las=1)

plot(all.yrs,yr.avg.catches,xaxp=c(min(all.yrs),max(all.yrs),length(all.yrs)-1),xlab="Year",ylab="Average Catch",pch=16,type="b",col="#A40122",bty="l")

dens <- hist(species.data$Month,breaks=seq(from=0.5,to=12.5,by=1),plot=FALSE)
      
barplot(dens$density,space = 0,border=NA,col="#A40122",ylab="Density")
axis(side = 1,at = seq(from=0.5,to=11.5,length.out=12),labels = month.abb)
title(xlab="Month")
box(bty="l")
```

## Rustic shoulder-knot

```{r,echo=FALSE,results=TRUE}
species.data <- moth.data[which(moth.data$Species=="Rustic shoulder-knot"),]
total.caught <- sum(species.data$NumberCaught,na.rm=TRUE)
n.traps <- length(unique(species.data$TrapNumber))
first.year <- min(species.data$Year,na.rm=TRUE)
last.year <- max(species.data$Year,na.rm=TRUE)

species.data$Year <- factor(x = species.data$Year,levels = all.yrs)
```
In total, I have caught `r total.caught` individuals of Rustic shoulder-knot, in `r n.traps` traps, between `r first.year` and `r last.year`.

```{r,echo=FALSE,results=TRUE}
yr.avg.catches <- tapply(X = species.data$NumberCaught,INDEX = species.data$Year,FUN = mean)
yr.avg.catches[is.na(yr.avg.catches)] <- 0

par(mar=c(3.2,3.2,0.5,0.2))
par(mgp=c(2.0,0.2,0))
par(tck=-0.01)
par(las=1)

plot(all.yrs,yr.avg.catches,xaxp=c(min(all.yrs),max(all.yrs),length(all.yrs)-1),xlab="Year",ylab="Average Catch",pch=16,type="b",col="#A40122",bty="l")

dens <- hist(species.data$Month,breaks=seq(from=0.5,to=12.5,by=1),plot=FALSE)
      
barplot(dens$density,space = 0,border=NA,col="#A40122",ylab="Density")
axis(side = 1,at = seq(from=0.5,to=11.5,length.out=12),labels = month.abb)
title(xlab="Month")
box(bty="l")
```

## Satellite

```{r,echo=FALSE,results=TRUE}
species.data <- moth.data[which(moth.data$Species=="Satellite"),]
total.caught <- sum(species.data$NumberCaught,na.rm=TRUE)
n.traps <- length(unique(species.data$TrapNumber))
first.year <- min(species.data$Year,na.rm=TRUE)
last.year <- max(species.data$Year,na.rm=TRUE)

species.data$Year <- factor(x = species.data$Year,levels = all.yrs)
```
In total, I have caught `r total.caught` individuals of Satellite, in `r n.traps` traps, between `r first.year` and `r last.year`.

```{r,echo=FALSE,results=TRUE}
yr.avg.catches <- tapply(X = species.data$NumberCaught,INDEX = species.data$Year,FUN = mean)
yr.avg.catches[is.na(yr.avg.catches)] <- 0

par(mar=c(3.2,3.2,0.5,0.2))
par(mgp=c(2.0,0.2,0))
par(tck=-0.01)
par(las=1)

plot(all.yrs,yr.avg.catches,xaxp=c(min(all.yrs),max(all.yrs),length(all.yrs)-1),xlab="Year",ylab="Average Catch",pch=16,type="b",col="#A40122",bty="l")

dens <- hist(species.data$Month,breaks=seq(from=0.5,to=12.5,by=1),plot=FALSE)
      
barplot(dens$density,space = 0,border=NA,col="#A40122",ylab="Density")
axis(side = 1,at = seq(from=0.5,to=11.5,length.out=12),labels = month.abb)
title(xlab="Month")
box(bty="l")
```

## Scalloped Oak

```{r,echo=FALSE,results=TRUE}
species.data <- moth.data[which(moth.data$Species=="Scalloped Oak"),]
total.caught <- sum(species.data$NumberCaught,na.rm=TRUE)
n.traps <- length(unique(species.data$TrapNumber))
first.year <- min(species.data$Year,na.rm=TRUE)
last.year <- max(species.data$Year,na.rm=TRUE)

species.data$Year <- factor(x = species.data$Year,levels = all.yrs)
```
In total, I have caught `r total.caught` individuals of Scalloped Oak, in `r n.traps` traps, between `r first.year` and `r last.year`.

```{r,echo=FALSE,results=TRUE}
yr.avg.catches <- tapply(X = species.data$NumberCaught,INDEX = species.data$Year,FUN = mean)
yr.avg.catches[is.na(yr.avg.catches)] <- 0

par(mar=c(3.2,3.2,0.5,0.2))
par(mgp=c(2.0,0.2,0))
par(tck=-0.01)
par(las=1)

plot(all.yrs,yr.avg.catches,xaxp=c(min(all.yrs),max(all.yrs),length(all.yrs)-1),xlab="Year",ylab="Average Catch",pch=16,type="b",col="#A40122",bty="l")

dens <- hist(species.data$Month,breaks=seq(from=0.5,to=12.5,by=1),plot=FALSE)
      
barplot(dens$density,space = 0,border=NA,col="#A40122",ylab="Density")
axis(side = 1,at = seq(from=0.5,to=11.5,length.out=12),labels = month.abb)
title(xlab="Month")
box(bty="l")
```

## Scarce footman

```{r,echo=FALSE,results=TRUE}
species.data <- moth.data[which(moth.data$Species=="Scarce footman"),]
total.caught <- sum(species.data$NumberCaught,na.rm=TRUE)
n.traps <- length(unique(species.data$TrapNumber))
first.year <- min(species.data$Year,na.rm=TRUE)
last.year <- max(species.data$Year,na.rm=TRUE)

species.data$Year <- factor(x = species.data$Year,levels = all.yrs)
```
In total, I have caught `r total.caught` individuals of Scarce footman, in `r n.traps` traps, between `r first.year` and `r last.year`.

```{r,echo=FALSE,results=TRUE}
yr.avg.catches <- tapply(X = species.data$NumberCaught,INDEX = species.data$Year,FUN = mean)
yr.avg.catches[is.na(yr.avg.catches)] <- 0

par(mar=c(3.2,3.2,0.5,0.2))
par(mgp=c(2.0,0.2,0))
par(tck=-0.01)
par(las=1)

plot(all.yrs,yr.avg.catches,xaxp=c(min(all.yrs),max(all.yrs),length(all.yrs)-1),xlab="Year",ylab="Average Catch",pch=16,type="b",col="#A40122",bty="l")

dens <- hist(species.data$Month,breaks=seq(from=0.5,to=12.5,by=1),plot=FALSE)
      
barplot(dens$density,space = 0,border=NA,col="#A40122",ylab="Density")
axis(side = 1,at = seq(from=0.5,to=11.5,length.out=12),labels = month.abb)
title(xlab="Month")
box(bty="l")
```

## Scarlet tiger

```{r,echo=FALSE,results=TRUE}
species.data <- moth.data[which(moth.data$Species=="Scarlet tiger"),]
total.caught <- sum(species.data$NumberCaught,na.rm=TRUE)
n.traps <- length(unique(species.data$TrapNumber))
first.year <- min(species.data$Year,na.rm=TRUE)
last.year <- max(species.data$Year,na.rm=TRUE)

species.data$Year <- factor(x = species.data$Year,levels = all.yrs)
```
In total, I have caught `r total.caught` individuals of Scarlet tiger, in `r n.traps` traps, between `r first.year` and `r last.year`.

```{r,echo=FALSE,results=TRUE}
yr.avg.catches <- tapply(X = species.data$NumberCaught,INDEX = species.data$Year,FUN = mean)
yr.avg.catches[is.na(yr.avg.catches)] <- 0

par(mar=c(3.2,3.2,0.5,0.2))
par(mgp=c(2.0,0.2,0))
par(tck=-0.01)
par(las=1)

plot(all.yrs,yr.avg.catches,xaxp=c(min(all.yrs),max(all.yrs),length(all.yrs)-1),xlab="Year",ylab="Average Catch",pch=16,type="b",col="#A40122",bty="l")

dens <- hist(species.data$Month,breaks=seq(from=0.5,to=12.5,by=1),plot=FALSE)
      
barplot(dens$density,space = 0,border=NA,col="#A40122",ylab="Density")
axis(side = 1,at = seq(from=0.5,to=11.5,length.out=12),labels = month.abb)
title(xlab="Month")
box(bty="l")
```

## Scorched carpet

```{r,echo=FALSE,results=TRUE}
species.data <- moth.data[which(moth.data$Species=="Scorched carpet"),]
total.caught <- sum(species.data$NumberCaught,na.rm=TRUE)
n.traps <- length(unique(species.data$TrapNumber))
first.year <- min(species.data$Year,na.rm=TRUE)
last.year <- max(species.data$Year,na.rm=TRUE)

species.data$Year <- factor(x = species.data$Year,levels = all.yrs)
```
In total, I have caught `r total.caught` individuals of Scorched carpet, in `r n.traps` traps, between `r first.year` and `r last.year`.

```{r,echo=FALSE,results=TRUE}
yr.avg.catches <- tapply(X = species.data$NumberCaught,INDEX = species.data$Year,FUN = mean)
yr.avg.catches[is.na(yr.avg.catches)] <- 0

par(mar=c(3.2,3.2,0.5,0.2))
par(mgp=c(2.0,0.2,0))
par(tck=-0.01)
par(las=1)

plot(all.yrs,yr.avg.catches,xaxp=c(min(all.yrs),max(all.yrs),length(all.yrs)-1),xlab="Year",ylab="Average Catch",pch=16,type="b",col="#A40122",bty="l")

dens <- hist(species.data$Month,breaks=seq(from=0.5,to=12.5,by=1),plot=FALSE)
      
barplot(dens$density,space = 0,border=NA,col="#A40122",ylab="Density")
axis(side = 1,at = seq(from=0.5,to=11.5,length.out=12),labels = month.abb)
title(xlab="Month")
box(bty="l")
```

## Setaceous Hebrew character

```{r,echo=FALSE,results=TRUE}
species.data <- moth.data[which(moth.data$Species=="Setaceous Hebrew character"),]
total.caught <- sum(species.data$NumberCaught,na.rm=TRUE)
n.traps <- length(unique(species.data$TrapNumber))
first.year <- min(species.data$Year,na.rm=TRUE)
last.year <- max(species.data$Year,na.rm=TRUE)

species.data$Year <- factor(x = species.data$Year,levels = all.yrs)
```
In total, I have caught `r total.caught` individuals of Setaceous Hebrew character, in `r n.traps` traps, between `r first.year` and `r last.year`.

```{r,echo=FALSE,results=TRUE}
yr.avg.catches <- tapply(X = species.data$NumberCaught,INDEX = species.data$Year,FUN = mean)
yr.avg.catches[is.na(yr.avg.catches)] <- 0

par(mar=c(3.2,3.2,0.5,0.2))
par(mgp=c(2.0,0.2,0))
par(tck=-0.01)
par(las=1)

plot(all.yrs,yr.avg.catches,xaxp=c(min(all.yrs),max(all.yrs),length(all.yrs)-1),xlab="Year",ylab="Average Catch",pch=16,type="b",col="#A40122",bty="l")

dens <- hist(species.data$Month,breaks=seq(from=0.5,to=12.5,by=1),plot=FALSE)
      
barplot(dens$density,space = 0,border=NA,col="#A40122",ylab="Density")
axis(side = 1,at = seq(from=0.5,to=11.5,length.out=12),labels = month.abb)
title(xlab="Month")
box(bty="l")
```

## Shaded broad-bar

```{r,echo=FALSE,results=TRUE}
species.data <- moth.data[which(moth.data$Species=="Shaded broad-bar"),]
total.caught <- sum(species.data$NumberCaught,na.rm=TRUE)
n.traps <- length(unique(species.data$TrapNumber))
first.year <- min(species.data$Year,na.rm=TRUE)
last.year <- max(species.data$Year,na.rm=TRUE)

species.data$Year <- factor(x = species.data$Year,levels = all.yrs)
```
In total, I have caught `r total.caught` individuals of Shaded broad-bar, in `r n.traps` traps, between `r first.year` and `r last.year`.

```{r,echo=FALSE,results=TRUE}
yr.avg.catches <- tapply(X = species.data$NumberCaught,INDEX = species.data$Year,FUN = mean)
yr.avg.catches[is.na(yr.avg.catches)] <- 0

par(mar=c(3.2,3.2,0.5,0.2))
par(mgp=c(2.0,0.2,0))
par(tck=-0.01)
par(las=1)

plot(all.yrs,yr.avg.catches,xaxp=c(min(all.yrs),max(all.yrs),length(all.yrs)-1),xlab="Year",ylab="Average Catch",pch=16,type="b",col="#A40122",bty="l")

dens <- hist(species.data$Month,breaks=seq(from=0.5,to=12.5,by=1),plot=FALSE)
      
barplot(dens$density,space = 0,border=NA,col="#A40122",ylab="Density")
axis(side = 1,at = seq(from=0.5,to=11.5,length.out=12),labels = month.abb)
title(xlab="Month")
box(bty="l")
```

## Shark

```{r,echo=FALSE,results=TRUE}
species.data <- moth.data[which(moth.data$Species=="Shark"),]
total.caught <- sum(species.data$NumberCaught,na.rm=TRUE)
n.traps <- length(unique(species.data$TrapNumber))
first.year <- min(species.data$Year,na.rm=TRUE)
last.year <- max(species.data$Year,na.rm=TRUE)

species.data$Year <- factor(x = species.data$Year,levels = all.yrs)
```
In total, I have caught `r total.caught` individuals of Shark, in `r n.traps` traps, between `r first.year` and `r last.year`.

```{r,echo=FALSE,results=TRUE}
yr.avg.catches <- tapply(X = species.data$NumberCaught,INDEX = species.data$Year,FUN = mean)
yr.avg.catches[is.na(yr.avg.catches)] <- 0

par(mar=c(3.2,3.2,0.5,0.2))
par(mgp=c(2.0,0.2,0))
par(tck=-0.01)
par(las=1)

plot(all.yrs,yr.avg.catches,xaxp=c(min(all.yrs),max(all.yrs),length(all.yrs)-1),xlab="Year",ylab="Average Catch",pch=16,type="b",col="#A40122",bty="l")

dens <- hist(species.data$Month,breaks=seq(from=0.5,to=12.5,by=1),plot=FALSE)
      
barplot(dens$density,space = 0,border=NA,col="#A40122",ylab="Density")
axis(side = 1,at = seq(from=0.5,to=11.5,length.out=12),labels = month.abb)
title(xlab="Month")
box(bty="l")
```

## Short-cloaked moth

```{r,echo=FALSE,results=TRUE}
species.data <- moth.data[which(moth.data$Species=="Short-cloaked moth"),]
total.caught <- sum(species.data$NumberCaught,na.rm=TRUE)
n.traps <- length(unique(species.data$TrapNumber))
first.year <- min(species.data$Year,na.rm=TRUE)
last.year <- max(species.data$Year,na.rm=TRUE)

species.data$Year <- factor(x = species.data$Year,levels = all.yrs)
```
In total, I have caught `r total.caught` individuals of Short-cloaked moth, in `r n.traps` traps, between `r first.year` and `r last.year`.

```{r,echo=FALSE,results=TRUE}
yr.avg.catches <- tapply(X = species.data$NumberCaught,INDEX = species.data$Year,FUN = mean)
yr.avg.catches[is.na(yr.avg.catches)] <- 0

par(mar=c(3.2,3.2,0.5,0.2))
par(mgp=c(2.0,0.2,0))
par(tck=-0.01)
par(las=1)

plot(all.yrs,yr.avg.catches,xaxp=c(min(all.yrs),max(all.yrs),length(all.yrs)-1),xlab="Year",ylab="Average Catch",pch=16,type="b",col="#A40122",bty="l")

dens <- hist(species.data$Month,breaks=seq(from=0.5,to=12.5,by=1),plot=FALSE)
      
barplot(dens$density,space = 0,border=NA,col="#A40122",ylab="Density")
axis(side = 1,at = seq(from=0.5,to=11.5,length.out=12),labels = month.abb)
title(xlab="Month")
box(bty="l")
```

## Shuttle-shaped dart

```{r,echo=FALSE,results=TRUE}
species.data <- moth.data[which(moth.data$Species=="Shuttle-shaped dart"),]
total.caught <- sum(species.data$NumberCaught,na.rm=TRUE)
n.traps <- length(unique(species.data$TrapNumber))
first.year <- min(species.data$Year,na.rm=TRUE)
last.year <- max(species.data$Year,na.rm=TRUE)

species.data$Year <- factor(x = species.data$Year,levels = all.yrs)
```
In total, I have caught `r total.caught` individuals of Shuttle-shaped dart, in `r n.traps` traps, between `r first.year` and `r last.year`.

```{r,echo=FALSE,results=TRUE}
yr.avg.catches <- tapply(X = species.data$NumberCaught,INDEX = species.data$Year,FUN = mean)
yr.avg.catches[is.na(yr.avg.catches)] <- 0

par(mar=c(3.2,3.2,0.5,0.2))
par(mgp=c(2.0,0.2,0))
par(tck=-0.01)
par(las=1)

plot(all.yrs,yr.avg.catches,xaxp=c(min(all.yrs),max(all.yrs),length(all.yrs)-1),xlab="Year",ylab="Average Catch",pch=16,type="b",col="#A40122",bty="l")

dens <- hist(species.data$Month,breaks=seq(from=0.5,to=12.5,by=1),plot=FALSE)
      
barplot(dens$density,space = 0,border=NA,col="#A40122",ylab="Density")
axis(side = 1,at = seq(from=0.5,to=11.5,length.out=12),labels = month.abb)
title(xlab="Month")
box(bty="l")
```

## Silver-ground carpet

```{r,echo=FALSE,results=TRUE}
species.data <- moth.data[which(moth.data$Species=="Silver-ground carpet"),]
total.caught <- sum(species.data$NumberCaught,na.rm=TRUE)
n.traps <- length(unique(species.data$TrapNumber))
first.year <- min(species.data$Year,na.rm=TRUE)
last.year <- max(species.data$Year,na.rm=TRUE)

species.data$Year <- factor(x = species.data$Year,levels = all.yrs)
```
In total, I have caught `r total.caught` individuals of Silver-ground carpet, in `r n.traps` traps, between `r first.year` and `r last.year`.

```{r,echo=FALSE,results=TRUE}
yr.avg.catches <- tapply(X = species.data$NumberCaught,INDEX = species.data$Year,FUN = mean)
yr.avg.catches[is.na(yr.avg.catches)] <- 0

par(mar=c(3.2,3.2,0.5,0.2))
par(mgp=c(2.0,0.2,0))
par(tck=-0.01)
par(las=1)

plot(all.yrs,yr.avg.catches,xaxp=c(min(all.yrs),max(all.yrs),length(all.yrs)-1),xlab="Year",ylab="Average Catch",pch=16,type="b",col="#A40122",bty="l")

dens <- hist(species.data$Month,breaks=seq(from=0.5,to=12.5,by=1),plot=FALSE)
      
barplot(dens$density,space = 0,border=NA,col="#A40122",ylab="Density")
axis(side = 1,at = seq(from=0.5,to=11.5,length.out=12),labels = month.abb)
title(xlab="Month")
box(bty="l")
```

## Silver Y

```{r,echo=FALSE,results=TRUE}
species.data <- moth.data[which(moth.data$Species=="Silver Y"),]
total.caught <- sum(species.data$NumberCaught,na.rm=TRUE)
n.traps <- length(unique(species.data$TrapNumber))
first.year <- min(species.data$Year,na.rm=TRUE)
last.year <- max(species.data$Year,na.rm=TRUE)

species.data$Year <- factor(x = species.data$Year,levels = all.yrs)
```
In total, I have caught `r total.caught` individuals of Silver Y, in `r n.traps` traps, between `r first.year` and `r last.year`.

```{r,echo=FALSE,results=TRUE}
yr.avg.catches <- tapply(X = species.data$NumberCaught,INDEX = species.data$Year,FUN = mean)
yr.avg.catches[is.na(yr.avg.catches)] <- 0

par(mar=c(3.2,3.2,0.5,0.2))
par(mgp=c(2.0,0.2,0))
par(tck=-0.01)
par(las=1)

plot(all.yrs,yr.avg.catches,xaxp=c(min(all.yrs),max(all.yrs),length(all.yrs)-1),xlab="Year",ylab="Average Catch",pch=16,type="b",col="#A40122",bty="l")

dens <- hist(species.data$Month,breaks=seq(from=0.5,to=12.5,by=1),plot=FALSE)
      
barplot(dens$density,space = 0,border=NA,col="#A40122",ylab="Density")
axis(side = 1,at = seq(from=0.5,to=11.5,length.out=12),labels = month.abb)
title(xlab="Month")
box(bty="l")
```

## Single-dotted wave

```{r,echo=FALSE,results=TRUE}
species.data <- moth.data[which(moth.data$Species=="Single-dotted wave"),]
total.caught <- sum(species.data$NumberCaught,na.rm=TRUE)
n.traps <- length(unique(species.data$TrapNumber))
first.year <- min(species.data$Year,na.rm=TRUE)
last.year <- max(species.data$Year,na.rm=TRUE)

species.data$Year <- factor(x = species.data$Year,levels = all.yrs)
```
In total, I have caught `r total.caught` individuals of Single-dotted wave, in `r n.traps` traps, between `r first.year` and `r last.year`.

```{r,echo=FALSE,results=TRUE}
yr.avg.catches <- tapply(X = species.data$NumberCaught,INDEX = species.data$Year,FUN = mean)
yr.avg.catches[is.na(yr.avg.catches)] <- 0

par(mar=c(3.2,3.2,0.5,0.2))
par(mgp=c(2.0,0.2,0))
par(tck=-0.01)
par(las=1)

plot(all.yrs,yr.avg.catches,xaxp=c(min(all.yrs),max(all.yrs),length(all.yrs)-1),xlab="Year",ylab="Average Catch",pch=16,type="b",col="#A40122",bty="l")

dens <- hist(species.data$Month,breaks=seq(from=0.5,to=12.5,by=1),plot=FALSE)
      
barplot(dens$density,space = 0,border=NA,col="#A40122",ylab="Density")
axis(side = 1,at = seq(from=0.5,to=11.5,length.out=12),labels = month.abb)
title(xlab="Month")
box(bty="l")
```

## Small blood-vein

```{r,echo=FALSE,results=TRUE}
species.data <- moth.data[which(moth.data$Species=="Small blood-vein"),]
total.caught <- sum(species.data$NumberCaught,na.rm=TRUE)
n.traps <- length(unique(species.data$TrapNumber))
first.year <- min(species.data$Year,na.rm=TRUE)
last.year <- max(species.data$Year,na.rm=TRUE)

species.data$Year <- factor(x = species.data$Year,levels = all.yrs)
```
In total, I have caught `r total.caught` individuals of Small blood-vein, in `r n.traps` traps, between `r first.year` and `r last.year`.

```{r,echo=FALSE,results=TRUE}
yr.avg.catches <- tapply(X = species.data$NumberCaught,INDEX = species.data$Year,FUN = mean)
yr.avg.catches[is.na(yr.avg.catches)] <- 0

par(mar=c(3.2,3.2,0.5,0.2))
par(mgp=c(2.0,0.2,0))
par(tck=-0.01)
par(las=1)

plot(all.yrs,yr.avg.catches,xaxp=c(min(all.yrs),max(all.yrs),length(all.yrs)-1),xlab="Year",ylab="Average Catch",pch=16,type="b",col="#A40122",bty="l")

dens <- hist(species.data$Month,breaks=seq(from=0.5,to=12.5,by=1),plot=FALSE)
      
barplot(dens$density,space = 0,border=NA,col="#A40122",ylab="Density")
axis(side = 1,at = seq(from=0.5,to=11.5,length.out=12),labels = month.abb)
title(xlab="Month")
box(bty="l")
```

## Small clouded brindle

```{r,echo=FALSE,results=TRUE}
species.data <- moth.data[which(moth.data$Species=="Small clouded brindle"),]
total.caught <- sum(species.data$NumberCaught,na.rm=TRUE)
n.traps <- length(unique(species.data$TrapNumber))
first.year <- min(species.data$Year,na.rm=TRUE)
last.year <- max(species.data$Year,na.rm=TRUE)

species.data$Year <- factor(x = species.data$Year,levels = all.yrs)
```
In total, I have caught `r total.caught` individuals of Small clouded brindle, in `r n.traps` traps, between `r first.year` and `r last.year`.

```{r,echo=FALSE,results=TRUE}
yr.avg.catches <- tapply(X = species.data$NumberCaught,INDEX = species.data$Year,FUN = mean)
yr.avg.catches[is.na(yr.avg.catches)] <- 0

par(mar=c(3.2,3.2,0.5,0.2))
par(mgp=c(2.0,0.2,0))
par(tck=-0.01)
par(las=1)

plot(all.yrs,yr.avg.catches,xaxp=c(min(all.yrs),max(all.yrs),length(all.yrs)-1),xlab="Year",ylab="Average Catch",pch=16,type="b",col="#A40122",bty="l")

dens <- hist(species.data$Month,breaks=seq(from=0.5,to=12.5,by=1),plot=FALSE)
      
barplot(dens$density,space = 0,border=NA,col="#A40122",ylab="Density")
axis(side = 1,at = seq(from=0.5,to=11.5,length.out=12),labels = month.abb)
title(xlab="Month")
box(bty="l")
```

## Small dusty wave

```{r,echo=FALSE,results=TRUE}
species.data <- moth.data[which(moth.data$Species=="Small dusty wave"),]
total.caught <- sum(species.data$NumberCaught,na.rm=TRUE)
n.traps <- length(unique(species.data$TrapNumber))
first.year <- min(species.data$Year,na.rm=TRUE)
last.year <- max(species.data$Year,na.rm=TRUE)

species.data$Year <- factor(x = species.data$Year,levels = all.yrs)
```
In total, I have caught `r total.caught` individuals of Small dusty wave, in `r n.traps` traps, between `r first.year` and `r last.year`.

```{r,echo=FALSE,results=TRUE}
yr.avg.catches <- tapply(X = species.data$NumberCaught,INDEX = species.data$Year,FUN = mean)
yr.avg.catches[is.na(yr.avg.catches)] <- 0

par(mar=c(3.2,3.2,0.5,0.2))
par(mgp=c(2.0,0.2,0))
par(tck=-0.01)
par(las=1)

plot(all.yrs,yr.avg.catches,xaxp=c(min(all.yrs),max(all.yrs),length(all.yrs)-1),xlab="Year",ylab="Average Catch",pch=16,type="b",col="#A40122",bty="l")

dens <- hist(species.data$Month,breaks=seq(from=0.5,to=12.5,by=1),plot=FALSE)
      
barplot(dens$density,space = 0,border=NA,col="#A40122",ylab="Density")
axis(side = 1,at = seq(from=0.5,to=11.5,length.out=12),labels = month.abb)
title(xlab="Month")
box(bty="l")
```

## Small Eggar

```{r,echo=FALSE,results=TRUE}
species.data <- moth.data[which(moth.data$Species=="Small Eggar"),]
total.caught <- sum(species.data$NumberCaught,na.rm=TRUE)
n.traps <- length(unique(species.data$TrapNumber))
first.year <- min(species.data$Year,na.rm=TRUE)
last.year <- max(species.data$Year,na.rm=TRUE)

species.data$Year <- factor(x = species.data$Year,levels = all.yrs)
```
In total, I have caught `r total.caught` individuals of Small Eggar, in `r n.traps` traps, between `r first.year` and `r last.year`.

```{r,echo=FALSE,results=TRUE}
yr.avg.catches <- tapply(X = species.data$NumberCaught,INDEX = species.data$Year,FUN = mean)
yr.avg.catches[is.na(yr.avg.catches)] <- 0

par(mar=c(3.2,3.2,0.5,0.2))
par(mgp=c(2.0,0.2,0))
par(tck=-0.01)
par(las=1)

plot(all.yrs,yr.avg.catches,xaxp=c(min(all.yrs),max(all.yrs),length(all.yrs)-1),xlab="Year",ylab="Average Catch",pch=16,type="b",col="#A40122",bty="l")

dens <- hist(species.data$Month,breaks=seq(from=0.5,to=12.5,by=1),plot=FALSE)
      
barplot(dens$density,space = 0,border=NA,col="#A40122",ylab="Density")
axis(side = 1,at = seq(from=0.5,to=11.5,length.out=12),labels = month.abb)
title(xlab="Month")
box(bty="l")
```

## Small elephant hawkmoth

```{r,echo=FALSE,results=TRUE}
species.data <- moth.data[which(moth.data$Species=="Small elephant hawkmoth"),]
total.caught <- sum(species.data$NumberCaught,na.rm=TRUE)
n.traps <- length(unique(species.data$TrapNumber))
first.year <- min(species.data$Year,na.rm=TRUE)
last.year <- max(species.data$Year,na.rm=TRUE)

species.data$Year <- factor(x = species.data$Year,levels = all.yrs)
```
In total, I have caught `r total.caught` individuals of Small elephant hawkmoth, in `r n.traps` traps, between `r first.year` and `r last.year`.

```{r,echo=FALSE,results=TRUE}
yr.avg.catches <- tapply(X = species.data$NumberCaught,INDEX = species.data$Year,FUN = mean)
yr.avg.catches[is.na(yr.avg.catches)] <- 0

par(mar=c(3.2,3.2,0.5,0.2))
par(mgp=c(2.0,0.2,0))
par(tck=-0.01)
par(las=1)

plot(all.yrs,yr.avg.catches,xaxp=c(min(all.yrs),max(all.yrs),length(all.yrs)-1),xlab="Year",ylab="Average Catch",pch=16,type="b",col="#A40122",bty="l")

dens <- hist(species.data$Month,breaks=seq(from=0.5,to=12.5,by=1),plot=FALSE)
      
barplot(dens$density,space = 0,border=NA,col="#A40122",ylab="Density")
axis(side = 1,at = seq(from=0.5,to=11.5,length.out=12),labels = month.abb)
title(xlab="Month")
box(bty="l")
```

## Small fan-footed wave

```{r,echo=FALSE,results=TRUE}
species.data <- moth.data[which(moth.data$Species=="Small fan-footed wave"),]
total.caught <- sum(species.data$NumberCaught,na.rm=TRUE)
n.traps <- length(unique(species.data$TrapNumber))
first.year <- min(species.data$Year,na.rm=TRUE)
last.year <- max(species.data$Year,na.rm=TRUE)

species.data$Year <- factor(x = species.data$Year,levels = all.yrs)
```
In total, I have caught `r total.caught` individuals of Small fan-footed wave, in `r n.traps` traps, between `r first.year` and `r last.year`.

```{r,echo=FALSE,results=TRUE}
yr.avg.catches <- tapply(X = species.data$NumberCaught,INDEX = species.data$Year,FUN = mean)
yr.avg.catches[is.na(yr.avg.catches)] <- 0

par(mar=c(3.2,3.2,0.5,0.2))
par(mgp=c(2.0,0.2,0))
par(tck=-0.01)
par(las=1)

plot(all.yrs,yr.avg.catches,xaxp=c(min(all.yrs),max(all.yrs),length(all.yrs)-1),xlab="Year",ylab="Average Catch",pch=16,type="b",col="#A40122",bty="l")

dens <- hist(species.data$Month,breaks=seq(from=0.5,to=12.5,by=1),plot=FALSE)
      
barplot(dens$density,space = 0,border=NA,col="#A40122",ylab="Density")
axis(side = 1,at = seq(from=0.5,to=11.5,length.out=12),labels = month.abb)
title(xlab="Month")
box(bty="l")
```

## Small Quaker

```{r,echo=FALSE,results=TRUE}
species.data <- moth.data[which(moth.data$Species=="Small Quaker"),]
total.caught <- sum(species.data$NumberCaught,na.rm=TRUE)
n.traps <- length(unique(species.data$TrapNumber))
first.year <- min(species.data$Year,na.rm=TRUE)
last.year <- max(species.data$Year,na.rm=TRUE)

species.data$Year <- factor(x = species.data$Year,levels = all.yrs)
```
In total, I have caught `r total.caught` individuals of Small Quaker, in `r n.traps` traps, between `r first.year` and `r last.year`.

```{r,echo=FALSE,results=TRUE}
yr.avg.catches <- tapply(X = species.data$NumberCaught,INDEX = species.data$Year,FUN = mean)
yr.avg.catches[is.na(yr.avg.catches)] <- 0

par(mar=c(3.2,3.2,0.5,0.2))
par(mgp=c(2.0,0.2,0))
par(tck=-0.01)
par(las=1)

plot(all.yrs,yr.avg.catches,xaxp=c(min(all.yrs),max(all.yrs),length(all.yrs)-1),xlab="Year",ylab="Average Catch",pch=16,type="b",col="#A40122",bty="l")

dens <- hist(species.data$Month,breaks=seq(from=0.5,to=12.5,by=1),plot=FALSE)
      
barplot(dens$density,space = 0,border=NA,col="#A40122",ylab="Density")
axis(side = 1,at = seq(from=0.5,to=11.5,length.out=12),labels = month.abb)
title(xlab="Month")
box(bty="l")
```

## Small ranunculus

```{r,echo=FALSE,results=TRUE}
species.data <- moth.data[which(moth.data$Species=="Small ranunculus"),]
total.caught <- sum(species.data$NumberCaught,na.rm=TRUE)
n.traps <- length(unique(species.data$TrapNumber))
first.year <- min(species.data$Year,na.rm=TRUE)
last.year <- max(species.data$Year,na.rm=TRUE)

species.data$Year <- factor(x = species.data$Year,levels = all.yrs)
```
In total, I have caught `r total.caught` individuals of Small ranunculus, in `r n.traps` traps, between `r first.year` and `r last.year`.

```{r,echo=FALSE,results=TRUE}
yr.avg.catches <- tapply(X = species.data$NumberCaught,INDEX = species.data$Year,FUN = mean)
yr.avg.catches[is.na(yr.avg.catches)] <- 0

par(mar=c(3.2,3.2,0.5,0.2))
par(mgp=c(2.0,0.2,0))
par(tck=-0.01)
par(las=1)

plot(all.yrs,yr.avg.catches,xaxp=c(min(all.yrs),max(all.yrs),length(all.yrs)-1),xlab="Year",ylab="Average Catch",pch=16,type="b",col="#A40122",bty="l")

dens <- hist(species.data$Month,breaks=seq(from=0.5,to=12.5,by=1),plot=FALSE)
      
barplot(dens$density,space = 0,border=NA,col="#A40122",ylab="Density")
axis(side = 1,at = seq(from=0.5,to=11.5,length.out=12),labels = month.abb)
title(xlab="Month")
box(bty="l")
```

## Small scallop

```{r,echo=FALSE,results=TRUE}
species.data <- moth.data[which(moth.data$Species=="Small scallop"),]
total.caught <- sum(species.data$NumberCaught,na.rm=TRUE)
n.traps <- length(unique(species.data$TrapNumber))
first.year <- min(species.data$Year,na.rm=TRUE)
last.year <- max(species.data$Year,na.rm=TRUE)

species.data$Year <- factor(x = species.data$Year,levels = all.yrs)
```
In total, I have caught `r total.caught` individuals of Small scallop, in `r n.traps` traps, between `r first.year` and `r last.year`.

```{r,echo=FALSE,results=TRUE}
yr.avg.catches <- tapply(X = species.data$NumberCaught,INDEX = species.data$Year,FUN = mean)
yr.avg.catches[is.na(yr.avg.catches)] <- 0

par(mar=c(3.2,3.2,0.5,0.2))
par(mgp=c(2.0,0.2,0))
par(tck=-0.01)
par(las=1)

plot(all.yrs,yr.avg.catches,xaxp=c(min(all.yrs),max(all.yrs),length(all.yrs)-1),xlab="Year",ylab="Average Catch",pch=16,type="b",col="#A40122",bty="l")

dens <- hist(species.data$Month,breaks=seq(from=0.5,to=12.5,by=1),plot=FALSE)
      
barplot(dens$density,space = 0,border=NA,col="#A40122",ylab="Density")
axis(side = 1,at = seq(from=0.5,to=11.5,length.out=12),labels = month.abb)
title(xlab="Month")
box(bty="l")
```

## Small square-spot

```{r,echo=FALSE,results=TRUE}
species.data <- moth.data[which(moth.data$Species=="Small square-spot"),]
total.caught <- sum(species.data$NumberCaught,na.rm=TRUE)
n.traps <- length(unique(species.data$TrapNumber))
first.year <- min(species.data$Year,na.rm=TRUE)
last.year <- max(species.data$Year,na.rm=TRUE)

species.data$Year <- factor(x = species.data$Year,levels = all.yrs)
```
In total, I have caught `r total.caught` individuals of Small square-spot, in `r n.traps` traps, between `r first.year` and `r last.year`.

```{r,echo=FALSE,results=TRUE}
yr.avg.catches <- tapply(X = species.data$NumberCaught,INDEX = species.data$Year,FUN = mean)
yr.avg.catches[is.na(yr.avg.catches)] <- 0

par(mar=c(3.2,3.2,0.5,0.2))
par(mgp=c(2.0,0.2,0))
par(tck=-0.01)
par(las=1)

plot(all.yrs,yr.avg.catches,xaxp=c(min(all.yrs),max(all.yrs),length(all.yrs)-1),xlab="Year",ylab="Average Catch",pch=16,type="b",col="#A40122",bty="l")

dens <- hist(species.data$Month,breaks=seq(from=0.5,to=12.5,by=1),plot=FALSE)
      
barplot(dens$density,space = 0,border=NA,col="#A40122",ylab="Density")
axis(side = 1,at = seq(from=0.5,to=11.5,length.out=12),labels = month.abb)
title(xlab="Month")
box(bty="l")
```

## Smoky wainscot

```{r,echo=FALSE,results=TRUE}
species.data <- moth.data[which(moth.data$Species=="Smoky wainscot"),]
total.caught <- sum(species.data$NumberCaught,na.rm=TRUE)
n.traps <- length(unique(species.data$TrapNumber))
first.year <- min(species.data$Year,na.rm=TRUE)
last.year <- max(species.data$Year,na.rm=TRUE)

species.data$Year <- factor(x = species.data$Year,levels = all.yrs)
```
In total, I have caught `r total.caught` individuals of Smoky wainscot, in `r n.traps` traps, between `r first.year` and `r last.year`.

```{r,echo=FALSE,results=TRUE}
yr.avg.catches <- tapply(X = species.data$NumberCaught,INDEX = species.data$Year,FUN = mean)
yr.avg.catches[is.na(yr.avg.catches)] <- 0

par(mar=c(3.2,3.2,0.5,0.2))
par(mgp=c(2.0,0.2,0))
par(tck=-0.01)
par(las=1)

plot(all.yrs,yr.avg.catches,xaxp=c(min(all.yrs),max(all.yrs),length(all.yrs)-1),xlab="Year",ylab="Average Catch",pch=16,type="b",col="#A40122",bty="l")

dens <- hist(species.data$Month,breaks=seq(from=0.5,to=12.5,by=1),plot=FALSE)
      
barplot(dens$density,space = 0,border=NA,col="#A40122",ylab="Density")
axis(side = 1,at = seq(from=0.5,to=11.5,length.out=12),labels = month.abb)
title(xlab="Month")
box(bty="l")
```

## Snout

```{r,echo=FALSE,results=TRUE}
species.data <- moth.data[which(moth.data$Species=="Snout"),]
total.caught <- sum(species.data$NumberCaught,na.rm=TRUE)
n.traps <- length(unique(species.data$TrapNumber))
first.year <- min(species.data$Year,na.rm=TRUE)
last.year <- max(species.data$Year,na.rm=TRUE)

species.data$Year <- factor(x = species.data$Year,levels = all.yrs)
```
In total, I have caught `r total.caught` individuals of Snout, in `r n.traps` traps, between `r first.year` and `r last.year`.

```{r,echo=FALSE,results=TRUE}
yr.avg.catches <- tapply(X = species.data$NumberCaught,INDEX = species.data$Year,FUN = mean)
yr.avg.catches[is.na(yr.avg.catches)] <- 0

par(mar=c(3.2,3.2,0.5,0.2))
par(mgp=c(2.0,0.2,0))
par(tck=-0.01)
par(las=1)

plot(all.yrs,yr.avg.catches,xaxp=c(min(all.yrs),max(all.yrs),length(all.yrs)-1),xlab="Year",ylab="Average Catch",pch=16,type="b",col="#A40122",bty="l")

dens <- hist(species.data$Month,breaks=seq(from=0.5,to=12.5,by=1),plot=FALSE)
      
barplot(dens$density,space = 0,border=NA,col="#A40122",ylab="Density")
axis(side = 1,at = seq(from=0.5,to=11.5,length.out=12),labels = month.abb)
title(xlab="Month")
box(bty="l")
```

## Spectacle

```{r,echo=FALSE,results=TRUE}
species.data <- moth.data[which(moth.data$Species=="Spectacle"),]
total.caught <- sum(species.data$NumberCaught,na.rm=TRUE)
n.traps <- length(unique(species.data$TrapNumber))
first.year <- min(species.data$Year,na.rm=TRUE)
last.year <- max(species.data$Year,na.rm=TRUE)

species.data$Year <- factor(x = species.data$Year,levels = all.yrs)
```
In total, I have caught `r total.caught` individuals of Spectacle, in `r n.traps` traps, between `r first.year` and `r last.year`.

```{r,echo=FALSE,results=TRUE}
yr.avg.catches <- tapply(X = species.data$NumberCaught,INDEX = species.data$Year,FUN = mean)
yr.avg.catches[is.na(yr.avg.catches)] <- 0

par(mar=c(3.2,3.2,0.5,0.2))
par(mgp=c(2.0,0.2,0))
par(tck=-0.01)
par(las=1)

plot(all.yrs,yr.avg.catches,xaxp=c(min(all.yrs),max(all.yrs),length(all.yrs)-1),xlab="Year",ylab="Average Catch",pch=16,type="b",col="#A40122",bty="l")

dens <- hist(species.data$Month,breaks=seq(from=0.5,to=12.5,by=1),plot=FALSE)
      
barplot(dens$density,space = 0,border=NA,col="#A40122",ylab="Density")
axis(side = 1,at = seq(from=0.5,to=11.5,length.out=12),labels = month.abb)
title(xlab="Month")
box(bty="l")
```

## Sprawler

```{r,echo=FALSE,results=TRUE}
species.data <- moth.data[which(moth.data$Species=="Sprawler"),]
total.caught <- sum(species.data$NumberCaught,na.rm=TRUE)
n.traps <- length(unique(species.data$TrapNumber))
first.year <- min(species.data$Year,na.rm=TRUE)
last.year <- max(species.data$Year,na.rm=TRUE)

species.data$Year <- factor(x = species.data$Year,levels = all.yrs)
```
In total, I have caught `r total.caught` individuals of Sprawler, in `r n.traps` traps, between `r first.year` and `r last.year`.

```{r,echo=FALSE,results=TRUE}
yr.avg.catches <- tapply(X = species.data$NumberCaught,INDEX = species.data$Year,FUN = mean)
yr.avg.catches[is.na(yr.avg.catches)] <- 0

par(mar=c(3.2,3.2,0.5,0.2))
par(mgp=c(2.0,0.2,0))
par(tck=-0.01)
par(las=1)

plot(all.yrs,yr.avg.catches,xaxp=c(min(all.yrs),max(all.yrs),length(all.yrs)-1),xlab="Year",ylab="Average Catch",pch=16,type="b",col="#A40122",bty="l")

dens <- hist(species.data$Month,breaks=seq(from=0.5,to=12.5,by=1),plot=FALSE)
      
barplot(dens$density,space = 0,border=NA,col="#A40122",ylab="Density")
axis(side = 1,at = seq(from=0.5,to=11.5,length.out=12),labels = month.abb)
title(xlab="Month")
box(bty="l")
```

## Spring usher

```{r,echo=FALSE,results=TRUE}
species.data <- moth.data[which(moth.data$Species=="Spring usher"),]
total.caught <- sum(species.data$NumberCaught,na.rm=TRUE)
n.traps <- length(unique(species.data$TrapNumber))
first.year <- min(species.data$Year,na.rm=TRUE)
last.year <- max(species.data$Year,na.rm=TRUE)

species.data$Year <- factor(x = species.data$Year,levels = all.yrs)
```
In total, I have caught `r total.caught` individuals of Spring usher, in `r n.traps` traps, between `r first.year` and `r last.year`.

```{r,echo=FALSE,results=TRUE}
yr.avg.catches <- tapply(X = species.data$NumberCaught,INDEX = species.data$Year,FUN = mean)
yr.avg.catches[is.na(yr.avg.catches)] <- 0

par(mar=c(3.2,3.2,0.5,0.2))
par(mgp=c(2.0,0.2,0))
par(tck=-0.01)
par(las=1)

plot(all.yrs,yr.avg.catches,xaxp=c(min(all.yrs),max(all.yrs),length(all.yrs)-1),xlab="Year",ylab="Average Catch",pch=16,type="b",col="#A40122",bty="l")

dens <- hist(species.data$Month,breaks=seq(from=0.5,to=12.5,by=1),plot=FALSE)
      
barplot(dens$density,space = 0,border=NA,col="#A40122",ylab="Density")
axis(side = 1,at = seq(from=0.5,to=11.5,length.out=12),labels = month.abb)
title(xlab="Month")
box(bty="l")
```

## Spruce carpet

```{r,echo=FALSE,results=TRUE}
species.data <- moth.data[which(moth.data$Species=="Spruce carpet"),]
total.caught <- sum(species.data$NumberCaught,na.rm=TRUE)
n.traps <- length(unique(species.data$TrapNumber))
first.year <- min(species.data$Year,na.rm=TRUE)
last.year <- max(species.data$Year,na.rm=TRUE)

species.data$Year <- factor(x = species.data$Year,levels = all.yrs)
```
In total, I have caught `r total.caught` individuals of Spruce carpet, in `r n.traps` traps, between `r first.year` and `r last.year`.

```{r,echo=FALSE,results=TRUE}
yr.avg.catches <- tapply(X = species.data$NumberCaught,INDEX = species.data$Year,FUN = mean)
yr.avg.catches[is.na(yr.avg.catches)] <- 0

par(mar=c(3.2,3.2,0.5,0.2))
par(mgp=c(2.0,0.2,0))
par(tck=-0.01)
par(las=1)

plot(all.yrs,yr.avg.catches,xaxp=c(min(all.yrs),max(all.yrs),length(all.yrs)-1),xlab="Year",ylab="Average Catch",pch=16,type="b",col="#A40122",bty="l")

dens <- hist(species.data$Month,breaks=seq(from=0.5,to=12.5,by=1),plot=FALSE)
      
barplot(dens$density,space = 0,border=NA,col="#A40122",ylab="Density")
axis(side = 1,at = seq(from=0.5,to=11.5,length.out=12),labels = month.abb)
title(xlab="Month")
box(bty="l")
```

## Square-spot rustic

```{r,echo=FALSE,results=TRUE}
species.data <- moth.data[which(moth.data$Species=="Square-spot rustic"),]
total.caught <- sum(species.data$NumberCaught,na.rm=TRUE)
n.traps <- length(unique(species.data$TrapNumber))
first.year <- min(species.data$Year,na.rm=TRUE)
last.year <- max(species.data$Year,na.rm=TRUE)

species.data$Year <- factor(x = species.data$Year,levels = all.yrs)
```
In total, I have caught `r total.caught` individuals of Square-spot rustic, in `r n.traps` traps, between `r first.year` and `r last.year`.

```{r,echo=FALSE,results=TRUE}
yr.avg.catches <- tapply(X = species.data$NumberCaught,INDEX = species.data$Year,FUN = mean)
yr.avg.catches[is.na(yr.avg.catches)] <- 0

par(mar=c(3.2,3.2,0.5,0.2))
par(mgp=c(2.0,0.2,0))
par(tck=-0.01)
par(las=1)

plot(all.yrs,yr.avg.catches,xaxp=c(min(all.yrs),max(all.yrs),length(all.yrs)-1),xlab="Year",ylab="Average Catch",pch=16,type="b",col="#A40122",bty="l")

dens <- hist(species.data$Month,breaks=seq(from=0.5,to=12.5,by=1),plot=FALSE)
      
barplot(dens$density,space = 0,border=NA,col="#A40122",ylab="Density")
axis(side = 1,at = seq(from=0.5,to=11.5,length.out=12),labels = month.abb)
title(xlab="Month")
box(bty="l")
```

## Straw dot

```{r,echo=FALSE,results=TRUE}
species.data <- moth.data[which(moth.data$Species=="Straw dot"),]
total.caught <- sum(species.data$NumberCaught,na.rm=TRUE)
n.traps <- length(unique(species.data$TrapNumber))
first.year <- min(species.data$Year,na.rm=TRUE)
last.year <- max(species.data$Year,na.rm=TRUE)

species.data$Year <- factor(x = species.data$Year,levels = all.yrs)
```
In total, I have caught `r total.caught` individuals of Straw dot, in `r n.traps` traps, between `r first.year` and `r last.year`.

```{r,echo=FALSE,results=TRUE}
yr.avg.catches <- tapply(X = species.data$NumberCaught,INDEX = species.data$Year,FUN = mean)
yr.avg.catches[is.na(yr.avg.catches)] <- 0

par(mar=c(3.2,3.2,0.5,0.2))
par(mgp=c(2.0,0.2,0))
par(tck=-0.01)
par(las=1)

plot(all.yrs,yr.avg.catches,xaxp=c(min(all.yrs),max(all.yrs),length(all.yrs)-1),xlab="Year",ylab="Average Catch",pch=16,type="b",col="#A40122",bty="l")

dens <- hist(species.data$Month,breaks=seq(from=0.5,to=12.5,by=1),plot=FALSE)
      
barplot(dens$density,space = 0,border=NA,col="#A40122",ylab="Density")
axis(side = 1,at = seq(from=0.5,to=11.5,length.out=12),labels = month.abb)
title(xlab="Month")
box(bty="l")
```

## Straw underwing

```{r,echo=FALSE,results=TRUE}
species.data <- moth.data[which(moth.data$Species=="Straw underwing"),]
total.caught <- sum(species.data$NumberCaught,na.rm=TRUE)
n.traps <- length(unique(species.data$TrapNumber))
first.year <- min(species.data$Year,na.rm=TRUE)
last.year <- max(species.data$Year,na.rm=TRUE)

species.data$Year <- factor(x = species.data$Year,levels = all.yrs)
```
In total, I have caught `r total.caught` individuals of Straw underwing, in `r n.traps` traps, between `r first.year` and `r last.year`.

```{r,echo=FALSE,results=TRUE}
yr.avg.catches <- tapply(X = species.data$NumberCaught,INDEX = species.data$Year,FUN = mean)
yr.avg.catches[is.na(yr.avg.catches)] <- 0

par(mar=c(3.2,3.2,0.5,0.2))
par(mgp=c(2.0,0.2,0))
par(tck=-0.01)
par(las=1)

plot(all.yrs,yr.avg.catches,xaxp=c(min(all.yrs),max(all.yrs),length(all.yrs)-1),xlab="Year",ylab="Average Catch",pch=16,type="b",col="#A40122",bty="l")

dens <- hist(species.data$Month,breaks=seq(from=0.5,to=12.5,by=1),plot=FALSE)
      
barplot(dens$density,space = 0,border=NA,col="#A40122",ylab="Density")
axis(side = 1,at = seq(from=0.5,to=11.5,length.out=12),labels = month.abb)
title(xlab="Month")
box(bty="l")
```

## Streamer

```{r,echo=FALSE,results=TRUE}
species.data <- moth.data[which(moth.data$Species=="Streamer"),]
total.caught <- sum(species.data$NumberCaught,na.rm=TRUE)
n.traps <- length(unique(species.data$TrapNumber))
first.year <- min(species.data$Year,na.rm=TRUE)
last.year <- max(species.data$Year,na.rm=TRUE)

species.data$Year <- factor(x = species.data$Year,levels = all.yrs)
```
In total, I have caught `r total.caught` individuals of Streamer, in `r n.traps` traps, between `r first.year` and `r last.year`.

```{r,echo=FALSE,results=TRUE}
yr.avg.catches <- tapply(X = species.data$NumberCaught,INDEX = species.data$Year,FUN = mean)
yr.avg.catches[is.na(yr.avg.catches)] <- 0

par(mar=c(3.2,3.2,0.5,0.2))
par(mgp=c(2.0,0.2,0))
par(tck=-0.01)
par(las=1)

plot(all.yrs,yr.avg.catches,xaxp=c(min(all.yrs),max(all.yrs),length(all.yrs)-1),xlab="Year",ylab="Average Catch",pch=16,type="b",col="#A40122",bty="l")

dens <- hist(species.data$Month,breaks=seq(from=0.5,to=12.5,by=1),plot=FALSE)
      
barplot(dens$density,space = 0,border=NA,col="#A40122",ylab="Density")
axis(side = 1,at = seq(from=0.5,to=11.5,length.out=12),labels = month.abb)
title(xlab="Month")
box(bty="l")
```

## Svenssons copper underwing

```{r,echo=FALSE,results=TRUE}
species.data <- moth.data[which(moth.data$Species=="Svenssons copper underwing"),]
total.caught <- sum(species.data$NumberCaught,na.rm=TRUE)
n.traps <- length(unique(species.data$TrapNumber))
first.year <- min(species.data$Year,na.rm=TRUE)
last.year <- max(species.data$Year,na.rm=TRUE)

species.data$Year <- factor(x = species.data$Year,levels = all.yrs)
```
In total, I have caught `r total.caught` individuals of Svenssons copper underwing, in `r n.traps` traps, between `r first.year` and `r last.year`.

```{r,echo=FALSE,results=TRUE}
yr.avg.catches <- tapply(X = species.data$NumberCaught,INDEX = species.data$Year,FUN = mean)
yr.avg.catches[is.na(yr.avg.catches)] <- 0

par(mar=c(3.2,3.2,0.5,0.2))
par(mgp=c(2.0,0.2,0))
par(tck=-0.01)
par(las=1)

plot(all.yrs,yr.avg.catches,xaxp=c(min(all.yrs),max(all.yrs),length(all.yrs)-1),xlab="Year",ylab="Average Catch",pch=16,type="b",col="#A40122",bty="l")

dens <- hist(species.data$Month,breaks=seq(from=0.5,to=12.5,by=1),plot=FALSE)
      
barplot(dens$density,space = 0,border=NA,col="#A40122",ylab="Density")
axis(side = 1,at = seq(from=0.5,to=11.5,length.out=12),labels = month.abb)
title(xlab="Month")
box(bty="l")
```

## Swallow-tailed

```{r,echo=FALSE,results=TRUE}
species.data <- moth.data[which(moth.data$Species=="Swallow-tailed"),]
total.caught <- sum(species.data$NumberCaught,na.rm=TRUE)
n.traps <- length(unique(species.data$TrapNumber))
first.year <- min(species.data$Year,na.rm=TRUE)
last.year <- max(species.data$Year,na.rm=TRUE)

species.data$Year <- factor(x = species.data$Year,levels = all.yrs)
```
In total, I have caught `r total.caught` individuals of Swallow-tailed, in `r n.traps` traps, between `r first.year` and `r last.year`.

```{r,echo=FALSE,results=TRUE}
yr.avg.catches <- tapply(X = species.data$NumberCaught,INDEX = species.data$Year,FUN = mean)
yr.avg.catches[is.na(yr.avg.catches)] <- 0

par(mar=c(3.2,3.2,0.5,0.2))
par(mgp=c(2.0,0.2,0))
par(tck=-0.01)
par(las=1)

plot(all.yrs,yr.avg.catches,xaxp=c(min(all.yrs),max(all.yrs),length(all.yrs)-1),xlab="Year",ylab="Average Catch",pch=16,type="b",col="#A40122",bty="l")

dens <- hist(species.data$Month,breaks=seq(from=0.5,to=12.5,by=1),plot=FALSE)
      
barplot(dens$density,space = 0,border=NA,col="#A40122",ylab="Density")
axis(side = 1,at = seq(from=0.5,to=11.5,length.out=12),labels = month.abb)
title(xlab="Month")
box(bty="l")
```

## Toadflax brocade

```{r,echo=FALSE,results=TRUE}
species.data <- moth.data[which(moth.data$Species=="Toadflax brocade"),]
total.caught <- sum(species.data$NumberCaught,na.rm=TRUE)
n.traps <- length(unique(species.data$TrapNumber))
first.year <- min(species.data$Year,na.rm=TRUE)
last.year <- max(species.data$Year,na.rm=TRUE)

species.data$Year <- factor(x = species.data$Year,levels = all.yrs)
```
In total, I have caught `r total.caught` individuals of Toadflax brocade, in `r n.traps` traps, between `r first.year` and `r last.year`.

```{r,echo=FALSE,results=TRUE}
yr.avg.catches <- tapply(X = species.data$NumberCaught,INDEX = species.data$Year,FUN = mean)
yr.avg.catches[is.na(yr.avg.catches)] <- 0

par(mar=c(3.2,3.2,0.5,0.2))
par(mgp=c(2.0,0.2,0))
par(tck=-0.01)
par(las=1)

plot(all.yrs,yr.avg.catches,xaxp=c(min(all.yrs),max(all.yrs),length(all.yrs)-1),xlab="Year",ylab="Average Catch",pch=16,type="b",col="#A40122",bty="l")

dens <- hist(species.data$Month,breaks=seq(from=0.5,to=12.5,by=1),plot=FALSE)
      
barplot(dens$density,space = 0,border=NA,col="#A40122",ylab="Density")
axis(side = 1,at = seq(from=0.5,to=11.5,length.out=12),labels = month.abb)
title(xlab="Month")
box(bty="l")
```

## Treble bar

```{r,echo=FALSE,results=TRUE}
species.data <- moth.data[which(moth.data$Species=="Treble bar"),]
total.caught <- sum(species.data$NumberCaught,na.rm=TRUE)
n.traps <- length(unique(species.data$TrapNumber))
first.year <- min(species.data$Year,na.rm=TRUE)
last.year <- max(species.data$Year,na.rm=TRUE)

species.data$Year <- factor(x = species.data$Year,levels = all.yrs)
```
In total, I have caught `r total.caught` individuals of Treble bar, in `r n.traps` traps, between `r first.year` and `r last.year`.

```{r,echo=FALSE,results=TRUE}
yr.avg.catches <- tapply(X = species.data$NumberCaught,INDEX = species.data$Year,FUN = mean)
yr.avg.catches[is.na(yr.avg.catches)] <- 0

par(mar=c(3.2,3.2,0.5,0.2))
par(mgp=c(2.0,0.2,0))
par(tck=-0.01)
par(las=1)

plot(all.yrs,yr.avg.catches,xaxp=c(min(all.yrs),max(all.yrs),length(all.yrs)-1),xlab="Year",ylab="Average Catch",pch=16,type="b",col="#A40122",bty="l")

dens <- hist(species.data$Month,breaks=seq(from=0.5,to=12.5,by=1),plot=FALSE)
      
barplot(dens$density,space = 0,border=NA,col="#A40122",ylab="Density")
axis(side = 1,at = seq(from=0.5,to=11.5,length.out=12),labels = month.abb)
title(xlab="Month")
box(bty="l")
```

## Treble brown spot

```{r,echo=FALSE,results=TRUE}
species.data <- moth.data[which(moth.data$Species=="Treble brown spot"),]
total.caught <- sum(species.data$NumberCaught,na.rm=TRUE)
n.traps <- length(unique(species.data$TrapNumber))
first.year <- min(species.data$Year,na.rm=TRUE)
last.year <- max(species.data$Year,na.rm=TRUE)

species.data$Year <- factor(x = species.data$Year,levels = all.yrs)
```
In total, I have caught `r total.caught` individuals of Treble brown spot, in `r n.traps` traps, between `r first.year` and `r last.year`.

```{r,echo=FALSE,results=TRUE}
yr.avg.catches <- tapply(X = species.data$NumberCaught,INDEX = species.data$Year,FUN = mean)
yr.avg.catches[is.na(yr.avg.catches)] <- 0

par(mar=c(3.2,3.2,0.5,0.2))
par(mgp=c(2.0,0.2,0))
par(tck=-0.01)
par(las=1)

plot(all.yrs,yr.avg.catches,xaxp=c(min(all.yrs),max(all.yrs),length(all.yrs)-1),xlab="Year",ylab="Average Catch",pch=16,type="b",col="#A40122",bty="l")

dens <- hist(species.data$Month,breaks=seq(from=0.5,to=12.5,by=1),plot=FALSE)
      
barplot(dens$density,space = 0,border=NA,col="#A40122",ylab="Density")
axis(side = 1,at = seq(from=0.5,to=11.5,length.out=12),labels = month.abb)
title(xlab="Month")
box(bty="l")
```

## Treble lines

```{r,echo=FALSE,results=TRUE}
species.data <- moth.data[which(moth.data$Species=="Treble lines"),]
total.caught <- sum(species.data$NumberCaught,na.rm=TRUE)
n.traps <- length(unique(species.data$TrapNumber))
first.year <- min(species.data$Year,na.rm=TRUE)
last.year <- max(species.data$Year,na.rm=TRUE)

species.data$Year <- factor(x = species.data$Year,levels = all.yrs)
```
In total, I have caught `r total.caught` individuals of Treble lines, in `r n.traps` traps, between `r first.year` and `r last.year`.

```{r,echo=FALSE,results=TRUE}
yr.avg.catches <- tapply(X = species.data$NumberCaught,INDEX = species.data$Year,FUN = mean)
yr.avg.catches[is.na(yr.avg.catches)] <- 0

par(mar=c(3.2,3.2,0.5,0.2))
par(mgp=c(2.0,0.2,0))
par(tck=-0.01)
par(las=1)

plot(all.yrs,yr.avg.catches,xaxp=c(min(all.yrs),max(all.yrs),length(all.yrs)-1),xlab="Year",ylab="Average Catch",pch=16,type="b",col="#A40122",bty="l")

dens <- hist(species.data$Month,breaks=seq(from=0.5,to=12.5,by=1),plot=FALSE)
      
barplot(dens$density,space = 0,border=NA,col="#A40122",ylab="Density")
axis(side = 1,at = seq(from=0.5,to=11.5,length.out=12),labels = month.abb)
title(xlab="Month")
box(bty="l")
```

## Tree lichen beauty

```{r,echo=FALSE,results=TRUE}
species.data <- moth.data[which(moth.data$Species=="Tree lichen beauty"),]
total.caught <- sum(species.data$NumberCaught,na.rm=TRUE)
n.traps <- length(unique(species.data$TrapNumber))
first.year <- min(species.data$Year,na.rm=TRUE)
last.year <- max(species.data$Year,na.rm=TRUE)

species.data$Year <- factor(x = species.data$Year,levels = all.yrs)
```
In total, I have caught `r total.caught` individuals of Tree lichen beauty, in `r n.traps` traps, between `r first.year` and `r last.year`.

```{r,echo=FALSE,results=TRUE}
yr.avg.catches <- tapply(X = species.data$NumberCaught,INDEX = species.data$Year,FUN = mean)
yr.avg.catches[is.na(yr.avg.catches)] <- 0

par(mar=c(3.2,3.2,0.5,0.2))
par(mgp=c(2.0,0.2,0))
par(tck=-0.01)
par(las=1)

plot(all.yrs,yr.avg.catches,xaxp=c(min(all.yrs),max(all.yrs),length(all.yrs)-1),xlab="Year",ylab="Average Catch",pch=16,type="b",col="#A40122",bty="l")

dens <- hist(species.data$Month,breaks=seq(from=0.5,to=12.5,by=1),plot=FALSE)
      
barplot(dens$density,space = 0,border=NA,col="#A40122",ylab="Density")
axis(side = 1,at = seq(from=0.5,to=11.5,length.out=12),labels = month.abb)
title(xlab="Month")
box(bty="l")
```

## Turnip

```{r,echo=FALSE,results=TRUE}
species.data <- moth.data[which(moth.data$Species=="Turnip"),]
total.caught <- sum(species.data$NumberCaught,na.rm=TRUE)
n.traps <- length(unique(species.data$TrapNumber))
first.year <- min(species.data$Year,na.rm=TRUE)
last.year <- max(species.data$Year,na.rm=TRUE)

species.data$Year <- factor(x = species.data$Year,levels = all.yrs)
```
In total, I have caught `r total.caught` individuals of Turnip, in `r n.traps` traps, between `r first.year` and `r last.year`.

```{r,echo=FALSE,results=TRUE}
yr.avg.catches <- tapply(X = species.data$NumberCaught,INDEX = species.data$Year,FUN = mean)
yr.avg.catches[is.na(yr.avg.catches)] <- 0

par(mar=c(3.2,3.2,0.5,0.2))
par(mgp=c(2.0,0.2,0))
par(tck=-0.01)
par(las=1)

plot(all.yrs,yr.avg.catches,xaxp=c(min(all.yrs),max(all.yrs),length(all.yrs)-1),xlab="Year",ylab="Average Catch",pch=16,type="b",col="#A40122",bty="l")

dens <- hist(species.data$Month,breaks=seq(from=0.5,to=12.5,by=1),plot=FALSE)
      
barplot(dens$density,space = 0,border=NA,col="#A40122",ylab="Density")
axis(side = 1,at = seq(from=0.5,to=11.5,length.out=12),labels = month.abb)
title(xlab="Month")
box(bty="l")
```

## Twin-spot Quaker

```{r,echo=FALSE,results=TRUE}
species.data <- moth.data[which(moth.data$Species=="Twin-spot Quaker"),]
total.caught <- sum(species.data$NumberCaught,na.rm=TRUE)
n.traps <- length(unique(species.data$TrapNumber))
first.year <- min(species.data$Year,na.rm=TRUE)
last.year <- max(species.data$Year,na.rm=TRUE)

species.data$Year <- factor(x = species.data$Year,levels = all.yrs)
```
In total, I have caught `r total.caught` individuals of Twin-spot Quaker, in `r n.traps` traps, between `r first.year` and `r last.year`.

```{r,echo=FALSE,results=TRUE}
yr.avg.catches <- tapply(X = species.data$NumberCaught,INDEX = species.data$Year,FUN = mean)
yr.avg.catches[is.na(yr.avg.catches)] <- 0

par(mar=c(3.2,3.2,0.5,0.2))
par(mgp=c(2.0,0.2,0))
par(tck=-0.01)
par(las=1)

plot(all.yrs,yr.avg.catches,xaxp=c(min(all.yrs),max(all.yrs),length(all.yrs)-1),xlab="Year",ylab="Average Catch",pch=16,type="b",col="#A40122",bty="l")

dens <- hist(species.data$Month,breaks=seq(from=0.5,to=12.5,by=1),plot=FALSE)
      
barplot(dens$density,space = 0,border=NA,col="#A40122",ylab="Density")
axis(side = 1,at = seq(from=0.5,to=11.5,length.out=12),labels = month.abb)
title(xlab="Month")
box(bty="l")
```

## Uncertain

```{r,echo=FALSE,results=TRUE}
species.data <- moth.data[which(moth.data$Species=="Uncertain"),]
total.caught <- sum(species.data$NumberCaught,na.rm=TRUE)
n.traps <- length(unique(species.data$TrapNumber))
first.year <- min(species.data$Year,na.rm=TRUE)
last.year <- max(species.data$Year,na.rm=TRUE)

species.data$Year <- factor(x = species.data$Year,levels = all.yrs)
```
In total, I have caught `r total.caught` individuals of Uncertain, in `r n.traps` traps, between `r first.year` and `r last.year`.

```{r,echo=FALSE,results=TRUE}
yr.avg.catches <- tapply(X = species.data$NumberCaught,INDEX = species.data$Year,FUN = mean)
yr.avg.catches[is.na(yr.avg.catches)] <- 0

par(mar=c(3.2,3.2,0.5,0.2))
par(mgp=c(2.0,0.2,0))
par(tck=-0.01)
par(las=1)

plot(all.yrs,yr.avg.catches,xaxp=c(min(all.yrs),max(all.yrs),length(all.yrs)-1),xlab="Year",ylab="Average Catch",pch=16,type="b",col="#A40122",bty="l")

dens <- hist(species.data$Month,breaks=seq(from=0.5,to=12.5,by=1),plot=FALSE)
      
barplot(dens$density,space = 0,border=NA,col="#A40122",ylab="Density")
axis(side = 1,at = seq(from=0.5,to=11.5,length.out=12),labels = month.abb)
title(xlab="Month")
box(bty="l")
```

## Vapourer

```{r,echo=FALSE,results=TRUE}
species.data <- moth.data[which(moth.data$Species=="Vapourer"),]
total.caught <- sum(species.data$NumberCaught,na.rm=TRUE)
n.traps <- length(unique(species.data$TrapNumber))
first.year <- min(species.data$Year,na.rm=TRUE)
last.year <- max(species.data$Year,na.rm=TRUE)

species.data$Year <- factor(x = species.data$Year,levels = all.yrs)
```
In total, I have caught `r total.caught` individuals of Vapourer, in `r n.traps` traps, between `r first.year` and `r last.year`.

```{r,echo=FALSE,results=TRUE}
yr.avg.catches <- tapply(X = species.data$NumberCaught,INDEX = species.data$Year,FUN = mean)
yr.avg.catches[is.na(yr.avg.catches)] <- 0

par(mar=c(3.2,3.2,0.5,0.2))
par(mgp=c(2.0,0.2,0))
par(tck=-0.01)
par(las=1)

plot(all.yrs,yr.avg.catches,xaxp=c(min(all.yrs),max(all.yrs),length(all.yrs)-1),xlab="Year",ylab="Average Catch",pch=16,type="b",col="#A40122",bty="l")

dens <- hist(species.data$Month,breaks=seq(from=0.5,to=12.5,by=1),plot=FALSE)
      
barplot(dens$density,space = 0,border=NA,col="#A40122",ylab="Density")
axis(side = 1,at = seq(from=0.5,to=11.5,length.out=12),labels = month.abb)
title(xlab="Month")
box(bty="l")
```

## Vines rustic

```{r,echo=FALSE,results=TRUE}
species.data <- moth.data[which(moth.data$Species=="Vines rustic"),]
total.caught <- sum(species.data$NumberCaught,na.rm=TRUE)
n.traps <- length(unique(species.data$TrapNumber))
first.year <- min(species.data$Year,na.rm=TRUE)
last.year <- max(species.data$Year,na.rm=TRUE)

species.data$Year <- factor(x = species.data$Year,levels = all.yrs)
```
In total, I have caught `r total.caught` individuals of Vines rustic, in `r n.traps` traps, between `r first.year` and `r last.year`.

```{r,echo=FALSE,results=TRUE}
yr.avg.catches <- tapply(X = species.data$NumberCaught,INDEX = species.data$Year,FUN = mean)
yr.avg.catches[is.na(yr.avg.catches)] <- 0

par(mar=c(3.2,3.2,0.5,0.2))
par(mgp=c(2.0,0.2,0))
par(tck=-0.01)
par(las=1)

plot(all.yrs,yr.avg.catches,xaxp=c(min(all.yrs),max(all.yrs),length(all.yrs)-1),xlab="Year",ylab="Average Catch",pch=16,type="b",col="#A40122",bty="l")

dens <- hist(species.data$Month,breaks=seq(from=0.5,to=12.5,by=1),plot=FALSE)
      
barplot(dens$density,space = 0,border=NA,col="#A40122",ylab="Density")
axis(side = 1,at = seq(from=0.5,to=11.5,length.out=12),labels = month.abb)
title(xlab="Month")
box(bty="l")
```

## Webb's wainscot

```{r,echo=FALSE,results=TRUE}
species.data <- moth.data[which(moth.data$Species=="Webb's wainscot"),]
total.caught <- sum(species.data$NumberCaught,na.rm=TRUE)
n.traps <- length(unique(species.data$TrapNumber))
first.year <- min(species.data$Year,na.rm=TRUE)
last.year <- max(species.data$Year,na.rm=TRUE)

species.data$Year <- factor(x = species.data$Year,levels = all.yrs)
```
In total, I have caught `r total.caught` individuals of Webb's wainscot, in `r n.traps` traps, between `r first.year` and `r last.year`.

```{r,echo=FALSE,results=TRUE}
yr.avg.catches <- tapply(X = species.data$NumberCaught,INDEX = species.data$Year,FUN = mean)
yr.avg.catches[is.na(yr.avg.catches)] <- 0

par(mar=c(3.2,3.2,0.5,0.2))
par(mgp=c(2.0,0.2,0))
par(tck=-0.01)
par(las=1)

plot(all.yrs,yr.avg.catches,xaxp=c(min(all.yrs),max(all.yrs),length(all.yrs)-1),xlab="Year",ylab="Average Catch",pch=16,type="b",col="#A40122",bty="l")

dens <- hist(species.data$Month,breaks=seq(from=0.5,to=12.5,by=1),plot=FALSE)
      
barplot(dens$density,space = 0,border=NA,col="#A40122",ylab="Density")
axis(side = 1,at = seq(from=0.5,to=11.5,length.out=12),labels = month.abb)
title(xlab="Month")
box(bty="l")
```

## White-point

```{r,echo=FALSE,results=TRUE}
species.data <- moth.data[which(moth.data$Species=="White-point"),]
total.caught <- sum(species.data$NumberCaught,na.rm=TRUE)
n.traps <- length(unique(species.data$TrapNumber))
first.year <- min(species.data$Year,na.rm=TRUE)
last.year <- max(species.data$Year,na.rm=TRUE)

species.data$Year <- factor(x = species.data$Year,levels = all.yrs)
```
In total, I have caught `r total.caught` individuals of White-point, in `r n.traps` traps, between `r first.year` and `r last.year`.

```{r,echo=FALSE,results=TRUE}
yr.avg.catches <- tapply(X = species.data$NumberCaught,INDEX = species.data$Year,FUN = mean)
yr.avg.catches[is.na(yr.avg.catches)] <- 0

par(mar=c(3.2,3.2,0.5,0.2))
par(mgp=c(2.0,0.2,0))
par(tck=-0.01)
par(las=1)

plot(all.yrs,yr.avg.catches,xaxp=c(min(all.yrs),max(all.yrs),length(all.yrs)-1),xlab="Year",ylab="Average Catch",pch=16,type="b",col="#A40122",bty="l")

dens <- hist(species.data$Month,breaks=seq(from=0.5,to=12.5,by=1),plot=FALSE)
      
barplot(dens$density,space = 0,border=NA,col="#A40122",ylab="Density")
axis(side = 1,at = seq(from=0.5,to=11.5,length.out=12),labels = month.abb)
title(xlab="Month")
box(bty="l")
```

## White ermine

```{r,echo=FALSE,results=TRUE}
species.data <- moth.data[which(moth.data$Species=="White ermine"),]
total.caught <- sum(species.data$NumberCaught,na.rm=TRUE)
n.traps <- length(unique(species.data$TrapNumber))
first.year <- min(species.data$Year,na.rm=TRUE)
last.year <- max(species.data$Year,na.rm=TRUE)

species.data$Year <- factor(x = species.data$Year,levels = all.yrs)
```
In total, I have caught `r total.caught` individuals of White ermine, in `r n.traps` traps, between `r first.year` and `r last.year`.

```{r,echo=FALSE,results=TRUE}
yr.avg.catches <- tapply(X = species.data$NumberCaught,INDEX = species.data$Year,FUN = mean)
yr.avg.catches[is.na(yr.avg.catches)] <- 0

par(mar=c(3.2,3.2,0.5,0.2))
par(mgp=c(2.0,0.2,0))
par(tck=-0.01)
par(las=1)

plot(all.yrs,yr.avg.catches,xaxp=c(min(all.yrs),max(all.yrs),length(all.yrs)-1),xlab="Year",ylab="Average Catch",pch=16,type="b",col="#A40122",bty="l")

dens <- hist(species.data$Month,breaks=seq(from=0.5,to=12.5,by=1),plot=FALSE)
      
barplot(dens$density,space = 0,border=NA,col="#A40122",ylab="Density")
axis(side = 1,at = seq(from=0.5,to=11.5,length.out=12),labels = month.abb)
title(xlab="Month")
box(bty="l")
```

## White satin

```{r,echo=FALSE,results=TRUE}
species.data <- moth.data[which(moth.data$Species=="White satin"),]
total.caught <- sum(species.data$NumberCaught,na.rm=TRUE)
n.traps <- length(unique(species.data$TrapNumber))
first.year <- min(species.data$Year,na.rm=TRUE)
last.year <- max(species.data$Year,na.rm=TRUE)

species.data$Year <- factor(x = species.data$Year,levels = all.yrs)
```
In total, I have caught `r total.caught` individuals of White satin, in `r n.traps` traps, between `r first.year` and `r last.year`.

```{r,echo=FALSE,results=TRUE}
yr.avg.catches <- tapply(X = species.data$NumberCaught,INDEX = species.data$Year,FUN = mean)
yr.avg.catches[is.na(yr.avg.catches)] <- 0

par(mar=c(3.2,3.2,0.5,0.2))
par(mgp=c(2.0,0.2,0))
par(tck=-0.01)
par(las=1)

plot(all.yrs,yr.avg.catches,xaxp=c(min(all.yrs),max(all.yrs),length(all.yrs)-1),xlab="Year",ylab="Average Catch",pch=16,type="b",col="#A40122",bty="l")

dens <- hist(species.data$Month,breaks=seq(from=0.5,to=12.5,by=1),plot=FALSE)
      
barplot(dens$density,space = 0,border=NA,col="#A40122",ylab="Density")
axis(side = 1,at = seq(from=0.5,to=11.5,length.out=12),labels = month.abb)
title(xlab="Month")
box(bty="l")
```

## Willow beauty

```{r,echo=FALSE,results=TRUE}
species.data <- moth.data[which(moth.data$Species=="Willow beauty"),]
total.caught <- sum(species.data$NumberCaught,na.rm=TRUE)
n.traps <- length(unique(species.data$TrapNumber))
first.year <- min(species.data$Year,na.rm=TRUE)
last.year <- max(species.data$Year,na.rm=TRUE)

species.data$Year <- factor(x = species.data$Year,levels = all.yrs)
```
In total, I have caught `r total.caught` individuals of Willow beauty, in `r n.traps` traps, between `r first.year` and `r last.year`.

```{r,echo=FALSE,results=TRUE}
yr.avg.catches <- tapply(X = species.data$NumberCaught,INDEX = species.data$Year,FUN = mean)
yr.avg.catches[is.na(yr.avg.catches)] <- 0

par(mar=c(3.2,3.2,0.5,0.2))
par(mgp=c(2.0,0.2,0))
par(tck=-0.01)
par(las=1)

plot(all.yrs,yr.avg.catches,xaxp=c(min(all.yrs),max(all.yrs),length(all.yrs)-1),xlab="Year",ylab="Average Catch",pch=16,type="b",col="#A40122",bty="l")

dens <- hist(species.data$Month,breaks=seq(from=0.5,to=12.5,by=1),plot=FALSE)
      
barplot(dens$density,space = 0,border=NA,col="#A40122",ylab="Density")
axis(side = 1,at = seq(from=0.5,to=11.5,length.out=12),labels = month.abb)
title(xlab="Month")
box(bty="l")
```

## Winter moth

```{r,echo=FALSE,results=TRUE}
species.data <- moth.data[which(moth.data$Species=="Winter moth"),]
total.caught <- sum(species.data$NumberCaught,na.rm=TRUE)
n.traps <- length(unique(species.data$TrapNumber))
first.year <- min(species.data$Year,na.rm=TRUE)
last.year <- max(species.data$Year,na.rm=TRUE)

species.data$Year <- factor(x = species.data$Year,levels = all.yrs)
```
In total, I have caught `r total.caught` individuals of Winter moth, in `r n.traps` traps, between `r first.year` and `r last.year`.

```{r,echo=FALSE,results=TRUE}
yr.avg.catches <- tapply(X = species.data$NumberCaught,INDEX = species.data$Year,FUN = mean)
yr.avg.catches[is.na(yr.avg.catches)] <- 0

par(mar=c(3.2,3.2,0.5,0.2))
par(mgp=c(2.0,0.2,0))
par(tck=-0.01)
par(las=1)

plot(all.yrs,yr.avg.catches,xaxp=c(min(all.yrs),max(all.yrs),length(all.yrs)-1),xlab="Year",ylab="Average Catch",pch=16,type="b",col="#A40122",bty="l")

dens <- hist(species.data$Month,breaks=seq(from=0.5,to=12.5,by=1),plot=FALSE)
      
barplot(dens$density,space = 0,border=NA,col="#A40122",ylab="Density")
axis(side = 1,at = seq(from=0.5,to=11.5,length.out=12),labels = month.abb)
title(xlab="Month")
box(bty="l")
```

## Wormwood pug

```{r,echo=FALSE,results=TRUE}
species.data <- moth.data[which(moth.data$Species=="Wormwood pug"),]
total.caught <- sum(species.data$NumberCaught,na.rm=TRUE)
n.traps <- length(unique(species.data$TrapNumber))
first.year <- min(species.data$Year,na.rm=TRUE)
last.year <- max(species.data$Year,na.rm=TRUE)

species.data$Year <- factor(x = species.data$Year,levels = all.yrs)
```
In total, I have caught `r total.caught` individuals of Wormwood pug, in `r n.traps` traps, between `r first.year` and `r last.year`.

```{r,echo=FALSE,results=TRUE}
yr.avg.catches <- tapply(X = species.data$NumberCaught,INDEX = species.data$Year,FUN = mean)
yr.avg.catches[is.na(yr.avg.catches)] <- 0

par(mar=c(3.2,3.2,0.5,0.2))
par(mgp=c(2.0,0.2,0))
par(tck=-0.01)
par(las=1)

plot(all.yrs,yr.avg.catches,xaxp=c(min(all.yrs),max(all.yrs),length(all.yrs)-1),xlab="Year",ylab="Average Catch",pch=16,type="b",col="#A40122",bty="l")

dens <- hist(species.data$Month,breaks=seq(from=0.5,to=12.5,by=1),plot=FALSE)
      
barplot(dens$density,space = 0,border=NA,col="#A40122",ylab="Density")
axis(side = 1,at = seq(from=0.5,to=11.5,length.out=12),labels = month.abb)
title(xlab="Month")
box(bty="l")
```

## Yellow-barred brindle

```{r,echo=FALSE,results=TRUE}
species.data <- moth.data[which(moth.data$Species=="Yellow-barred brindle"),]
total.caught <- sum(species.data$NumberCaught,na.rm=TRUE)
n.traps <- length(unique(species.data$TrapNumber))
first.year <- min(species.data$Year,na.rm=TRUE)
last.year <- max(species.data$Year,na.rm=TRUE)

species.data$Year <- factor(x = species.data$Year,levels = all.yrs)
```
In total, I have caught `r total.caught` individuals of Yellow-barred brindle, in `r n.traps` traps, between `r first.year` and `r last.year`.

```{r,echo=FALSE,results=TRUE}
yr.avg.catches <- tapply(X = species.data$NumberCaught,INDEX = species.data$Year,FUN = mean)
yr.avg.catches[is.na(yr.avg.catches)] <- 0

par(mar=c(3.2,3.2,0.5,0.2))
par(mgp=c(2.0,0.2,0))
par(tck=-0.01)
par(las=1)

plot(all.yrs,yr.avg.catches,xaxp=c(min(all.yrs),max(all.yrs),length(all.yrs)-1),xlab="Year",ylab="Average Catch",pch=16,type="b",col="#A40122",bty="l")

dens <- hist(species.data$Month,breaks=seq(from=0.5,to=12.5,by=1),plot=FALSE)
      
barplot(dens$density,space = 0,border=NA,col="#A40122",ylab="Density")
axis(side = 1,at = seq(from=0.5,to=11.5,length.out=12),labels = month.abb)
title(xlab="Month")
box(bty="l")
```

## Yellow-line Quaker

```{r,echo=FALSE,results=TRUE}
species.data <- moth.data[which(moth.data$Species=="Yellow-line Quaker"),]
total.caught <- sum(species.data$NumberCaught,na.rm=TRUE)
n.traps <- length(unique(species.data$TrapNumber))
first.year <- min(species.data$Year,na.rm=TRUE)
last.year <- max(species.data$Year,na.rm=TRUE)

species.data$Year <- factor(x = species.data$Year,levels = all.yrs)
```
In total, I have caught `r total.caught` individuals of Yellow-line Quaker, in `r n.traps` traps, between `r first.year` and `r last.year`.

```{r,echo=FALSE,results=TRUE}
yr.avg.catches <- tapply(X = species.data$NumberCaught,INDEX = species.data$Year,FUN = mean)
yr.avg.catches[is.na(yr.avg.catches)] <- 0

par(mar=c(3.2,3.2,0.5,0.2))
par(mgp=c(2.0,0.2,0))
par(tck=-0.01)
par(las=1)

plot(all.yrs,yr.avg.catches,xaxp=c(min(all.yrs),max(all.yrs),length(all.yrs)-1),xlab="Year",ylab="Average Catch",pch=16,type="b",col="#A40122",bty="l")

dens <- hist(species.data$Month,breaks=seq(from=0.5,to=12.5,by=1),plot=FALSE)
      
barplot(dens$density,space = 0,border=NA,col="#A40122",ylab="Density")
axis(side = 1,at = seq(from=0.5,to=11.5,length.out=12),labels = month.abb)
title(xlab="Month")
box(bty="l")
```

## Yellow-tail

```{r,echo=FALSE,results=TRUE}
species.data <- moth.data[which(moth.data$Species=="Yellow-tail"),]
total.caught <- sum(species.data$NumberCaught,na.rm=TRUE)
n.traps <- length(unique(species.data$TrapNumber))
first.year <- min(species.data$Year,na.rm=TRUE)
last.year <- max(species.data$Year,na.rm=TRUE)

species.data$Year <- factor(x = species.data$Year,levels = all.yrs)
```
In total, I have caught `r total.caught` individuals of Yellow-tail, in `r n.traps` traps, between `r first.year` and `r last.year`.

```{r,echo=FALSE,results=TRUE}
yr.avg.catches <- tapply(X = species.data$NumberCaught,INDEX = species.data$Year,FUN = mean)
yr.avg.catches[is.na(yr.avg.catches)] <- 0

par(mar=c(3.2,3.2,0.5,0.2))
par(mgp=c(2.0,0.2,0))
par(tck=-0.01)
par(las=1)

plot(all.yrs,yr.avg.catches,xaxp=c(min(all.yrs),max(all.yrs),length(all.yrs)-1),xlab="Year",ylab="Average Catch",pch=16,type="b",col="#A40122",bty="l")

dens <- hist(species.data$Month,breaks=seq(from=0.5,to=12.5,by=1),plot=FALSE)
      
barplot(dens$density,space = 0,border=NA,col="#A40122",ylab="Density")
axis(side = 1,at = seq(from=0.5,to=11.5,length.out=12),labels = month.abb)
title(xlab="Month")
box(bty="l")
```

## Yellow shell

```{r,echo=FALSE,results=TRUE}
species.data <- moth.data[which(moth.data$Species=="Yellow shell"),]
total.caught <- sum(species.data$NumberCaught,na.rm=TRUE)
n.traps <- length(unique(species.data$TrapNumber))
first.year <- min(species.data$Year,na.rm=TRUE)
last.year <- max(species.data$Year,na.rm=TRUE)

species.data$Year <- factor(x = species.data$Year,levels = all.yrs)
```
In total, I have caught `r total.caught` individuals of Yellow shell, in `r n.traps` traps, between `r first.year` and `r last.year`.

```{r,echo=FALSE,results=TRUE}
yr.avg.catches <- tapply(X = species.data$NumberCaught,INDEX = species.data$Year,FUN = mean)
yr.avg.catches[is.na(yr.avg.catches)] <- 0

par(mar=c(3.2,3.2,0.5,0.2))
par(mgp=c(2.0,0.2,0))
par(tck=-0.01)
par(las=1)

plot(all.yrs,yr.avg.catches,xaxp=c(min(all.yrs),max(all.yrs),length(all.yrs)-1),xlab="Year",ylab="Average Catch",pch=16,type="b",col="#A40122",bty="l")

dens <- hist(species.data$Month,breaks=seq(from=0.5,to=12.5,by=1),plot=FALSE)
      
barplot(dens$density,space = 0,border=NA,col="#A40122",ylab="Density")
axis(side = 1,at = seq(from=0.5,to=11.5,length.out=12),labels = month.abb)
title(xlab="Month")
box(bty="l")
```
